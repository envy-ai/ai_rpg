<task>Write at most two cohesive paragraphs showing the player receiving exactly the listed quest rewards.</task>

Writing style notes:
{{ setting.writingStyleNotes }}

Indexed rewards:
{% for reward in questRewards %}{{ loop.index }}. {{ reward }}
{% endfor %}

Create a compact coverage plan. For every indexed reward, state how it will appear in the scene. Do not invent another reward. Return XML only with one <reward> per listed index:
<rewardPlan><reward><index>1</index><presentation>brief plan</presentation></reward></rewardPlan>

{% llmparse('exact_xml_root', 'rewardPlan') %}

Draft Response: Write the complete player-facing reward prose. Include every listed reward exactly once as a reward. For an item, make its appearance and physical qualities concrete; for currency, state the exact amount and type. Output prose only.

{% llmparse('player_action_required_prose') %}

{% set tinyBrainAuditFocus = 'Every indexed reward must be received; exact currency amounts and types must be preserved; no unlisted reward may be invented; maximum two paragraphs.' %}
{% include "_includes/tinybrain-prose-audit.njk" %}

{% llmresult('quest_reward_result') %}
