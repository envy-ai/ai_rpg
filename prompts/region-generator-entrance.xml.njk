<template>
    <comment>This prompt runs after the region generator prompt, with the context from the region generator still incliuded, hence the lack of a system prompt and other context.</comment>
    <role>Scenery Image Generator</role>
    <generationPrompt>
From the locations above, which one makes the most sense as the entrance? If there's a tie or ambiguity, pick the one that seems the most interesting or evocative.
Output only the exact name of the location in this format:

<entrance>
<name>…</name>
</entrance>
    </generationPrompt>
</template>