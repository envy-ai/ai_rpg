# Docs Table of Contents

This index lists every other Markdown file under `docs/` with a brief description.

Recent player Abilities/Skills modal docs note: `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` document the chat-sidebar Abilities and Skills buttons plus `A`/`S` hotkeys, the fresh `/api/player` refresh before `#playerAbilitiesModal` or `#playerSkillsModal` opens, their no-backdrop/non-blurring `aria-modal="false"` behavior, Skills picker rank-then-name ordering, and click-to-insert `[Ability Name]` / `[Skill Name]` chat input behavior using the View modal ability-card and skill-card formats.
Recent combat NPC-turn config docs note: `config.md`, `api/chat.md`, and `server_llm_notes.md` document that ordinary post-player NPC turns are gated by `npc_turns.enabled` outside combat, while combat NPC turns are gated independently by `combat_npc_turns.enabled` during combat so combat turns can still run when regular NPC turns are disabled.
Recent event-entry isolation docs note: `classes/Events.md` and `classes/EventsEventTypes.md` document that independent multi-entry item/disposition event handlers warn and skip only failed entries while continuing later entries, with handler-level preconditions still failing loudly.
Recent travel-prose timing docs note: `api/chat.md`, `server_llm_notes.md`, and `slop_and_repetition.md` document that ordinary non-vehicle `<travelProse>` player movement now advances world time by the stored shortest graph route, falling back to parsed `<playerDestination><travelTime>` minutes only when no graph route exists, while direct metadata travel remains authoritative through `/api/player/move`.
Recent map fast-travel confirmation docs note: `ui/maps.md` and `api/players.md` document that Region Map, World Map, and Favorites fast travel now calls the read-only `GET /api/player/fast-travel-preview` route before travel, formats the shared helper's route time as hours/minutes, and only switches to Adventure after the player confirms.
Recent while-you-were-away arrival docs note: `classes/Player.md`, `classes/Globals.md`, `api/chat.md`, `config.md`, and `server_llm_notes.md` document that non-NPC `Player.setLocation(...)` snapshots destination visit state before marking locations visited, and that revisit threshold checks compare pre-arrival `lastVisitedTime` against canonical total world minutes.
Recent offscreen NPC activity docs note: `server_llm_notes.md` documents that offscreen NPC candidate last-mention previews are code-point truncated and XML-normalized before base-context rendering so emoji-heavy previews cannot split surrogate pairs inside CDATA.
Recent chat-history mutation docs note: `api/chat.md`, `api/scene-summaries.md`, `classes/SceneSummaries.md`, `server_llm_notes.md`, and `ui/chat_interface.md` document the generic-prompt-only `editChatLogEntry({ entry? | index?, content, reason? })` tool for direct chat-log replacement across structured and plain-text log entry types, including summary/structured-payload `getHistory` discovery, authoritative plain-text conversion via `metadata.editedPlainText`, `rerunSceneSummary({ sceneNumber, reason? })` for regenerating a stored scene-summary range with `redo: true`, and `editSceneSummary({ sceneNumber, summary, details?, quotes?, reason? })` for direct scene-summary edits.
Recent LLM retry docs note: `classes/LLMClient.md` and `server_llm_notes.md` document that per-attempt `ERROR_chatCompletionError_*` logs include attempt/max/will-retry metadata, including Axios/custom-serialized errors, and `api/chat.md`, `config.md`, and `server_llm_notes.md` document that while-you-were-away prompt completions must include a complete `<response>...</response>` wrapper before returning to parser logic.
Recent faction upsert tool docs note: `api/chat.md`, `api/factions.md`, `classes/Faction.md`, and `server_llm_notes.md` document the `upsertFactionFields({ operation, faction?, fields })` generic/scheduled mutation tool for creating real persisted `Faction` instances or updating existing factions from direct field/value pairs, including duplicate-name rejection and id-keyed relation validation.
Recent party mutation tool docs note: `api/chat.md`, `classes/Player.md`, and `server_llm_notes.md` document the generic/scheduled `updatePartyMembers({ add?, remove? })` chat tool for validated multi-NPC party membership changes, including off-location remote additions and removals placed at the current player location.
Recent crafting mod-field docs note: `api/crafting.md` documents that crafted/recovered item instantiation preserves registered first-class Thing fields parsed from generated item XML, such as `<implantSlot>`, and applies `clearThingSlotWhenPresent` before constructing the resulting `Thing`.
Recent modal interaction docs note: `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` document that shared image/thing lightboxes close when the visible pane or surrounding backdrop is clicked, and that Ctrl/Cmd+Enter in crafting, processing, modify-location, salvage, and harvest note/intent fields triggers the primary prose submit button rather than the no-prose alternate.
Recent entity-image upload docs note: `api/images.md`, `ui/chat_interface.md`, and `ui/modals_overlays.md` document `/api/images/upload`, the reusable `#entityImageUploadModal`, and `Upload Image` context menu entries for locations, things, NPCs, and the player, with PNG/JPEG/WebP/GIF uploads replacing the entity's current `imageId`.
Recent crafting equipped-drag docs note: `ui/chat_interface.md` and `ui/modals_overlays.md` document that dragging an equipped player item into a crafting slot asks whether to unequip it first, then assigns the item only after the existing equipment endpoint succeeds.
Recent Adventure layout docs note: `ui/chat_interface.md` and `ui/assets_styles.md` document that the nested `.chat-wrapper` allows flex shrinkage so wide chat descendants stay inside the remaining middle-column width instead of forcing the chat tab wider than the resized side panels allow.
Recent tool-call debug docs note: `config.md`, `ui/chat_interface.md`, `api/chat.md`, `ui/assets_styles.md`, `classes/SceneSummaries.md`, and `slashcommands/ScrubLegacyDebugCommand.md` document that visible `debug_tool_calls` Result/Error `content` fields are XML-entity-decoded into separate preformatted text blocks, with `[shown below]` placeholders left in the JSON tree and persisted tool payloads unchanged, that tool-call debug chat boxes expose the normal hover delete action whenever an entry id or timestamp is present while edit stays disabled, that system-role/diagnostic entries are excluded from both normal and generic prompt history plus scene-summary indexing, and that `/scrub_legacy_debug [dry_run]` explicitly cleans existing legacy check/tool debug pollution from chat history and stored scene summaries.
Recent location-favorites docs note: `classes/Location.md`, `api/common.md`, `api/locations.md`, `ui/chat_interface.md`, `ui/assets_styles.md`, and `ui/maps.md` document persisted `Location.favorite`, legacy-save defaulting to not favorite, `GET /api/locations?scope=favorites` returning favorite-card `computedTravelTimeMinutes`, `PUT /api/locations/:id/favorite`, the Play tab Favorites subtab using `star_solid.svg`, queued refresh during initial `#tab-favorites` hash activation, the Favorites tab's Factions-style dark rounded content box, computed favorite-card shortest-route travel-time labels, the yellow borderless current-location star toggle inset `4px` from the location image corner, and favorite-card travel reusing the world-map fast-travel helper.
Recent checked-container docs note: `api/things.md`, `api/chat.md`, `server_llm_notes.md`, `ui/modals_overlays.md`, `ui/chat_interface.md`, `ui/assets_styles.md`, `classes/Thing.md`, `classes/Events.md`, `classes/EventsEventTypes.md`, and `classes/EventsXmlEventSchema.md` document the first-class `requiresCheckToOpen` container flag, the `player-action-open-container` prompt route with required skill-check tool usage, `<permanentlyOpened>` parsing that clears future checks after a successful permanent open, slop removal, visible prose storage, normal event checks, the larger red lock overlay on checked container badges, and `consumeItem` destruction spilling instantiated container contents to the container's current holder or location before deleting it.
Recent location-stub region edit docs note: `api/locations.md`, `classes/Location.md`, `ui/modals_overlays.md`, `ui/maps.md`, and `ui/pages.md` document ordinary location stub Region editing through `PUT /api/stubs/:id` `targetRegionId`, live/pending region membership sync, preserved exits, and the distinction that region-entry stubs cannot retarget their represented region.
Recent delete-thing tool docs note: `api/chat.md`, `api/things.md`, `server_llm_notes.md`, `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` document the generic/scheduled `deleteThing({ thing })` mutation tool, its client confirmation-mode `player_input_request` panel, cancellation/unavailable tool errors that leave the world unchanged, and reuse of existing thing deletion protections such as non-empty container rejection.
Recent stack-merge docs note: `api/things.md`, `classes/Thing.md`, `classes/Player.md`, `classes/Location.md`, and `config.md` document automatic same-name/same-checksum item-stack merging when item stacks are dropped into locations or placed into actor/container inventories, the `mergeStacks: false` opt-out used by explicit split/separate flows, runtime `things` map cleanup for merged-away incoming Things, and generated NPC quantity groups preserving copied gear base names so dropped gear can stack later.
Recent scheduled-event docs note: `api/chat.md`, `classes/ScheduledEvent.md`, `classes/Utils.md`, `classes/IdGenerator.md`, `config.md`, `slashcommands/ScheduledCommand.md`, and `server_llm_notes.md` document the regular-prose `scheduleEvent({ event, region, location, in? | at? })` chat tool, persisted `scheduledEvents.json` records with compact `sevent_n` ids, base-context `dayIndex`/`timeMinutes` timing, due-event resolution through `scheduled-event-resolution` with mutation-capable built-in and registered mod chat tools except generic-prompt-only chat-history mutation tools, the default `scheduled_event_resolution: 5000` prompt-progress target, hidden `scheduled-event` summaries, visible `scheduled-event-prose` when the player is present, same-location player-action interruption rewriting through `player_action_interruption_rewrite` before slop removal, and `/scheduled` for listing pending scheduled events as readable markdown.
Recent mystery direct-edit tool docs note: `api/chat.md`, `classes/MysteryBox.md`, `classes/MysteryThread.md`, `server_llm_notes.md`, and `config.md` document the generic-prompt-only `updateMysteryBoxFields({ mysteryBox, fields })` tool for direct field replacement of mystery-box `name`, `keys`, and `text`, `updateMysteryThreadFields({ mysteryThread, fields })` for direct replacement of mystery-thread `name`, `status`, `keys`, `summary`, and `constraints`, plus top-level `max_tool_calls` as the configured tool-loop round limit.
Recent mod-hook docs note: `modding.md`, `modding_hooks.md`, `classes/ActorAttachmentSystem.md`, `classes/ItemModuleSystem.md`, `classes/ModExtensionRegistry.md`, `classes/ModLoader.md`, `classes/ModManager.md`, `classes/Player.md`, `classes/SettingInfo.md`, `classes/Thing.md`, `classes/Events.md`, `classes/EventsXmlEventSchema.md`, `api/chat.md`, `api/game.md`, `api/settings.md`, `api/things.md`, `config.md`, `ui/assets_styles.md`, `ui/chat_interface.md`, and `ui/pages.md` document runtime mod hooks, hybrid `mod.js` plus `defs/` mods, live chat-tool registration, player-action prompt step injection with automatic `1g`/`3k`-style numbering, registered XML event tags with prompt-rendered names/descriptions/XML examples, mod-owned first-class Thing fields exposed through live `createThing` / `updateObjectFields` schemas including optional structured `toolSchema`, generated item XML tags including craft/process/salvage/harvest outputs and JSON rendering for seeded array/object extension fields, and item edit modal fields, special-system Thing fields that clear normal equipment slots, mod-owned Thing image badges with SVG mask and raster rendering via `/mods/<mod>/assets`, mod-owned server-backed Thing context actions, dynamic badge asset/label overrides from world-profile settings, dynamic active-setting entity field descriptions, select/action/array mod setting fields for confirmed preset application and custom row editors, mod-owned World Profiles tabs, the top-level `/mods` manager, save metadata `enabledMods`, load-time mod mismatch handling, pending post-restart save loading, optional self restart, persisted actor `modState`, world-profile `modSettings`, installed item-module bonuses/effects, and the bundled `implants`, `need-bar-lust`, `modules`, and `spells` mods.
Recent module inventory docs note: `mods/modules.md`, `classes/ItemModuleSystem.md`, `api/chat.md`, `api/npcs.md`, `api/things.md`, `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` document that installed item modules remain raw Things held by the same holder as their base item for persistence/mechanics but are hidden as standalone entries in inventory UI, loose location item panels, crafting pickers, barter offers, and base-context inventory prompts; module-compatible items use an upper-left `modular.svg` badge that opens the module workbench and shows shadow-only occupied/total slots, installable modules use an upper-left `module.svg` badge, installed modules appear in tooltip/lightbox details with a small icon beside each module name, modules can be installed either through the workbench or by dropping loose module cards directly onto compatible modular item cards while transferring modules to the base item's holder, stacked loose modules split one single-count Thing for installation instead of moving/installing the whole stack, occupied slot thumbnails use contained scaling, and the `modules` preset id is the required default in `mods/modules/presets.yaml` for successful mod registration.
Recent mod generation-instruction docs note: `modding.md`, `modding_hooks.md`, `classes/ModExtensionRegistry.md`, and `mods/modules.md` document `registerGenerationPromptInstruction(...)` for dynamic item/location/region generator guidance, plus the modules mod's item-generation ratio guidance when persisted module items are below two per modular item.
Recent item tooltip docs note: `classes/Thing.md`, `ui/chat_interface.md`, `ui/modals_overlays.md`, and `modding_hooks.md` document that generated item descriptions keep descriptive prose only while structured mechanical fields render in tooltips, that item tooltips show attribute bonuses/equipper status effects for non-slot mod items such as implants, and that event-generated `<type>` values persist as `itemTypeDetail` while UI weapon detection also accepts older `metadata.itemType` records.
Recent mod UI design/plan note: `superpowers/specs/2026-05-23-mod-ui-hooks-design.md` defines the approved hybrid registry/client-runtime design for mod-owned UI pages, navigation entries, panels, actions, limited overrides, named extension slots, lifecycle events, shared asset injection, and bundled implant/spell UI hook examples. `superpowers/plans/2026-05-23-mod-ui-hooks.md` breaks that design into implementation tasks for registry hooks, mod scope helpers, manifest routes, shared template injection, the browser `ModUI` runtime, core slots/actions, bundled mod panels, tests, and docs.
Recent UI/NPC-turn docs note: `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` now document the docked prompt tracker between chat log and input, its always-visible idle state with only an italic translucent `no prompts running` label, persisted collapsed/one-line/table states, character-based progress payloads, one-line background progress fill with floored approximate percentage text, tightly spaced transparent white SVG view/restart/cancel controls placed before the prompt name with fully bright slight-glow hover/active treatment, fixed 60%-width weight-600 prompt names with ellipsis overflow and `(and N more)` text when multiple prompts run, table rows that omit `Target`, `Avg Out`, `Runs`, and `Avg/s` columns while retaining those payload/stat fields, right-aligned white compress/expand icon controls, 250 ms completion pulse, and modeless prompt/response viewer windows that spawn per view-click, keep the page interactive, retain their last prompt snapshots after live progress clears, and close only when explicitly closed; they also document the non-modal `#chatSpinnerStatusBar` between the prompt dock and chat input that replaces the old play-page blocking spinner overlay and now receives request-scoped chat progress text instead of temporary loading bubbles. `ui/assets_styles.md` also documents the Roboto Google Fonts import and default body font width/weight. `classes/LLMClient.md`, `server_llm_notes.md`, `config.md`, and `slashcommands/PromptStatsCommand.md` document prompt-progress character targets, average-output target selection, slower post-target asymptotic progress, persistent output-character stats that exclude tool-call-only completions, base-label aggregation for character-appended inventory/NPC stats, plus `/promptstats` and `/promptstats clear` for inspecting or clearing those averages. `api/chat.md`, `api/common.md`, `server_llm_notes.md`, and `ui/chat_interface.md` document live prompt-excluded `check-results` chat bubbles for skill, single-target attack, and grouped `resolveAreaAttack` tool-call results with concise collapsed summaries, existing-degree skill success/failure icons, attack hit/miss icons plus hit damage badges, and area-attack hit/no-hit summaries with per-target expanded details that reuse the regular attack breakdown for each target when available; `api/common.md`, `api/crafting.md`, `api/locations.md`, `server_llm_notes.md`, and `ui/chat_interface.md` document craft/process/salvage/harvest and location-modification success-degree outcomes using that same `check-results` rendering. `api/chat.md`, `api/common.md`, `server_llm_notes.md`, and `ui/chat_interface.md` document the visible prompt-excluded pending `npc-action` planned-action entry that appears during `next_npc_list`, renames to the acting NPC, and is finalized in place with the deslopped planned action while final NPC prose remains a separate turn entry. They also note that `npc_action` prose prompts use the same regular-prompt tool set as `player_action`, with omitted plausibility actors defaulting to the acting NPC, that cached repeated attack/area-attack/skill-check tool results append a no-rerun warning to the tool content returned to the model, that debug tool-call Result/Error `content` fields are XML-entity-decoded for display only, that vehicle location PIP previews use the same weather/light display-variant cache and refresh path as the main location image, and that `show_hidden_notes` controls Story Tools full-history visibility for player-action `<hidden>...</hidden>` notes while Adventure chat always filters them.
Recent player-modal refresh docs note: `ui/chat_interface.md`, `api/common.md`, `api/npcs.md`, and `modding_hooks.md` document that player View and Inventory buttons fetch fresh `/api/player` data before opening modals, character detail refreshes preserve `modStatusSections`, and `/api/npcs/:id` includes registered mod actor-status sections so tool-driven player mod-status changes stay visible.
Recent hidden-NPC docs note: `classes/Player.md`, `classes/Events.md`, `classes/EventsEventTypes.md`, `classes/EventsXmlEventSchema.md`, `api/chat.md`, `api/common.md`, `api/npcs.md`, `config.md`, `server_llm_notes.md`, and `ui/chat_interface.md` document persisted `hiddenFromPlayer`, client payloads that include hidden NPCs while the Adventure UI hides living hidden cards by default behind a `show hidden` eye toggle, the NPC-card `Toggle Hidden` context-menu action backed by `PUT /api/npcs/:id`, corpses always being visible, base-context `<hidden>true</hidden>` output only for living hidden NPCs, generated NPC `<hiddenFromPlayer>` parsing, XML/legacy `revealHiddenNpc` and `hideVisibleNpc` events including the event XML prompt's explicit list of current hidden NPC names, optional `hideFromPlayer` on NPC arrival/departure events, automatic no-modifier hide/perception checks when living hidden NPCs newly share the player's location, hidden living attackers being revealed by attack tools and `attackDamage` events, the `hide_hide_checks` client-visibility flag for failed automatic checks, and prose `revealEntity` / `hideEntity` tools.
Recent plot-analysis docs note: `config.md`, `api/chat.md`, `server_llm_notes.md`, `classes/Globals.md`, `classes/Utils.md`, `api/game.md`, and `slashcommands/PlotAnalysisCommand.md` document the default-on `plot_analysis.enabled` config gate, `plot_analysis.max_plot_threads` and `plot_analysis.max_plot_complications` blurb-limit settings, the non-blocking per-player-action `plot-analysis` base-context prompt, newest-completion global storage in `Globals.plotAnalysis`, save metadata persistence via `metadata.plotAnalysis`, base-context `<plotAnalysis>` injection for non-empty analysis only, the readable `plot-analysis-blurb.njk` thread/complication include, and `/plot_analysis` Markdown display.
Recent request-input docs note: `api/chat.md`, `config.md`, `server_llm_notes.md`, `ui/chat_interface.md`, and `ui/modals_overlays.md` document the `requestUserInput({ question })` chat tool, the `chat_tools.request_user_input_enabled` default-on config gate, `/api/chat/user-input-response`, and the draggable non-modal `#playerInputRequestPanel` that appears on every tab sharing the originating prompt client id without blocking the interface below.
Recent transfer-item docs note: `classes/Events.md`, `classes/EventsEventTypes.md`, and `classes/EventsXmlEventSchema.md` document that `transfer_item` now ensures handed/traded items exist by using giver inventory first, then matching unowned current-location items, then generating any missing shortfall before giving the exact quantity to the receiver.
Recent summary docs note: `api/chat.md` and `server_llm_notes.md` document that automatic post-turn summary-threshold work, including scene-summary prompts, now runs fire-and-forget after random events/NPC turn handling and no longer delays chat completion/response delivery; failures still warn in the server console.
Recent mystery-thread docs note: `classes/MysteryThread.md`, `classes/MysteryBox.md`, `api/mystery-threads.md`, `api/mystery-boxes.md`, `classes/Events.md`, `api/chat.md`, `api/game.md`, `config.md`, `classes/Utils.md`, `classes/IdGenerator.md`, `server_llm_notes.md`, and `ui/chat_interface.md` document reactive `mysteryBoxMention` handling, the private `mystery-thread-check` prompt that parses `<resolvedMysteries>` and can inactivate resolved active threads or mark individual boxes resolved before updates, the private tool-enabled `mystery-box-update` prompt, active mystery-thread base-context injection capped by `mystery_threads.max_active` with resolved boxes filtered out, persisted `mysteryThreads.json` / `mysteryBoxes.json` continuity records, compact `mthread_n` / `mystery_n` ids, legacy inactive-thread migration, Story Tools thread editing, and the `listMysteryBoxes` / `findMysteryBoxes` / `getMysteryBox` / `listMysteryThreads` / `getMysteryThread` chat tools.
Recent world-profile docs note: `ui/pages.md`, `api/settings.md`, `api/game.md`, `classes/SettingInfo.md`, `classes/UnifiedTonalScale.md`, `modding.md`, `config.md`, and `server_llm_notes.md` document the `/settings` Tone Scale tab, persisted `SettingInfo.unifiedTonalScale` selections/comments, the `/settings` structured Calendar tab with dynamic month/day selectors and stale-reference validation, persisted `SettingInfo.calendarDefinition` drafts, setting-calendar generation/default routes, `defs/unified_tonal_scale.yaml` rendering with generated half-step dropdown values such as `3.5 - Hopeful/Mixed`, insertion of the generated tonal-scale system-prompt section before `extra_system_instructions`, and world-profile hide/perception selectors sourced from defined attributes/default skills with `setting_hide_perception` load-time backfill for legacy saves.
Recent barter docs note: `config.md`, `api/npcs.md`, `api/common.md`, `classes/Player.md`, `classes/Events.md`, `classes/EventsEventTypes.md`, `classes/EventsXmlEventSchema.md`, `server_llm_notes.md`, `ui/chat_interface.md`, and `ui/modals_overlays.md` document NPC barter sessions, persisted NPC barter inventory, trade willingness/refusal expiry, the `barter-prices` base-context prompt with returned-existing-offers-only trade availability, name-first existing-item matching, warning-only bad ids plus warning-sanitized Unicode replacement characters before strict XML parsing, refreshed merchant currency on stock refresh, batched `inventory-generator` instantiation of generated barter stock via `barter.generated_stock.max_items_per_prompt`, trade/haggle/commit/conclude endpoints for current-location or party-member NPCs that use the same disposition-threshold hostility heuristic as client `isHostileToPlayer`, standalone `⚖️ Trade` event-summary blocks for committed item/currency exchanges, merchant-currency shortfall confirmation/partial payment, client-rendered haggle speaker labels, async merchant NPC actions after committed trades, merchant NPC actions after concluded haggles, `tradeAvailability` event processing, and the Adventure NPC-card trade icon plus confirmation-gated drag/drop barter modal with loading inventory spinners, persistent coin value badges, and hidden unavailable offers.
Recent travel/time docs note: `api/chat.md`, `server_llm_notes.md`, `classes/Events.md`, `classes/EventsEventTypes.md`, `classes/EventsXmlEventSchema.md`, and `slop_and_repetition.md` document player-action `<finalProse><timePassed>` parsing as the primary action elapsed-time source, with that block stripped from visible prose and passed into event checks as `initialTimeProgress` so event-check `timePassed` / `time_passed` is only a fallback. They also document same-location scheduled events inside that elapsed interval interrupting normal final prose before slop removal, with split time advancement and a rewritten player-action XML response that preserves hidden/time fields. They also document `<travelProse><playerDestination><travelTime>...</travelTime></playerDestination></travelProse>` parsing, including generated-exit duration normalization and the rule that travel prose uses structured travel-time fields, not a top-level `<timePassed>` block. Timed vehicle travel refresh handling remains documented: travel-prose event checks for players on location/region vehicles combine origin/between/destination prose at the original onboard location, event-check moves directly to an active in-motion vehicle destination are suppressed while leaving fallback elapsed time available for arrival finalization, timed vehicle state changes request a final client location refresh after event processing, ordinary no-tool player actions are still included as `<playerAction>` in event-check prompts, and relevant due-arrival finalization records the normal vehicle-movement event summary. They also document that Adventure exits and map summaries suppress the containing region vehicle icon for ordinary exits between locations inside the current vehicle region, while retaining icons for actual vehicle destinations and inbound boarding exits. They also document that player-action XML parsing now strips to the last complete `<finalProse>`, `<travelProse>`, or `<rejected>` block before strict parsing so earlier draft/thinking tags do not win.
Recent time-parser docs note: `classes/Utils.md`, `classes/StatusEffect.md`, and `classes/Events.md` document parser-only tolerance for second-unit model mistakes, which are rounded to the nearest minute. Prompt, API, slash-command, and UI guidance continue to request minute-or-larger duration units.
Recent crafting/container docs note: `api/crafting.md`, `api/things.md`, `ui/chat_interface.md`, `ui/modals_overlays.md`, `ui/assets_styles.md`, `classes/Thing.md`, `classes/Events.md`, `classes/EventsEventTypes.md`, and `classes/EventsXmlEventSchema.md` document that workbench craft/process pickers include current-location items and scenery plus item contents inside current-location containers alongside player inventory, including contents of scenery containers while keeping nested container contents item-only; submitted inputs are limited to player inventory, loose current-location items/scenery, or current-location container contents, and non-empty containers are still greyed out in the crafting inventory and rejected as crafting inputs until emptied, with emptiness determined from normalized nonblank contained thing ids and refreshed from cached container payloads when a container is emptied while the craft picker is open. Location container cards are direct drop targets for loose location items and current-player modal inventory items, highlight on valid hover, and consume the drop before item/scenery grid conversion can run. Dragging an item out of an inventory-style modal or either container-modal column drops it into the current location via `/api/things/:id/drop`, with contained items detached from their containing Thing before placement. Opening a container with pending generated `containerContents` seeds now shows a vendor-style spinner and generated-content message until the container inventory response returns, while exact empty sentinel names and zero-count seeds are discarded so loaded empty containers do not fire `thing-generator-contents` and instead show empty-container text. Containers with `requiresCheckToOpen` open a player-description check modal and run `player-action-open-container` before showing inventory. The XML event pipeline also supports `putItemInContainer` and `removeItemFromContainer`, moving exact item quantities between actor inventory or current-location loose items and existing direct container contents; remove events generate any missing direct-content shortfall into the container before removal, and `consumeItem` spills instantiated contents before deleting a destroyed container.
Recent location-repair docs note: `api/locations.md`, `ui/maps.md`, `ui/modals_overlays.md`, and `ui/pages.md` document explicit map context-menu stub expansion via `POST /api/stubs/:id/expand`, plus location-editor region relocation through `POST /api/locations/:id/relocate` with server-side region membership repair, selected exit cleanup, pending-region `unstub first` handling, and optional target-region entrance assignment.
Recent mobile-drag docs note: `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` document movement-threshold touch dragging for modal inventory item cards, container transfers, barter offers, and NPC party recruit/dismiss cards, replacing the prior long-press gesture while keeping native long-press image/callout suppression on thing icons.
Recent while-you-were-away docs note: `config.md` and `server_llm_notes.md` document optional `<itemSceneryMoves>` parsing for reunion prompts, including moving matching existing origin-location things to the arrival location while warning and ignoring names that are not at the origin or are in character inventory, clamping parsed need-bar percentages to `0..100`, requiring an affirmative pre-arrival visit snapshot before the prompt can run, and hard-ignoring `thing_move_with_character` during the scoped follow-up event check.
Recent slop docs note: `api/chat.md`, `server_llm_notes.md`, and `slop_and_repetition.md` document that visible `while-you-were-away-player` reunion prose uses the shared slop-removal pipeline and records the usual linked `slop-remover` attachment when it runs, that `defs/slopwords.yaml` `forbidden_tropes` entries are exposed to slop-remover prompts as context only, and that slop-remover XML validation/parsing extracts the final complete XML block before strict parsing. Story Tools now refreshes its full-history view after normal chat completion, so prompt-generated `while-you-were-away` and `while-you-were-away-player` entries appear without waiting for a tab reactivation.
Recent scene-tool docs note: `api/chat.md`, `api/scene-summaries.md`, `server_llm_notes.md`, `classes/SceneSummaries.md`, and `ui/chat_interface.md` document the `getFullScene({ sceneNumber })` chat tool, which resolves stored `Scene N` entries listed inside `<olderStoryHistory>` through the shared scene-summary index and returns delineated user/NPC action inputs plus prose for the stored scene, `rerunSceneSummary({ sceneNumber })` for regenerating stored ranges, plus the Story Tools `Scene Summaries` tab for viewing and editing persisted scene-summary text/details/quotes.
Recent chat-tool docs note: `api/chat.md`, `api/locations.md`, `server_llm_notes.md`, `classes/LLMClient.md`, and `classes/base_context_history.md` document `moreInfo({ name, type?, includeFullState? })` returning compact JSON by default, with bulky/runtime character fields plus region/location scaffolding omitted unless `includeFullState: true` is explicitly requested while region `secrets` remain visible, `@`/`@@`/`@@@` generic prompts receiving ordinary stored chat log entry types with renderable text in base context while still excluding system-role/diagnostic entries, `getHistory` searching every stored chat log entry type for those same generic prompts while regular prompts keep assistant-prose-like history search, dedicated `ERROR_tool_call_failed_*` JSON logs for failed tool calls with tool name, parameters, and backtrace, the generic/scheduled `createNpc(...)` world-mutation tool that delegates to single-NPC generation with Add NPC style seed fields, generic-prompt-only chat-log mutation tools for entry replacement and scene-summary reruns, plus the direct field patchers: legacy `updateCharacterFields({ character, fields })` for NPC fields, including nested `personality.type`/`traits`/`notes`/`aiNotes` mapping, and `updateObjectFields({ objectType, object, fields })` for allowlisted fields on things, locations, exits, regions, factions, player quests/objectives, and status effects, including full candidate JSON on ambiguous names.
Recent save-format docs note: `classes/IdGenerator.md`, `classes/Utils.md`, `classes/Globals.md`, and `api/game.md` document save version `1.2`, compact per-type persisted IDs (`char_n`, `thing_n`, `loc_n`, `exit_n`, `region_n`, `faction_n`, `quest_n`, `obj_n`, `status_n`, `sevent_n`), persisted `metadata.idCounters`, and old-save migration of exact structured references before hydration.
Recent prompt-viewer docs note: `ui/chat_interface.md`, `ui/modals_overlays.md`, and `ui/assets_styles.md` document prompt-progress streamed-response viewers as modeless floating dialogs that spawn per view click, do not block the page, preserve the last prompt/response snapshot after live progress clears, close only from their own close buttons, and keep a checked `Follow` pane scrolled to the bottom during throttled live updates.
Recent prompt logging docs note: `classes/LLMClient.md` and `server_llm_notes.md` document prompt-labeled error log filenames such as `ERROR_chatCompletionError_events-xml_<timestamp>.log`, including the separate chat-completion error label resolution path used when model override labels differ from prompt names.
Recent prompt parsing docs note: `server_llm_notes.md` documents base-context CDATA prompt-body wrapping for both `<systemPrompt>` and `<generationPrompt>` plus splitting for literal `]]>` sequences in dynamic game/history text, `server_llm_notes.md` and `classes/LLMClient.md` document `LLMClient.logPrompt()` artifacts for pre-LLM template parse failures, and `classes/SceneSummaries.md` plus `server_llm_notes.md` document scene-summary parsing of only the final `<scenes>` XML block after Step 1/2 prose.
Recent scene-summary coverage docs note: `classes/SceneSummaries.md` and `server_llm_notes.md` document that generated scene-summary results anchor stored first/last scene bounds to the requested summarized range when the model skips leading/trailing prompt entries, preventing permanent coverage gaps that retrigger automatic scene-summary prompts every turn.
Recent chat drawer docs note: `ui/chat_interface.md` documents routine `time_passed` rows moving from the `Time` body category to a right-aligned elapsed-time label in the collapsed `What changed` drawer header, with the `Time` category omitted when that routine elapsed-time row was its only item, elapsed-time rows excluded from the button count, and time-only drawers showing a greyed-out `What changed (0)` button. It also notes that direct moves now always include a travel row, add elapsed time when applicable, and attach their summaries to visible arrival prose, prior travel prose, or the travel user/comment row; legacy parentless direct-travel summaries are still attached to following visible arrival prose client-side, unexplored direct/bypass exits run travel prose with authoritative exit metadata while explored bypass exits keep using comment-only travel logging, `new_exit_discovered` rows can render map pills that open the origin-region map focused on the discovered location/region bubble, and need/disposition summary rows now collapse per character with configured icon/delta pills while preserving the existing detailed text inside the open row, with `all`/`fill` need-bar changes displaying the signed bar maximum in the pill.
Recent quest docs note: `classes/Quest.md`, `classes/Events.md`, `classes/QuestConfirmationManager.md`, `api/quests.md`, `api/common.md`, `ui/chat_interface.md`, `ui/modals_overlays.md`, and `server_llm_notes.md` document quest `rewardNpcDispositions`, generated `<npcDispositions>` reward parsing with `<reason>`, save/load persistence, quest-completion disposition application through the normal `dispositionChanges` notification format, warning-and-skip behavior for unknown NPC reward targets, quest-list NPC disposition rewards rendered as chat-style icon/delta pills without printing the stored reason text, current-player `dispositionDefinitions` in the client payload for quest reward dropdowns, quest-editor row editors for item rewards, faction reputation rewards, and NPC disposition rewards with configured disposition-type dropdowns and full-width second-row reason fields, plus quest-editor `giverName`, `paused`, and `rewardClaimed` controls.
Recent NPC progression docs note: `server_llm_notes.md` documents that initial NPC attributes remain priority-based, initial NPC skills and `/respec_skills` now prompt for exact skill point allocations from formula-derived pools, NPC level-up progression prompts for exact skill/attribute point allocations, and returned under/over point totals are warning-logged and randomly corrected.
Recent NPC generation docs note: `config.md` and `server_llm_notes.md` document generated NPC `<quantity>` parsing, the `npc_generation.max_quantity` cap, post-progression quantity expansion, per-NPC copied numbered gear, post-creation banned/slop-name enforcement for single/event NPC generation, and the concise generated-location spinner wording used for the location NPC pass.
Recent event-departure docs note: `classes/Events.md`, `classes/EventsEventTypes.md`, `classes/EventsXmlEventSchema.md`, and `server_llm_notes.md` document concrete non-current `npc_arrival_departure` destinations, including offscreen location stubs for unresolved locations in known regions and offscreen pending-region plus child-location stubs for unresolved regions, without creating player-visible exits.
Recent XML event docs note: `classes/Events.md`, `classes/EventsEventTypes.md`, `classes/EventsXmlEventSchema.md`, and `server_llm_notes.md` document `newExitDiscovered` using `<destinationType>` for XML destination kind, promoting it to `region` when `<destinationHasNewExits>true</destinationHasNewExits>` is present, and promoting an originally `location` destination's `locationName` into the region target/name while clearing the destination location field, split XML `<npcArrival>` / `<npcDeparture>` tags mapping into the legacy `npc_arrival_departure` parser and handler, `<thingArrival>` / `<thingDeparture>` mapping into `thing_arrival_departure` for existing item/scenery movement and offscreen tracking, `<thingMoveWithCharacter>` mapping into `thing_move_with_character` so existing things can follow player, party, or NPC movement, XML `<putItemInContainer>` / `<removeItemFromContainer>` mapping into container item movement handlers, NPC-turn `item_appear` / `scenery_appear` processing remaining enabled despite the player-movement lock, and `item_appear` incrementing exact same-name scene item stacks instead of silently skipping them.
Recent incapacitation docs note: `classes/Events.md`, `classes/EventsEventTypes.md`, `classes/Player.md`, and `slashcommands/IncapacitateCommand.md` document that incapacitation preserves current health, applies the explicit `Incapacitated` status effect, and lets that status drive `Player.isDisabled`.

## Root docs

- [API_README.md](API_README.md) — High-level index of Express routes registered in `api.js`, pointing to detailed endpoint docs in `docs/api/`.
- [config.md](config.md) — Configuration options for gameplay behavior (including selectable text backends via `ai.backend`, shared `ai.baseTimeoutSeconds` request timeout behavior with Codex bridge idle-timeout reset on streamed data, the local `codex_cli_bridge` transport and its `ai.codex_bridge.*` options such as `reasoning_effort`, generic `oauth-key`/`oauth-url` refresh-token auth for OpenAI-compatible requests, Codex `developer_instructions` handling for bridge/system prompts, fresh-only Codex concurrency via `ai.max_concurrent_requests` with resumed sessions still serialized, disposition first-impression multiplier config, `chat_tools.request_user_input_enabled` for the player-question chat tool, `plot_analysis.enabled` for the background plot-analysis prompt plus `plot_analysis.max_plot_threads` / `plot_analysis.max_plot_complications` blurb warning defaults, point pool formulas, difficulty DC, outcome-margin, and inclusive critical-roll threshold formulas with location-level variables, AI `custom_args` payload injection with per-key override merging, AI `headers` request-header injection with per-key override merging and null-delete semantics, AI `cachebuster` enable/disable final-user-message tagging, AI `waitAfterError` plus `waitAfterRateLimitError` retry-delay configuration with per-prompt override support, `prompt_progress.character_targets` for character-based prompt tracker progress including scene illustration prompts, extensible `random_event_frequency` file-based event types and disabled-state seed-pool gating, event-check enablement plus `event_checks.use_xml` for the default XML event pipeline with separate need-bar prompt vs legacy grouped prompts, barter stock/refusal/session timing under `barter`, generated NPC quantity caps under `npc_generation`, faction generation count behavior plus settings-level override precedence and default example count `7`, config-driven chat completion sound cues, world-time config, passive `healthRegenPercentPerMinute` regeneration with fractional internal health and upward-rounded client display, merged-config mod enable/disable flags under `mods.<name>.enabled`, save/load mod mismatch handling, `server.allowSelfRestart` and `server.selfRestartPortRetrySeconds`, image-generation prompt retries/batching plus item/scenery size overrides, ComfyUI location weather/lighting image variant settings with the default Flux2 Klein img2img workflow and ComfyUI-console prompt printing, slop-remover base attempts, player level-up ability draft counts, `extra_plot_prompts` automatic hidden-prompt toggles, `prompt_uses_caching` base-context/slop-remover behavior, setting-level tonal scale insertion before `extra_system_instructions`, `use_legacy_prompt_checks` for restoring separate attack/plausibility prompts instead of prose tool-call checks, `debug_tool_calls` visible prompt-excluded tool-call diagnostics with cache-hit markers and collapsible `@andypf/json-viewer` formatting, `show_hidden_notes` Story Tools full-history visibility for player-action hidden-note tags, `hide_hide_checks` failed automatic hidden-NPC check visibility, `recent_history_turns` vs `client_message_history` history-window behavior, plot-expander cadence config, the `while_you_were_away_threshold_minutes` destination revisit threshold for reunion prompts, startup `--config-override` YAML layering, and loaded-save per-game YAML overrides via the `/config` page).
- [developer_overview.md](developer_overview.md) — Quick-start developer overview of the game, architecture, and where to look first.
- [modding.md](modding.md) — Mod defs overlay support, merge semantics (deep-merge objects, append arrays, later mod wins on scalars), defs-only and hybrid `mod.js` plus `defs/` mods, runtime hook registration including player-action prompt steps, merged-config `mods.<name>.enabled` gating with `config.json.enabled` fallback, top-level `/mods` checkbox management, save metadata `enabledMods` mismatch handling including newly added vs missing mod lists in the load modal, pending post-restart save loading, startup validation, restart-required mod toggles, safe reload behavior, root `defs/unified_tonal_scale.yaml` tone-axis definitions used by the World Profiles Tone Scale tab, explicit need-bar audience flags (`player` / `party` / `non_party`) with legacy `player_only` compatibility, directional `need_values` overrides (`increase` / `decrease`) with fallback to the matching global defaults (including preserved decimal magnitudes), `change_per_minute` baseline drift definitions, and `while_you_were_away_prompt_notes` guidance for reunion prompts.
- [modding_hooks.md](modding_hooks.md) — Runtime mod hook overview, player-action prompt step injection, actor `modState`, setting `modSettings`, generic actor attachment/item-module/activatable helpers, bundled implants/need-bar-lust/modules/spells mods, and spell mana-cost formula variables.
- [minute_time_migration.md](minute_time_migration.md) — Implementation plan for migrating canonical time from decimal hours to minutes, including the original parser contract (`HH:MM` and day/hour/minute/round combinations), prompt updates, schema/API/save migration steps, and validation checklist.
- [potential_redundancies.md](potential_redundancies.md) — List of potential redundancies/inconsistencies found across docs and code, with suggested fixes.
- [playwright.md](playwright.md) — Playwright browser test setup and run commands for headless/headed Chromium, including same-user existing-X-session helper usage, one-off new-game flow automation, mode-selectable deterministic fixture-based playthrough regression replay (attack and cross-region round-trip checks), deterministic new-game vehicle-region regression from captured region-generation logs (with retained post-test save for manual inspection and Adventure UI vehicle-exit icon rendering assertions), settings persistence regression coverage (create/rename/delete/refresh plus saved calendar persistence), and a one-off settings-page capture/validation script for live UI snapshots.
- [run_all_combinations.md](run_all_combinations.md) — Temp prompt-comparison helper script docs for `tmp/glm-5.1_vs_glm-5/run_all_combinations.js`, covering config/system-prompt discovery, ordering, repeat count `10`, and combined-output generation.
- [run_prompts.md](run_prompts.md) — CLI helper script docs for `scripts/run_prompts.js`, including prompt-template arguments, repeated runs, XML extraction, and one-off `--config` override support.
- [server_llm_notes.md](server_llm_notes.md) — Deep notes on `server.js`, `api.js`, `Events.js`, and `LLMClient.js` responsibilities and flow, including backend-aware AI configuration validation (`openai_compatible` vs `codex_cli_bridge`), CDATA extraction for base-context `<systemPrompt>` and `<generationPrompt>` content that needs to mention literal XML examples, server-side Nunjucks `eval` filter support for nested string templates, compact base-context `<needs>` bullet rendering from need-bar threshold sentences plus strict need-sentence validation on new-game/load and warning-only validation on live reloads, base-context status-effect `<name>`/description/duration rendering, older/recent history split tags without a dangling recent-story separator, compact base-context item/inventory value labels, party-member exclusion from `<currentLocation><npcs>` because `<playerParty>` lists them separately, the mirrored `<currentConditions><currentCharacterNeeds>` quick-reference needs block for the player/party/on-location NPCs, outside-visible weather rendering as `<weatherOutside>` in base-context current conditions, NPC important-memory fallback selection plus deferred `choose_important_memories` background reranking so foreground prompts keep priority while skipping dead NPC/corpse actors, post-response party-memory scheduling plus low-priority background LLM semaphore behavior, NPC/player inventory generation through the base-context `inventory-generator` include prompt, NPC `<startingNeeds>` parsing into percent-of-max starting values plus persisted per-actor need-bar applicability, generated NPC `<startingHealthPercentage>` handling (including `%` stripping and `deceased` persistent corpses), same-location duplicate-NPC skip/reuse behavior during NPC generation, character portrait pre-job/image-job de-duping, shared item-prompt guidance for stacked-item naming/description when seed fields are blank, persisted prompt-scale `metadata.unscaledAttributeBonuses` for prompt-backed item round-tripping, shared API-scope wiring for prompt-backed thing separation, prompt-specific omission of `event-summary` history for `location-generator-stub` and `region-generator`, region exit-travel-time backfill behavior (`0` as unpopulated, prompt-generated zero normalized to `1`, plus `<stubRegion><travelTime>` forwarding onto created cross-region exits), the dedicated parallel `need_bar_event_checks` prompt path (submitted before grouped event checks), while-you-were-away arrival prompts that skip first-time/unvisited destinations and too-recent revisits based on destination `lastVisitedTime`, allow empty `<characterUpdates>`, and convert absolute need-bar `<value>` percentages to raw bar ranges after nonnumeric text stripping and blank/`N/A` skips, per-need `while_you_were_away_prompt_notes` plus existing-NPC name/alias `HERE` arrival parsing, current-party `HERE` arrivals staying party-only at the player's arrival location, the scoped while-you-were-away event-check pass that suppresses need-bar prompting and ignores need/arrival categories, inactive-need-bar suppression with warn-only handling for unknown identifiers, warn-and-skip handling for unresolved `HERE` arrivals, per-line indentation stripping for parsed `proseForPlayer`, and visible `while-you-were-away-player` chat entries, and Codex app-server-backed live preview streaming that decodes structured-output deltas back into plain assistant text while deriving Codex received-character counts from those decoded deltas.
  - `server_llm_notes.md` also notes that empty player-action continuations bypass plausibility and render the player-action prompt with empty `actionText`, player-action XML `<rejected>` responses display the reason while excluding both the triggering input and rejection message from future base context before aborting the turn, the legacy attack precheck/check prompts are disabled in favor of `resolveAttack`, the automatic player-action plausibility prompt call is disabled in favor of `resolveSkillCheck` / `resolveOpposedSkillCheck`, NPC plausibility parsing currently keeps only `<description>`, and plausible unopposed action DCs, outcome margins, and critical-roll gates come from `config.formulas.*` with current-location `level`.
  - `server_llm_notes.md` also documents scene-summary range-validation diagnostics, the shared scene-summary index used to keep caller/indexer entry counts aligned, and relative in-world time/date labels for scene-summary blocks plus non-assistant full history entries in base-context.
  - `server_llm_notes.md` also documents location/region alternate-name filtering against banned name fragments, slop words, and active-setting custom slop words before regenerated names are assigned.
- [slash_commands.md](slash_commands.md) — Quick guide to slash command lifecycle, shape, arg parsing, interaction API (`interaction.user.id`, `interaction.clientId`, `interaction.argsText`, history helpers including `interaction.getHistory(query, options?)` with string/string-array query support and optional pagination args, save helper access, shared world-time adjustment via `interaction.adjustWorldTimeByMinutes(...)` including positive time-jump need/status processing plus current player/location refresh requests, explicit `interaction.requestClientRefresh(...)` for invoking-tab redraws, shared region exit-travel-time backfill via `interaction.backfillRegionExitTravelTimes(...)` including `force` regeneration support, and shared upload/import helpers), typed reply actions including the reusable file-upload modal contract plus per-command `showExecutionOverlay` suppression for immediate UI actions, best practices including reuse of the shared alias-aware character-targeting helper, example, and testing.
- [slop_and_repetition.md](slop_and_repetition.md) — Overview of slop checking and repetition-busting systems, including configured-PPM ngram filtering (`defs/slopwords.yaml` `ngrams`), configured regex matching (`defs/slopwords.yaml` `regexes`) that strips asterisks before matching, regex names listed in slop-remover prompts and a current-response-only optimization for `ppm: 0` entries, `defs/slopwords.yaml` `forbidden_tropes` exposed to slop-remover prompts as context only, active-setting `customSlopWords` additions (single-word => slop words, multi-word => ngrams), game-intro and visible while-you-were-away slop-removal coverage, the standalone-vs-base-context slop-remover template switch controlled by `prompt_uses_caching`, final-block strict XML-validated `<editedText>` parsing for slop-remover responses with hidden-note preservation, final-root player-action XML extraction before strict parsing, travel prose output normalization and player-action scaffold scope, and detection logic (including retained `could`/`would` variants plus NPC name/alias token exclusion in k-gram normalization).

## UI docs (`docs/ui`)

- [ui/README.md](ui/README.md) — UI documentation index and scope.
- [ui/pages.md](ui/pages.md) — Route-to-template map with scripts including the chat page's vendored `json-viewer.js`, injected data, the shared `views/_includes/head-common.njk` page-head partial and pending-load script, shared classic crossed-swords SVG favicon wiring, shared `views/_includes/app-header.njk` app header with `Worlds`/`Mods`/`System` navigation labels, Tools disclosure, and phone-width embedded Tools menu behavior, and key form notes (including default skills prefill, new-game skill add/remove plus start-time hour input, remote new-game form settings save/load, blank-safe attribute alias matching during load, load-time NPC-alias generation confirmation for legacy saves, the Save Mods Differ load modal with newly added/missing mod groups, the `/mods` checkbox manager page, master-detail world-profile manager layout with tabbed editor sections including settings-local faction generation/editing, hide/perception selector fields, and the tonal-scale selector, stable tab pill sizing, container-scoped library/editor scrolling, search/sort library controls, server `/config` page tabs plus Debug Tool Calls, image prompt batching controls, and live per-game YAML override editing, settings prompt-guidance custom slop words list behavior, region edit/weather modals, the location/map context-menu tabbed calendar field editor plus the shared set-last-seen modal, the location Local Weather selector's exposed/outside-visible/sheltered/automatic modes, region edit modal parent/faction plus shared vehicle editor with active-destination editing semantics across `currentDestination`/`pendingDestination` including unresolved named pending targets and `New Region` actions for both active and fixed-route destinations, inside->outside vehicle-exit select behavior, collapsed secrets editor handling with overlay scrolling and no dialog max-height cap, stub-mode vehicle editing reuse in the shared location editor, settings rename/new-id behavior, persistent settings delete behavior, shared allocation partials, and allocation pool behavior/timing).
- [ui/chat_interface.md](ui/chat_interface.md) — Main chat UI layout, behavior, data flow, player-point warning indicator behavior, left-sidebar world-time indicator + transition summaries, current-location weather/lighting image variant swapping with a session display cache to avoid base-image flashes on UI refresh, region weather editor and tabbed calendar field editor modals plus the location `Set Last Seen` action/modal (using the same `H AM/PM`, `H:MM AM/PM`, or `duration ago` input as `/set_last_seen`), location `hasWeather` selector with outside-visible mode, unlabeled light-level line + conditional weather line + weather/light-threshold event summaries (with no-weather light-update suppression) plus immediate slash-command/realtime clock refreshes when `chat_history_updated` carries `worldTime`, direct-move/player-teleport chat refreshes so visible `while-you-were-away-player` entries appear immediately after arrival, icon-only chat tab buttons sourced from `assets/material-icons/game-tab-icons/` (`story_tools.svg` for Story Tools) plus the `star_solid.svg` Favorites subtab beside World Map, a yellow current-location favorite toggle, favorite-location cards, and mobile horizontal scrolling for the top-level tab strip, a left Adventure sidebar panel whose outer shape keeps only the bottom-right corner rounded, desktop drag handles for resizing the Adventure location and player/party columns with browser-persisted widths, mobile Adventure layout collapse of the desktop fixed-height/nested-scroll chain so the location and sidebar panels size to content on narrow screens, explicit hidden-tab overrides so non-active tabs never render even if they use flex layouts, player-view modal point allocation flow (including a Calculated Attributes grid above base attribute controls that colors modified totals by comparison to base values, in-modal skill add/remove for player view, formula-delta-aware skill-point previews during stat edits, server-derived visible unspent-point totals in non-player NPC views, read-only ability cards with active/passive/triggered color coding matching the level-up selector styling, and a faction section above equipment that shows NPC faction names or player faction tiers/benefits), player level-up ability draft modal flow (including fresh-new-game startup opening before the deferred intro is generated, modal-first generation message then card picks, sequential missing-level handling, non-blocking passive-state polling, gameplay blocking while pending, and top-row-safe modal offset), load modal topmost layering plus confirm-time prompt cancellation before load and forced visible-modal precedence above the elevated load backdrop, player/NPC edit modal aliases plus resistances/vulnerabilities/AI-notes text fields, the shared inventory-style thing-list renderer now reused by player/NPC inventory, location item/scenery sections, and the crafting inventory (including per-panel `Classic` / `Table` / `Grid` / `Small Grid` view popups, `Table` now using real HTML table rows/cells with half-height image/row sizing derived from the shared base icon size, the `•••` context-menu button anchored in the upper-right corner of the name cell, inventory/crafting-only column headings plus `Level` / `Value` / `Equipment Slot` columns with the equip toggle living in the equipment cell while location tables stay slim and unheaded, `1px` grid-mode tile gaps, inventory-style grid equipment pills that show slot labels and centered `Equipped`/`Unequipped` feedback, persisted per-player panel view selections across reloads and saves, narrow-panel icon-only filter popovers, a shared collapsed sort popup with stable secondary ordering across repeated sorts and options for `Alphabetical`, `Chronological`, `Level`, `Quality`, `Stack Size`, `Value`, and `Equipment Slot`, shared text search over item names/descriptions/details/metadata/status-effect text, `Show all` / `Equippable only` / `Non-equippable only` filters everywhere, and `Equipped only` on inventory-style views outside the location panels, compact location item/scenery popovers that temporarily raise their owning panel above neighboring content while open, view-popover auto-close on selection, outside-click close for view/sort popups, an available-items crafting header with the hint directly below `Available Items`, craft/process picker merging of player inventory plus current-location items, plus red-outlined equipped crafting cards that alert and reject slot assignment until unequipped, and the `Modify Location` action below the current-location image/level area reusing that modal in modification mode), inventory modal filter persistence across equip/unequip refreshes, NPC/party portrait health bars rendered just below the image to avoid overlap with in-portrait need bars plus a centered outlined upward-rounded `current/max` readout directly above each portrait health bar, top-left bare `L.<level>` portrait text with a `3px` black stroke across player and NPC card surfaces, skull icon + corpse countdown moved directly beneath the level readout with the same black outline, live status-effect health drains shown in the same portrait slot with a red blood icon and summed `-x` per-round loss for alive actors, relocation of the player unspent-points warning triangle to the lower-left just above the health bar, removal of the duplicate sidebar `HP:` and text-level rows on the player card, and hidden corpse countdown text for persistent corpses, location/inventory/equipment thing-card count badges sourced from persisted `thing.count` (with scenery hiding `1`), thing-image clicks now opening a combined lightbox that shows the image beside the full tooltip content plus compatible equipped-item comparison cards for equippable items, with the desktop tooltip pane vertically centered and no longer stretched to the full viewer height, a desktop-only inner tooltip scrollbar when needed, and click/tap dismissal from either pane, item-card `Separate` context-menu actions backed by the `thing-separate` prompt plus `Split Stack` and non-equipment `Merge Stacks` actions for stack management, with open item menus temporarily rendered in an auto-width fixed body-level floating layer to avoid section overlap and modal clipping, shared searchable chooser modals for summon/teleport/`Put in Inventory` target selection that replace the older summon `<select>` widgets with the same typeahead result-list interaction and NPC teleports force party/current-location refreshes after removing teleported party members, chat-input history recall that only triggers after `ArrowUp`/`ArrowDown` leave the caret unchanged so wrapped/multiline editing keeps native cursor movement, party-member context-menu bulk dismissal (`Dismiss everyone else`) plus non-party bulk re-recruitment via `Recruit all party members`, mobile tooltip width behavior (`80vw` for item/NPC/location tooltips) plus touch menu-tap tooltip suppression for entity `•••` actions, docked prompt-progress tracker behavior (including persisted collapsed/one-line/table states between the chat log and input, an always-visible idle state, one-line background progress fill with prompt name, received characters, eye/cancel/retry controls, and floored approximate percentage text, compress/expand icon mode controls, character-count progress fractions/targets/run stats, a three-row-capped table state, a 4px collapsed progress bar, and modeless streamed-response viewer windows with prompt-first combined text, Copy Prompt, Follow, header dragging, native resize, internal overflow scrolling, snapshot retention after live progress clears, and explicit per-window close), turn-linked event/status summaries rendered as a collapsible `What changed` state-diff drawer grouped by exact summary item category metadata, ordered by row severity inside each category, with id-backed `entityRefs` shown as selection-event chips and legacy uncategorized event rows falling under `Other`, event-bundle rendering of need-bar + disposition + faction-reputation deltas in one active summary box with per-need icons/reasons plus one standalone notification per need-bar change and a dedicated standalone `Disposition Changes` summary batch when disposition deltas arrive outside an active bundle, quantity-bearing item summary suffixes like `(x2)` / `(x10)` when stack counts exceed `1`, a bundled natural-language `time passed` line when the turn advances world time, the same bundled `time passed` line on prose-mode craft/process/salvage/harvest summaries, and direct/map fast-travel summaries that parent-link travel/time rows to visible arrival prose when available, attack-check insight multiplier/effectiveness rendering plus one top-right attack-check hover button per resolveAttack tool call, collapsible tool-call debug sub-boxes with `@andypf/json-viewer` JSON rendering, and tool-returned actionResolutions[]/plausibilities[] rendering as skill-check and plausibility hover buttons, location and modal-inventory thing drag/drop behavior (item inventory drops, modal item drops to the current location, pointer/touch movement-threshold drag support plus native image/callout suppression on thing icons, plus item/scenery conversion between grids), vehicle-info icon-driven location header swapping with vehicle-region-prefixed location titles plus a conditional vehicle status block (`Current location: ...` when idle, or underway destination/countdown/progress-bar UI with the vehicle icon riding the fill edge) and lower-right `16:9` vehicle-location picture-in-picture overlay on the location image, destination-vehicle exit left-side icon rendering with `🚗` fallback when destination icon metadata is missing, compact exit travel-time suffixes such as `(1h10m)`, exit-driven world-time advancement for non-vehicle traversal using stored `travelTimeMinutes`, destination-driven unexplored region labeling (`Unexplored huge vehicle` only when the destination is a vehicle, otherwise `Unexplored Region` with vehicle-exit-type wording when applicable), explicit edge-vs-destination vehicle semantics (`exit.isVehicle`/`exit.vehicleType` do not imply destination is a vehicle), and outbound `Exit Vehicle:` labels with left-side `⬅️` icons, item/scenery create-modal optional-name behavior plus Type and Slot `Not specified` options that omit item/scenery and slot guidance from generation, faction creation modal + AI fill-missing precreate flow, quest reward faction-name rendering for reputation rewards, Story Tools tab behavior (full-history client-side search, filtered paging, and shared edit modal), websocket `chat_complete` audio cue support via `completionSoundPath` with travel-action deferral until movement completion, chat-prefix help modal roll-override note plus `\` no-context prompt guidance and craft/process/salvage/harvest/modify-location placeholder hints for inline `<N>` die-roll overrides, LLM modal submit behavior (including concurrent Add NPC and Create Item/Create Scenery submits plus no-prose craft/process/salvage/harvest/modify-location submits), location exit/name/details caching, shared minute-duration client helpers, direct-move origin verification (`expectedOriginLocationId`), and client-history cap behavior independent from base-context recent-history segmentation.
  - `ui/chat_interface.md` also notes that async memory/disposition deltas are persisted as one aggregated `Disposition Check` summary batch per memory batch instead of one summary box per NPC.
  - Turn diff drawer id-backed entity chips route to exact UI targets where available; name-only refs remain non-clickable.
  - Current thing-card context-menu note: `Separate` is available on item and scenery cards; `Split Stack` and `Merge Stacks` remain item-only.
  - Drag/drop note: `ui/chat_interface.md` covers concurrent distinct dragged-item operations, same-item guards, active-session protection for late container responses, and the generated-content spinner shown while pending container contents hydrate.
  - Empty chat input now opens `#emptyActionConfirmModal`; confirming submits an intentional empty player-action continuation.
  - Story Tools search in `ui/chat_interface.md` now covers a 1-second typing debounce, a full-history client-side type filter populated from unique story entry types, full-history client-side modes for all-words, substring, and regex matching, plus case-sensitive matching and invalid-regex feedback.
  - Story Tools Mystery Boxes layout in `ui/chat_interface.md` now documents the read-only mention history as a right-hand column beside the editable name/key/private-note fields.
- [ui/modals_overlays.md](ui/modals_overlays.md) — Inventory of chat-page modals/overlays and tooltip behaviors (including player level-up ability draft modal behavior with modal-first generation message, top-row-safe offset, and centered submit action spacing, load modal topmost layering with confirm-time prompt cancellation, empty chat-submit confirmation via `#emptyActionConfirmModal`, docked prompt tracker states plus prompt-progress eye-action viewer behavior with a combined prompt/response pane, prompt text rendered first in a distinct inline span, `Copy Prompt` control, header dragging, native resize, internal overflow scrolling, and character-count received/rate cells, `#containerOpenCheckModal` for `requiresCheckToOpen` containers, the two-column/mobile half-height `#thingContainerModal` with pending-generated contents spinner, drag/drop, movement-threshold touch dragging, shift-click transfers, visible-filtered one-request `Add all`/`Remove all` bulk actions, the `#barterModal` trade/haggle interface opened from eligible NPC-card trade icons, plus nested-container breadcrumb navigation, status effect selectors/details, read-only active/passive/triggered color-coded ability cards and resistances/vulnerabilities boxes in character view, edit-form resistances/vulnerabilities/AI-notes fields, NPC-only need-bar applicability and trade-willingness controls in the character editor, calculated attribute bonus visibility above base attribute controls, single-scroll character view allocation sections, Skills + Apply Point Changes + Faction above Equipment ordering in character view, faction display differences for NPC vs player view, formula-delta-aware player allocation previews, server-derived visible unspent-point totals in non-player NPC views, equippable-item compatible-gear tooltip stacks, location/region vehicle-edit fields with active-destination editing over `currentDestination`/`pendingDestination` including unresolved named pending targets plus `New Region` actions for active and fixed-route destinations, inside->outside vehicle-exit selection, destinations add/remove typeahead, editable exit travel-time fields, existing pending-region entrance targets, and server-side user-entered new location/region name rejection in `#newExitModal`, tabbed calendar field-editor behavior, the shared `#setLastSeenModal`, and summon NPC/item modal live substring filters), plus immediate-close LLM modals and empty-slot craft/process/Modify Location submit behavior.
- [ui/maps.md](ui/maps.md) — Region and world map rendering and interactions, including immediate Adventure-tab focus when map/world-map node taps initiate travel, Favorites subtab travel reusing the world-map fast-travel helper, focused region-map loading for new-exit summary pills, shared region/world-map location context-menu actions such as `Set Last Seen` (using the same `H AM/PM`, `H:MM AM/PM`, or `duration ago` input as `/set_last_seen`), hydrated-location deletion from the map context menu, stub edit-modal vehicle metadata controls with inside->outside exit selection, shift-drag map creation requesting bidirectional new-location-stub exits, new-exit payloads that can target existing pending-region entrances, centered vehicle-icon overlays on vehicle location/region nodes, and fast-travel chat summaries when route time advances world time.
- [ui/assets_styles.md](ui/assets_styles.md) — Styling, assets, and vendor libraries including `@andypf/json-viewer` for tool-call debug JSON (plus shared app-header icon masks including the Mods `puzzle.svg` icon, shared `star.svg` / `star_solid.svg` outlined/solid star icon variants, shared entity theming primitives, icon-only chat tab buttons with shared `.tab-button__icon` sizing, transparent button chrome, and a flush no-gap/no-bottom-margin tab bar with a subtle inset shadow, an Adventure location-panel `.location-block .container` shape where only the bottom-right corner remains rounded, an Adventure `.chat-sidebar` shape where only the bottom-right outer corner remains rounded, a no-gap `.chat-wrapper` Adventure layout, slim desktop `.adventure-resize-handle` separators for resizing the location and player/party columns, mobile clearing of the Adventure desktop fixed-height and nested-scroll chain so `.location-block > .container` and `.chat-sidebar` grow with content on narrow screens, compact thing-list popovers that promote their owning panel with a temporary open-state stacking class, lower-right thing count thumbnail badges for item/scenery cards, mod-registered Thing image badges that can render SVG masks or raster assets in image corners, the shared thing-list panel/filter/sort/view shell classes used by inventory/location/crafting/container card lists with compact icon-only popover controls, shared `view.svg` / `sort.svg` / `filter.svg` inventory-view icons, a centralized `// Shared thing-view sizing tokens` block in `public/css/main.scss` for item-view geometry, SCSS-token-driven `Small Grid` `0.7x` overrides for the shared grid image/count/badge/menu sizing, two-column/mobile half-height `.thing-container-modal__*` layout/dropzone styling with compact visible-item bulk buttons, generated-content loading spinner sizing, touch-drag ghost styling, movement-threshold touch initiation, and thing-icon touch-action/native callout suppression, shared portrait bare-text level-badge styling plus upward-rounded health-bar readout text, chat-player warning-indicator anchoring from the health-bar geometry, and skull/blood condition icons scaled to line height, table-row, grid-tile, grid equipment pill, and grid equipment feedback styling for the shared `Classic` / `Table` / `Grid` / `Small Grid` item views including real-table table-mode sizing using the full shared base icon size with collapsed 2px borders plus floating open context menus, plus semantic container hooks such as `.view-classic-mode`, `.view-table-mode`, `.view-grid-mode-large`, and `.view-grid-mode-small`, chat prose markdown table styling with `0.25` row striping, edge-aware cell padding, and horizontal overflow scrolling, wrapped chat markdown `pre`/code-fence blocks (`pre-wrap` plus long-token breaking), stacked equippable-item comparison tooltip cards, shared `npc-list-editor-*` modal classes with legacy compatibility notes, long-name downscale via `.entity-name-long` with `1.25` base line-height behavior for regular names and item/scenery card triggering on any 12+ character word, skill allocation class hooks, shared active/passive/triggered ability type color styling, faction-tier threshold input width notes, fixed settings `.editor-tabs` height (`80px`), shared scrollbar theming via `@mixin themed-scrollbar` with `1ex` sizing and rounded gradient thumb, and visibility-based warning spacing).
  - `ui/assets_styles.md` documents the five-second `.health-change-float` duration used by portrait health/need change numbers.

## Mod docs (`docs/mods`)

- [mods/need-bar-lust.md](mods/need-bar-lust.md) — Bundled lust need-bar hybrid mod reference covering its `sex` need-bar defs overlay, slopword overlay, and stage-1 player-action prompt-step registrations.
- [mods/modules.md](mods/modules.md) — Bundled modules mod reference covering configurable module slot types, first-class Thing fields, tooltip/lightbox installed-module details, module/modular badge assets, editor controls, the module-compatible badge count/workbench entry point, direct module-card drop installs, occupied slot thumbnail scaling, inventory/location module sources, hidden standalone installed-module behavior, context actions, chat tools, XML events, and additive installed-module bonuses/status effects.

## API reference (`docs/api`)

- [api/attributes.md](api/attributes.md) — Attributes endpoints; notes the duplicate route definitions in `api.js` and that only the first binds.
- [api/chat.md](api/chat.md) — Chat endpoints, sorted by path; documents travel-prose split event payloads (including move-turn `item_appear`/`scenery_appear` handling), world-time payloads, destination/stub creation tooling (including `createLocationStub` and `createExit` duplicate no-op behavior), move-lock + pending-player-ability-selection `409` behavior, authoritative movement lookups against `gameLocations`, movement integrity cleanup for stale region location references, `?` question-action routing (`user-question`/`storyteller-answer` entries, slop-remover bypass), `@`/`@@`/`@@@` generic prompt routing with persistence modes, `\` no-context prompt routing with base-context exclusion plus no-tool execution, chat tool-loop execution with prompt-mode tool gating (regular prompts: info tools including JSON-returning `moreInfo`, plus the health-mutating `resolveAttack` exception and the `resolveSkillCheck`/`resolveOpposedSkillCheck` tools by default, or the legacy separate attack/plausibility prompt flow when `use_legacy_prompt_checks` is true, with finite integer skill-check modifiers accepted without the attack-tool standard-range guard, same-prompt attack/skill-check relitigation cached by round plus actor/target/opponent/weapon/ability/attribute/skill keys and cached check content warning the model not to rerun earlier-draft checks, live prompt-excluded `check-results` entries for skill/attack results, logged `<toolError>` tool messages plus dedicated `ERROR_tool_call_failed_*` JSON files for execution failures and exhausted tool-call attempts instead of prompt aborts, registered module install/remove chat tools, installed-module hiding in base-context inventory lists, and optional `debug_tool_calls` logging to a separate visible prompt-excluded `tool-call-debug` entry with structured records, cache-hit markers, and `@andypf/json-viewer` client rendering; `@`/`@@`/`@@@`: full world-mutation tool set including `createThing` named/container-flag seeds, `upsertFactionFields` direct faction create/update field pairs, plus `alterThing`/`alterNpc`/`alterLocation` helpers backed by existing alteration prompt/event flows and direct field patchers `updateCharacterFields`/`updateObjectFields`; `\`: none) plus structured `<toolError>` disambiguation output for recoverable tool failures, event-result arrays including disposition/faction-reputation deltas, quest-objective completion payloads that now include prompt-supplied completion reasons, inline `<N>` die-roll override parsing (including stripping tokens from persisted user entry text), creative `!` action repetition-buster XML enforcement when enabled, omission of deprecated AI-usage metrics from realtime/client chat responses, event-themed vehicle-movement summary entries with vehicle icons, bundled natural-language `time passed` summary items derived from `timeProgress.advancedMinutes` with exit-travel overrides respected, immediate visible-prose `chat_history_updated` refreshes after cleaned NPC/WYA/random/crafting/location-modification/quest prose is stored, server-only supplemental/offscreen/plot-summary/plot-expander/`while-you-were-away` hidden story-note entries plus visible `while-you-were-away-player` reunion prose entries and cadence rules (including absolute need-bar value parsing with blank/`N/A` skips, server-side NPC relocation when offscreen activity marks `moved=true`, current-party `HERE` arrivals staying party-only at the player's arrival location, scoped event checks for other while-you-were-away outcomes, plus threshold-gated blocking reunion updates on arrival), post-response party-memory `npc_memories` scheduling with chat travel keeping prior-location NPC memories separate from party-member memory prompts, asynchronous `random_event_seed_region` behavior with unseeded-region random-event skipping, realtime `chat_complete` completion-sound payload support, `/api/chat/history` behavior (turn-capped default plus `includeAllEntries=true` full-history mode, separate from `recent_history_turns`), plus shared payload references in `docs/api/common.md`.
  - Empty player-action continuations are accepted with empty final user content and skip plausibility before prompt rendering; legacy attack precheck/check prompts and the automatic player-action plausibility prompt call are disabled by default and re-enabled by `use_legacy_prompt_checks`.
- [api/common.md](api/common.md) — Shared response shapes and conventions referenced by multiple endpoints (including hidden ChatEntry types plus visible prompt-excluded `tool-call-debug` diagnostics with structured `toolCalls` records and cache-hit markers, `summaryItems`/SummaryItem drawer metadata with exact categories, severity, source type, entity refs, and need/disposition grouping metadata, StatusEffect modifier fields, actor resistances/vulnerabilities fields, actor `aiNotes`, actor `needBarApplicability` maps plus `changePerMinute` need-bar payloads, actor `persistWhenDead` and `wasEverInPlayerParty`, unspent attribute point fields on actor payloads, location thing lists now using direct `Thing.toJSON()` payloads including `isContainer`/`containerContents`/`containedThingIds`, vehicle metadata inside `LocationResponse.region` plus `VehicleInfo` shape details including icons/resolved current vehicle location names and `pending-region:<region name>` fixed-route entries, persisted `LocationExit` / `LocationResponse` exit `travelTimeMinutes` with `0` reserved for unpopulated legacy values, `LocationResponse` exit destination fields including visit-state metadata plus destination vehicle fields (`destinationIsVehicle`/`destinationVehicleType`) with explicit edge-vs-destination semantics, and map summary vehicle icon fields for location/region nodes and vehicle exits).
- [api/crafting.md](api/crafting.md) — Crafting endpoints (including canonical-minute `timeTaken` parsing with per-result invalid-entry skip+warning behavior and minimum 1-minute advancement, `timeTakenMinutes` responses, `noProse` execution mode, deterministic summary wording with `from`/`using` source context, prose-mode craft/salvage/harvest event summaries appending the same `⏳ <natural duration> passed.` line used by normal turns, inline `<N>` die-roll override tokens in crafting note fields, literal `<itemsConsumed>`-driven craft/process consumption with loud failure on mismatched consumed names, distinct crafted-item `causeStatusEffectOnTarget` vs `causeStatusEffectOnEquipper` preservation, preserved parsed crafted/recovered item `<count>` values during `Thing` instantiation, successful harvest-node tracking via `previouslyHarvestedItems` + `lastHarvested`, post-success autosave coverage for craft/process/salvage/harvest mutations, stack-aware input consumption that decrements `thing.count` before full deletion, the craft success-degree outer `<response>...</response>` wrapper gate plus base-result field backfill for omitted reroll fields, and the `player-action-craft` XML prose contract); references shared payloads in `docs/api/common.md`.
- [api/factions.md](api/factions.md) — Faction listing/CRUD, player standings endpoints, inbound relation generation from existing factions on new-faction create, AI fill-missing endpoint for partial faction creation payloads (including optional `generationNotes` guidance, explicit nested-XML autofill schema guidance, robust relation/tier/asset format parsing, quote-insensitive relation name mapping, and detailed autofill-missing errors), and the `upsertFactionFields` chat mutation tool's direct create/update semantics.
- [api/game.md](api/game.md) — Game lifecycle endpoints, including active calendar get/update routes, strict need-bar prompt-sentence validation on new-game/load, load-time hide/perception setting backfill, warning-only live reload behavior for per-game YAML overrides, save metadata `enabledMods`, mod mismatch load responses, mod manager APIs, pending-load APIs, and the existing save/load/new-game lifecycle details.
- [api/images.md](api/images.md) — Image generation and job endpoints plus extension-agnostic generated image serving; references job shapes in `docs/api/common.md`, including backend-selective `baseContextPreamble` prepending for image prompts (OpenAI/NanoGPT yes, ComfyUI no), LLM image-prompt retries with `image-prompt-failed` skips after repeated bad prompt/context output, debounced compatible LLM image-prompt batching via `imagegen.prompt_batching`, base location scene final-prompt post-processing through `templates/location-image-prompt.njk` with outside-visible weather labels, item/scenery size resolution via `default_settings.image` plus optional per-type overrides, any known thing/item being eligible for image generation, server-side coalescing of concurrent base location-image requests during the pre-job LLM prompt-generation phase, and the ComfyUI-only location weather/lighting variant request endpoint with authoritative base-image source resolution, locally rendered deterministic edit prompts that label outside-visible weather as `Weather outside:`, app-console and ComfyUI-console logging of actual variant edit prompts, and size-mismatch warnings for edited variants.
- [api/locations.md](api/locations.md) — Location and exit endpoints (including `GET /api/locations?scope=favorites` and `PUT /api/locations/:id/favorite`, current-location modification via `POST /api/locations/:id/modify` with consumed-input and newly received portable-item outputs, editable exit travel-time input with `1 minute` defaulting on `POST /api/locations/:id/exits`, existing live/pending region entrance targets for manual new exits, explicit stub expansion through `POST /api/stubs/:id/expand`, location relocation through `POST /api/locations/:id/relocate` with selected exit cleanup, pending-region preservation when deleting one of several exits to the same pending region, optional-name item/scenery generation on `POST /api/locations/:id/things` with omitted `seed.itemOrScenery` left unspecified for generation, optional-name NPC generation on `POST /api/locations/:id/npcs` with generated progression applied before the response returns, location vehicle-info and `hasWeather` hint updates on `PUT /api/locations/:id` using `yes`/`no`/`outside` plus legacy boolean normalization, stub vehicle-info updates and blank stub descriptions on `PUT /api/stubs/:id` with pending-region stub sync for region-entry stubs, concurrent unnamed NPC request behavior, and hydrated-location cascade deletion via `DELETE /api/locations/:id`); references shared shapes in `docs/api/serialization.md`.
- [api/lorebooks.md](api/lorebooks.md) — Lorebook listing endpoints with metadata details.
- [api/map.md](api/map.md) — Legacy index for map endpoints; points to newer docs.
- [api/misc.md](api/misc.md) — Misc/utility endpoints (feature flags, prompt cancel/retry controls, cancel-all prompt control with drain wait support, and slash-command execute/upload routes including typed reply actions plus optional per-client refresh via `clientId`).
- [api/mystery-boxes.md](api/mystery-boxes.md) — Browser-facing MysteryBox list/get/update routes used by the Story Tools Mystery Boxes tab, including current-save `mysteryBoxes.json` persistence on manual edits.
- [api/mystery-threads.md](api/mystery-threads.md) — Browser-facing MysteryThread list/get/update/box-assignment routes used by the Story Tools Mystery Boxes tab, including active-cap validation and current-save `mysteryThreads.json` persistence.
- [api/npcs.md](api/npcs.md) — NPC endpoints (including bulk `/api/npcs/generate-aliases` batching at 20 NPCs per prompt and metadata persistence, alias/resistance/vulnerability/AI-notes editing support, NPC-only `needBarApplicability`, `willingToTrade`, and `hiddenFromPlayer` updates in `PUT /api/npcs/:id`, barter session/haggle/commit routes with installed modules filtered from standalone barter offers, and optional shortest-route world-time accounting on `/api/npcs/:id/teleport` for map fast travel plus travel/time chat summaries when player fast travel advances time); references shared payloads in `docs/api/common.md`.
- [api/players.md](api/players.md) — Player and party endpoints (including strict `/api/player/move` origin verification + move-lock behavior plus non-vehicle exit travel-time advancement in returned `worldTime`/`timeProgress`, current-player inventory serialization that instantiates pending container contents, persisted shared thing-list panel view preferences via `/api/player/thing-list-view-preferences` including the container inventory panel keys, player ability-selection endpoints for level-up drafting with no-game suppression semantics, level-up chat logging with base-context history exclusion, plus admin stats updates with runtime registration of newly submitted skill names and definition-based attribute validation in `/api/player/update-stats`); references shared payloads in `docs/api/common.md`.
- [api/quests.md](api/quests.md) — Quest endpoints (including editable `rewardFactionReputation` deltas that accept object/array/string payload formats with integer positive/negative values, plus editable NPC disposition reward lines that warn-and-skip unknown NPCs); references shared payloads in `docs/api/common.md`.
- [api/regions.md](api/regions.md) — Region endpoints (including vehicle-info fields plus editable `weather`/`weatherState` payloads on region get/update responses); references shared payloads in `docs/api/common.md`.
- [api/scene-summaries.md](api/scene-summaries.md) — Browser-facing Story Tools scene-summary list/update routes with current-save persistence behavior, plus the chat-tool rerun path for stored scene ranges.
- [api/serialization.md](api/serialization.md) — Legacy index for shared shapes; points to `docs/api/common.md` as authoritative.
- [api/settings.md](api/settings.md) — Settings endpoints (including AI fill-missing guidance, default-skill augmentation, hide/perception mechanic selectors, settings-local faction generation/autofill endpoints with optional `generationNotes` guidance, `customSlopWords` and `unifiedTonalScale` payload handling, rename-as-new-id updates, and persistent delete behavior); references shared payloads in `docs/api/common.md`.
- [api/things.md](api/things.md) — Things and inventory endpoints, including AI-backed item/scenery separation via `POST /api/things/:id/separate` with first-returned-container nesting for returned item outputs, parser support for top-level `<stack>` separation responses that preserve unchanged source stats without rescaling bonuses, explicit `POST /api/things/:id/split-stack` and `POST /api/things/:id/merge-stacks` stack-management routes, mod-owned Thing context actions including base-item-driven module install/removal with inventory/location source fields, stacked module split-one installation, and optional location refresh payloads, prompt-free stack splitting for source things with `count > 1` that reuses the source image and copies the source thing's current `statusEffects` without re-enrichment, checksum/name/container-based stack merging that excludes equipped items, automatic same-destination item-stack merging when stacks are dropped or moved into inventories/containers, pending `containerContents` seed support, and first-class thing-container inventory routes (`GET /container`, checked `open-check`, `move-in`, `move-out`) with one-time `thing-generator-contents` generation, skill-check-gated `requiresCheckToOpen` attempts, single-item or bulk `thingIds` movement, and non-empty container delete protection; references shared payloads in `docs/api/common.md`.

## Class reference (`docs/classes`)

- [classes/ActorAttachmentSystem.md](classes/ActorAttachmentSystem.md) — Reusable helper for mod-owned inventory-backed actor attachments that remain in inventory, store installed Thing ids in actor `modState`, read first-class item slot fields such as `implantSlot`, preserve health ratio on modifier changes, and contribute attribute/status/profile sections.
- [classes/ItemModuleSystem.md](classes/ItemModuleSystem.md) — Module-owned helper at `mods/modules/ItemModuleSystem.js` for item-to-item module installation, stacked module split-one installs, validating configurable slot types, syncing base/module Thing links, preserving raw holder ownership for installed modules, and contributing installed-module attribute/status/target effects.
- [classes/ComfyUIClient.md](classes/ComfyUIClient.md) — Client for ComfyUI servers: queue workflows, poll status, upload img2img source images, download images, save results, and note that raw API queue requests do not send ComfyUI UI workflow metadata for `SaveImageWithMetaData` `*_with_json` sidecar formats.
- [classes/CodexBridgeClient.md](classes/CodexBridgeClient.md) — Codex CLI bridge for text-generation requests, including prompt wrapping, Codex `developer_instructions` mapping for wrapper/system prompts, app-server-backed fresh/resume thread handling, Codex-compatible structured output with always-present `content`/`tool_calls` keys and JSON-string tool arguments, optional reasoning-effort pass-through, abort-signal handling for popup cancel/retry, stdout-event forwarding that decodes structured-output deltas back into plain assistant preview text, per-turn usage extraction from app-server token-usage notifications, app-server rate-limit queries, prompt-log `=== RESPONSE ===` sections that write plain assistant `content` when available, and normalization back into OpenAI-style chat-completion payloads.
- [classes/Events.md](classes/Events.md) — LLM-based event checks that parse structured outcomes and apply world mutations, including the default XML event pathway with separate need-bar prompting, scoped ignored event keys plus optional dedicated need-bar prompt suppression for specialized callers, strict travel-boundary splitting with `thingMoveWithCharacter` deferred to destination application, and legacy grouped-prompt fallback via `event_checks.use_xml: false`, move suppression for split `<travelProse>` passes with post-arrival handlers temporarily scoped to the resolved destination, move-turn appearance override support for `<travelProse>` checks, NPC name normalization details (including same-location leading-name resolution like `Bob` -> `Bob Ross`) plus final-name propagation for event-created items/NPCs after duplicate/slop-name regeneration, robust arrow-delimiter parsing (`->` and unicode arrows such as `→`), `move_new_location` treating `sublocation` as a real location when moves are applied plus warnings when move events lack `context.location`, party-member departure handling that removes explicit `npc_arrival_departure left` actors from the party before moving them to their destination unless the destination is already the current party owner's location, tool-context-aware `alter_location` handling with an optional preserve-base-level mode for location modification flows, item alteration treating returned optional attribute/effect fields as authoritative so missing or empty tags clear stale data while populated status effects are synchronously expanded before persistence, item inflict handling plus same-turn dedupe against overlapping `status_effect_change` entries (exact or prefix name match), `item_ingest` target-effect inference with same-pair suppression of overlapping `item_inflict`, strict quantity-aware parsing and exact stack-amount processing for stack-mutating item events including partial-stack `alter_item` splitting and `consume_item` spilling instantiated container contents before deleting a fully consumed container, preserved stats/image plus unchanged `metadata.value` on partial-stack item moves/consumption/alterations, synthesized item-triggered status-change summary entries for client visibility, duplicate NPC-death no-op handling plus finite-delta death application before `isDead`, parsed disposition-change handling, faction-reputation event handling with witness gating (`a little` = `1`, `a lot` = `4`), quest checks skipping empty active-quest lists before prompt construction, quest-objective completion parsing that preserves prompt-supplied reasons, event generation option passthrough for scenery/resource appearance handling, quest-completion application of per-faction reputation reward deltas and per-NPC disposition reward deltas, `alter_npc` prompt logging + `<npc>`-block response enforcement with wrapper-tolerant parsing and persisted `aiNotes` updates, blank `<relativeLevel>` handling in `alter_npc` parsing (no implicit level reset), event-created stub level anchoring to origin location/current region, parsed `new_exit_discovered` travel times plus source/destination-region hints feeding event-created exit wiring including pending region stubs, missing explicit new-exit source locations being created as source-region stubs without extra current-location exits, source-aware route summary text that preserves explicit XML destination locations even for region exits, and post-application origin/destination/exit ids for clickable map summary pills, `time_passed` leading-reasoning ignore plus malformed duration skip+warning behavior, minimum one-minute advancement when `time_passed` is `0`, `initialTimeProgress` suppression of duplicate fallback `time_passed` advancement, route/exit-authoritative prompt-authored `time_passed` suppression while timed vehicle starts and suppressed active-vehicle-destination event moves leave fallback elapsed time intact, immediate time-based need/status tick resolution after event time advancement and before event-based need-bar changes, silent warning-logged suppression of forbidden vehicle-origin event exit creation, post-success + single-player-move turn guards for `move_location`/`movePlayerToDestination`, and fast-travel-helper world-time advancement for non-vehicle event player/party moves with a one-minute no-route fallback.
- [classes/EventsEventTypes.md](classes/EventsEventTypes.md) — Table reference for every non-dummy `Events.js` event type, including prompt-sourced, dedicated-prompt, quest-merge, legacy structured, and prompt-only keys, with payload shapes and application behavior such as travel suppressing prompt-authored `time_passed`.
- [classes/EventsXmlEventSchema.md](classes/EventsXmlEventSchema.md) — Informal XML schema for the default single-prompt event pipeline, including the `<events>` root, flat chronological event list, first-move travel boundary with required `<arriveAtLocation/>`, route/exit-authoritative travel timing, and LLM-facing guidance for each camelCase XML event tag.
- [classes/Faction.md](classes/Faction.md) — Faction model with goals/tags/relations/assets/reputation, static indexes, and the chat upsert tool's real-instance/direct-update behavior.
- [classes/Globals.md](classes/Globals.md) — Centralized static state/helpers for current player, locations, regions, prompt wiring, transient player-arrival pre-visit/time tracking for while-you-were-away gating including non-NPC `Player.setLocation(...)` snapshots, server-assigned slop detection helpers including asterisk-stripped regex matching, the server-assigned `appendChatEntry(...)` bridge for non-route chat inserts, and canonical world time/calendar utilities (including pure calendar-definition normalization, validated active-calendar replacement, non-recursive world-time initialization/context flow, season/holiday descriptors in world-time context, season `timeDescriptions`-driven light level descriptions, and Gregorian fallback calendar generation).
- [classes/IdGenerator.md](classes/IdGenerator.md) — Central compact-ID allocator for persisted domain objects, with per-type counters, `metadata.idCounters` persistence, and collision-skipping synchronous allocation.
- [classes/LLMClient.md](classes/LLMClient.md) — LLM chat client with backend-aware transport selection, foreground-prioritized concurrency with background slot reservation, generic OAuth refresh-token resolution for `oauth-key`/`oauth-url`, streaming, optional raw stream-chunk console logging, final-block XML validation, streamed `tool_calls` assembly + JSON-argument validation, automatic + user-triggered retries, retry-time config/override refresh, concise per-prompt `ai_model_overrides` console summaries, deterministic `forceOutput` modes (explicit payloads and per-`metadataLabel` fixture files), prompt logging plus prompt-labeled error log filenames and output-character stats headers, single + bulk cancellation utilities (with drain waiting), realtime-hub-backed prompt-progress tracking for streamed text with decoded-character counts, progress fractions, configured character targets, run counts, average output-character payload fields, persistent `logs/prompt-output-character-stats.json` validation/update behavior with base-label aggregation for character-appended inventory/NPC stats, fresh-only Codex concurrency with resumed-session serialization, Codex per-prompt usage logging plus every-5-counted-turn quota snapshots with active-bucket selection when Codex reports multiple model buckets, prompt-excluded category-tagged multi-line `🌀 Codex Quota` status-summary chat notices, request payload notes, and optional enable/disable final-user-message cachebuster tagging.
- [classes/Location.md](classes/Location.md) — Location model (description, exits, NPCs, items, status effects) with stub promotion support, persisted visited/`lastVisitedTime` tracking with `minutesSinceLastVisit(...)` and legacy-save inference for missing visit flags, persisted `favorite` markers for the Play tab Favorites subtab, automatic same-name/same-checksum loose item-stack merging on `addThingId(...)`, persisted display-only `imageVariants`, stub description/shortDescription prompting rules including blank short-description handling for event-created stubs, authoritative stub handling behavior, event-created location stubs inside pending region stubs via `pendingRegionStubs.locationIds`, persisted/API-exposed `generationHints.hasWeather` and stub weather metadata exposure scopes (`yes`/`no`/`outside`), `regionId` reassignment that keeps region membership and old-region entrance pointers in sync, and Dijkstra-based shortest travel-time lookup across the directed exit graph by id/object or exact region-scoped location names.
- [classes/LocationExit.md](classes/LocationExit.md) — Connection between locations/regions, with optional vehicle semantics, persisted `travelTimeMinutes`, bidirectional travel helpers that preserve travel time, and a runtime-only debug backtrace capturing exit creation.
- [classes/LorebookManager.md](classes/LorebookManager.md) — Lorebook manager for JSON lorebooks: load, enable/disable, keyword match, and prompt injection.
- [classes/ModLoader.md](classes/ModLoader.md) — Mod loader for enabled `mods/` entries with per-mod scope helpers including player-action prompt-step registration, configs, public and asset static serving, client asset discovery, defs-only and hybrid `mod.js` plus `defs/` mod support, shared `eval` filter support for mod prompt templates, and startup-frozen merged-config/per-mod `enabled` gating.
- [classes/ModExtensionRegistry.md](classes/ModExtensionRegistry.md) — Runtime registry for mod chat tools, XML event tags, player-action prompt steps, prompt/status contributors, attribute/status/Thing-target-effect modifiers, inventory sync hooks, World Profiles setting tabs/fields including select/action/array preset controls, first-class entity fields with dynamic descriptions, dynamic Thing image badges, and startup validators.
- [classes/ModManager.md](classes/ModManager.md) — Filesystem-backed mod manager helpers for `/mods`, normalized enabled-mod diffs, `config.yaml` enablement writes, save metadata comparisons, and pending post-restart load intents.
- [classes/MysteryBox.md](classes/MysteryBox.md) — Persisted private continuity notes for offscreen actors, mysteries, clues, secrets, and unresolved background threads, with alias/key lookup, mention history, resolved-box state filtered out of active mystery-thread base context, reactive `mysteryBoxMention` updates, lightweight `listMysteryBoxes({ query? })` search, full `findMysteryBoxes({ query })` search, `getMysteryBox({ key })` retrieval, and Story Tools browser editing through `/api/mystery-boxes`.
- [classes/MysteryThread.md](classes/MysteryThread.md) — Persisted GM-private continuity threads containing mystery-box ids, active/inactive/concluded status, summary/constraints, base-context active filtering that omits resolved boxes, and legacy grouping for old saves with boxes but no threads.
- [classes/NanoGPTImageClient.md](classes/NanoGPTImageClient.md) — NanoGPT image generation client that saves returned base64 images to disk.
- [classes/OpenAIImageClient.md](classes/OpenAIImageClient.md) — OpenAI image generation client that saves returned base64 images to disk.
- [classes/Player.md](classes/Player.md) — Player/NPC model with persisted NPC `aiNotes`, persisted NPC barter inventory and trade willingness/refusal expiry, shared need-bar and disposition definitions with config-driven first-impression multiplier, persisted per-actor `needBarApplicability`, persisted per-actor `needBarRatesAppliedAt` minute stamps for baseline `change_per_minute` drift, persisted per-actor `thingListViewPreferences` for shared location/inventory/crafting/container/barter panel view modes, persisted `persistWhenDead` corpse-retention flag, persisted `wasEverInPlayerParty` historical-party flag, persisted NPC `last_seen_time` / `last_seen_location` plus same-location previous-round tracking for absent-NPC prompt memory and newly present NPC reunion lists, non-NPC `setLocation(...)` pre-arrival visit snapshots for while-you-were-away gating, explicit `setNeedBarApplicability(...)` semantics (preserve kept values, restore newly re-enabled bars at `100`), audience filtering including historical-party activation of `party` bars, global-plus-per-bar directional `need_values` magnitude resolution (`increase` / `decrease`) with preserved decimal deltas, finite-float current health with upward-rounded client display, passive config-driven health regeneration, status-effect-driven max-health increases raising current health by the same delta (with decreases only clamping), exact-name or exact-description status-effect removal, prompt-sentence validation plus `%CHARACTER%` substitution helpers for base-context, prompt/UI-safe need-bar definition exports (including icon + medium/empty trigger buckets and while-you-were-away notes), event-summary icon metadata on need-bar changes, automatic same-name/same-checksum item-stack merging on inventory placement, legacy-save `100/true` need-bar hydration defaults, and the existing progression/inventory/party/location behaviors.
- [classes/Quest.md](classes/Quest.md) — Quest model with objectives/rewards/giver info (including per-faction reputation reward deltas and per-NPC disposition rewards), completion state, and static indexes.
- [classes/QuestConfirmationManager.md](classes/QuestConfirmationManager.md) — Manages async quest confirmations per client via `Globals.emitToClient`, including normalized NPC disposition reward previews.
- [classes/RealtimeHub.md](classes/RealtimeHub.md) — WebSocket hub for realtime updates with targeted send, broadcast, and typed emits.
- [classes/Region.md](classes/Region.md) — Region model containing locations, metadata, random events, status effects, and dynamic weather definitions/state with parent-region weather inheritance and minute-based weather durations (invalid weather duration strings are logged and skipped per weather-type entry), region API weather editing/reset behavior, including per-location short descriptions and `hasWeather` exposure scopes in blueprints, structured exit blueprint parsing via `<exit><destination>...<travelTime>...`, `<regionExits><stubRegion><travelTime>` forwarding onto created cross-region exits without persisting it on pending stub metadata, generated self-referential location/region exits being skipped with console warnings, prompt-generated zero-minute exit normalization up to `1`, direct-child blueprint parsing for `<locations><location>` (avoiding nested destination-tag misparsing), stub expansions that preserve pre-seeded pending-region location stubs, and authoritative controlling-faction handling for region-entry stubs.
- [classes/SanitizedStringMap.md](classes/SanitizedStringMap.md) — Map wrapper that normalizes string keys for case/punctuation-insensitive lookup.
- [classes/SanitizedStringSet.md](classes/SanitizedStringSet.md) — Set wrapper that normalizes string values for case/punctuation-insensitive lookup.
- [classes/SceneSummaries.md](classes/SceneSummaries.md) — Stores scene summaries from chat history, tracks scene ranges/per-scene detail lines/NPC names, replaces overlapping scenes on re-summarization, supports Story Tools display-number edits for summary text/details/quotes, and documents the shared scene-summary index including system/diagnostic exclusion, `getFullScene({ sceneNumber })` chat-tool lookup, `rerunSceneSummary({ sceneNumber })` mutation-tool reruns, plus rendered relative scene start-date labels.
- [classes/SettingInfo.md](classes/SettingInfo.md) — Game setting/world configuration (theme/genre/prompts/defaults, including starting location instructions, default skill lists, hide/perception check selectors, settings-level faction defaults, optional `calendarDefinition`, `customSlopWords`, and `unifiedTonalScale`) with persistence support, including id-based saved-file deletion helpers and backend-selective `baseContextPreamble` usage in image prompt execution.
- [classes/ScheduledEvent.md](classes/ScheduledEvent.md) — Persisted planned future event records created by `scheduleEvent`, including due-event ordering, `pending`/`resolved`/`skipped` status, complete tool-enabled resolution, `scheduledEvents.json` save/load helpers, hidden chat summaries, and visible player-present prose.
- [classes/Skill.md](classes/Skill.md) — Skill model with name, description, and optional attribute association.
- [classes/StatusEffect.md](classes/StatusEffect.md) — Status effect model for modifiers, need-bar deltas, strict minute-based duration parsing (including numeric string sentinels like `-1` for permanent effects, shared duration abbreviations, compact forms like `3d4h2m`, and parser-only second-unit tolerance for malformed model output), `appliedAt` tracking, and expiry semantics (negative=infinite, 0=expired).
- [classes/Thing.md](classes/Thing.md) — Item/scenery model with rarity, bonuses, status effects, placement, and indexes, including mod-overlay-aware rarity definitions, persisted integer `count`, automatic same-destination item-stack merge helpers plus runtime registry cleanup, first-class `isContainer` inventories with pending `containerContents` seeds and `requiresCheckToOpen` gating, registered top-level mod extension fields such as `implantSlot`, `containedThingIds` plus cycle-safe nested containers, mirrored raw prompt-scale `unscaledAttributeBonuses` metadata, `copy({...})` for fresh-id clones with the same hashable data/image, shared `consumeOne({ things })` decrement-or-delete stack handling, preserved distinct on-target vs on-equipper cause-effect entries, loaded-record background enrichment constraints, persisted harvest-node history (`previouslyHarvestedItems` + `lastHarvested`), and a stable non-cryptographic `checksum` getter derived from canonicalized item JSON with volatile identity/placement fields excluded.
- [classes/UnifiedTonalScale.md](classes/UnifiedTonalScale.md) — Utility module for loading `defs/unified_tonal_scale.yaml`, normalizing setting `unifiedTonalScale` selections/comments, generating half-step midpoint selection options, and rendering the markdown tonal-scale section inserted into system prompts before `extra_system_instructions`.
- [classes/VehicleInfo.md](classes/VehicleInfo.md) — Lightweight vehicle travel metadata shared by vehicle-capable locations/regions (terrain tags, icon, resolved `currentDestination`, structured `pendingDestination` for timed travel before arrival finalization, fixed-route destinations including `pending-region:<region name>` entries for unresolved route targets, absolute ETA/departure minutes, vehicle-exit id, resolved `location` getter, and start-aware trip state getters such as `isUnderway`/`hasArrived`/`isArriving`/`timeTraveled`/`tripCompleteFraction`).
- [classes/base_context_history.md](classes/base_context_history.md) — Base-context history inclusion helper, including generic prompt `includeAllHistoryEntryTypes` behavior for stored entries with renderable text and the always-on system/diagnostic prompt-history exclusion.
- [classes/Utils.md](classes/Utils.md) — Utility helpers (set math, text similarity, strict duration parsing with decimal units, optional signed-prefix parsing, parser-only second-unit tolerance for malformed model output, global punctuation scrubbing for noisy duration text, generated-exit zero-minute normalization, and backtrace-on-throw diagnostics, shared minute-duration display formatting including natural-language `A / A and B / A, B, and C` joins plus absolute-world-minute `... ago` formatting, XML parsing with bounded parse-failure console diagnostics, CDATA-aware prompt-node extraction, final-root/final-response XML block extraction, serialization including world-time/calendar persistence, `metadata.enabledMods`, legacy hour-save, pre-`1.1` need-bar scale migrations, and pre-`1.2` compact ID migrations with persisted `metadata.idCounters`, load-time defaulting of missing saved exit `travelTimeMinutes` to `0`, load-time player-registry reset before actor hydration, blank-safe location-description hydration without placeholder substitution, pending-region stub/member-location maintenance, capitalizeProperNoun options).

## Design ideas (`docs/ideas`)

- [ideas/DayNightCycle.md](ideas/DayNightCycle.md) — Design draft for a day/night cycle affecting danger, services, and NPC behavior.
- [ideas/Factions.md](ideas/Factions.md) — Design draft for faction systems and emergent conflict/cooperation.
- [ideas/area_attack_tool_plan.md](ideas/area_attack_tool_plan.md) — Implemented plan for a `resolveAreaAttack` prose tool that resolves one area-effect attack against multiple targets with per-target outcomes and grouped check-result UI.
- [ideas/gameplay_improvement_brainstorm.md](ideas/gameplay_improvement_brainstorm.md) — Gameplay improvement brainstorm covering low-effort, medium-effort, and ambitious feature ideas, including visible weather/time-of-day location image variants.
- [ideas/pending_region_specific_locations_plan.md](ideas/pending_region_specific_locations_plan.md) — Implementation plan for creating specific location stubs inside pending region-entry stubs and preserving them when the region expands.
- [ideas/visible_weather_image_variants_plan.md](ideas/visible_weather_image_variants_plan.md) — Implemented v1 plan for ComfyUI image-to-image weather and lighting variants of existing location images, cached by source image and normalized conditions.
- [ideas/dramatis_personae.md](ideas/dramatis_personae.md) — Brainstorm for a nemesis-style, setting-agnostic recurring NPC cast system.
- [ideas/setting_creation_studio_brainstorm.md](ideas/setting_creation_studio_brainstorm.md) — Deep-dive brainstorm for a guided setting creation studio with AI drafting, validation, previews, universal tone scales, factions, skills, image style, calendar, and new-game handoff.
- [ideas/tactical_battles_brainstorm.md](ideas/tactical_battles_brainstorm.md) — Expanded tactical battles brainstorm for optional structured conflict scenes, zones, objectives, turn flow, UI, and rollout slices.
- [ideas/tactical_map_generation_brainstorm.md](ideas/tactical_map_generation_brainstorm.md) — Deep-dive brainstorm on LLM-assisted tactical map generation for arbitrary locations using semantic zone graphs, validation, repair, and render layers.
- [ideas/trade_barter_system_plan.md](ideas/trade_barter_system_plan.md) — Implementation plan for an NPC trade/barter system with persistent NPC barter stock, LLM-authored buy/sell willingness and prices, container-style barter UI, haggle checks, temporary trade refusal, event integration, and daily stock refresh.
- [ideas/travel_planner_journey_risk_brainstorm.md](ideas/travel_planner_journey_risk_brainstorm.md) — Expanded travel planner brainstorm covering journey modes, lodging, camping, foraging, travel risk, encounters, UI, and rollout slices.
- [ideas/turn_state_diff_drawer_brainstorm_plan.md](ideas/turn_state_diff_drawer_brainstorm_plan.md) — Expanded brainstorm and implementation plan for rendering turn-linked event/status summaries as a collapsible state-diff drawer attached to assistant turns.
- [ideas/user_experience_improvement_brainstorm.md](ideas/user_experience_improvement_brainstorm.md) — RPG/adventure UX improvement brainstorm covering player journeys, core-loop legibility, resume/navigation aids, developer-facing diagnostics, shared design patterns, and a phased roadmap.
- [ideas/header_revamp_brainstorm.md](ideas/header_revamp_brainstorm.md) — Deep-dive brainstorm for a consistent top app header with namespaced styling, `Worlds`/`System` navigation wording, page action grouping, responsive behavior, and rollout/testing notes.
- [ideas/Vechicles2.md](ideas/Vechicles2.md) — Setting-agnostic vehicle brainstorm across items, scenery, NPCs, locations, and regions.
- [ideas/vehicles.md](ideas/vehicles.md) — Brainstorm of vehicle concepts spanning items, scenery, NPCs, locations, and regions.
- [ideas/world_inspector_editor_brainstorm.md](ideas/world_inspector_editor_brainstorm.md) — Deep-dive brainstorm for a developer/admin world inspector and safe editor with validation, backlinks, repair actions, and transaction previews.

## Implementation plans (`docs/superpowers/plans`)

- [superpowers/plans/2026-05-22-hidden-items-scenery-exits.md](superpowers/plans/2026-05-22-hidden-items-scenery-exits.md) — Implementation plan for hidden items, scenery, and exits using persisted hidden state, difficulty-label discovery checks without a prompt, reveal/hide events and tools, API gating, and show-hidden UI controls.
- [superpowers/plans/2026-05-24-scheduled-events.md](superpowers/plans/2026-05-24-scheduled-events.md) — Implementation plan for scheduled-event chat tools, persisted due-event state, resolution prompts, and visible/hidden chat logging.
- [superpowers/plans/2026-05-26-modular-items.md](superpowers/plans/2026-05-26-modular-items.md) — Implementation plan for the bundled modules mod, configurable slot types, item UI, mechanics hooks, tests, and docs.
- [superpowers/plans/2026-06-05-regen-party-images.md](superpowers/plans/2026-06-05-regen-party-images.md) — Implementation plan for `/regen_party_images`, which regenerates current party NPC portraits through the shared portrait generation path.
- [superpowers/plans/2026-06-09-map-fast-travel-confirmation.md](superpowers/plans/2026-06-09-map-fast-travel-confirmation.md) — Implementation plan for confirming Region Map, World Map, and Favorites fast travel with authoritative route time before navigation or mutation.

## Design specs (`docs/superpowers/specs`)

- [superpowers/specs/2026-05-26-modular-items-design.md](superpowers/specs/2026-05-26-modular-items-design.md) — Design spec for modular items: base item slots, slottable module items, configured slot-type options, additive stats/status effects, and UI requirements.
- [superpowers/specs/2026-06-05-regen-party-images-design.md](superpowers/specs/2026-06-05-regen-party-images-design.md) — Design spec for `/regen_party_images`, including player exclusion, party NPC resolution, and image prompt batching reuse.

## Slash command reference (`docs/slashcommands`)

- [slashcommands/Command.md](slashcommands/Command.md) — `/awardxp` command to grant exact raw experience points with alias-aware character targeting and current-location ambiguity tie-breaking.
- [slashcommands/CalendarInfoCommand.md](slashcommands/CalendarInfoCommand.md) — `/calendar_info` (alias `/calendar`) command to display in-game calendar details in markdown.
- [slashcommands/ClearPlotNotesCommand.md](slashcommands/ClearPlotNotesCommand.md) — `/clear_plot_notes` command to remove hidden `plot-summary` and `plot-expander` entries from chat history.
- [slashcommands/ClearSecretsCommand.md](slashcommands/ClearSecretsCommand.md) — `/clear_secrets` command to remove hidden supplemental/offscreen story info entries.
- [slashcommands/ExitBacktracesCommand.md](slashcommands/ExitBacktracesCommand.md) — `/exit_backtraces` command to dump every exit in the current location together with the runtime creation backtrace captured on each `LocationExit`.
- [slashcommands/ExportHistoryCommand.md](slashcommands/ExportHistoryCommand.md) — `/export_history` command to export chat history to text/HTML.
- [slashcommands/FillExitTravelTimesCommand.md](slashcommands/FillExitTravelTimesCommand.md) — `/fill_exit_travel_times` command to prompt-fill missing region exit travel times, with optional `force=true` regeneration that overwrites populated bidirectional pairs from a single prompted value.
- [slashcommands/FixExitsCommand.md](slashcommands/FixExitsCommand.md) — `/fix_exits` command to repair one-way location exits by creating missing reverse exits.
- [slashcommands/GameIntroCommand.md](slashcommands/GameIntroCommand.md) — `/game_intro` (alias `/intro`) command to generate and append intro narration, then attempt a standard autosave.
- [slashcommands/GenerateMissingSkillsCommand.md](slashcommands/GenerateMissingSkillsCommand.md) — `/generate_missing_skills` (aliases `/skills_generate_missing`, `/regen_skill_metadata`) to list skills missing metadata and generate/fill them.
- [slashcommands/GetConfigCommand.md](slashcommands/GetConfigCommand.md) — `/get` command to retrieve a nested config value.
- [slashcommands/HealCommand.md](slashcommands/HealCommand.md) — `/heal` (alias `/resurrect`) command to restore any character's health and clear death, defaulting to the invoking player when no target is supplied.
- [slashcommands/HelpCommand.md](slashcommands/HelpCommand.md) — `/help` command to list available slash commands and usage.
- [slashcommands/ImportItemCommand.md](slashcommands/ImportItemCommand.md) — `/import_item` command to open the shared slash upload modal without the blocking execution overlay and import parsed XML item/scenery entries into the invoking player's current location, preserving parsed XML `count` and using an optional absolute `level` arg or the current location level plus XML `relativeLevel`.
- [slashcommands/IncapacitateCommand.md](slashcommands/IncapacitateCommand.md) — `/incapacitate` command to apply the `Incapacitated` status without reducing NPC health, with alias-aware NPC targeting.
- [slashcommands/KillCommand.md](slashcommands/KillCommand.md) — `/kill` command to immediately kill an NPC by name, with alias-aware NPC targeting.
- [slashcommands/LocateCommand.md](slashcommands/LocateCommand.md) — `/locate` command to find NPCs (name/alias) and things by substring and return a markdown table of name/location/region/type.
- [slashcommands/NeedBarsCommand.md](slashcommands/NeedBarsCommand.md) — `/needbars` command to list loaded need bars and directly set/add/subtract need-bar values for the player, a named character, the party, the current location, or a combined `all` target set with immediate invoking-tab refresh.
- [slashcommands/RandomCommand.md](slashcommands/RandomCommand.md) — `/random` command to trigger a random event by type.
- [slashcommands/PromptStatsCommand.md](slashcommands/PromptStatsCommand.md) — `/promptstats` command to show prompt output-character averages in a markdown table, with `/promptstats clear` clearing all stored averages.
- [slashcommands/PlotAnalysisCommand.md](slashcommands/PlotAnalysisCommand.md) — `/plot_analysis` command to show the latest background plot-analysis global in readable Markdown.
- [slashcommands/RegenPartyImagesCommand.md](slashcommands/RegenPartyImagesCommand.md) — `/regen_party_images` command to regenerate all current party NPC portraits while excluding the player character and reusing shared portrait prompt batching.
- [slashcommands/RegexReplaceCommand.md](slashcommands/RegexReplaceCommand.md) — `/regex_replace` command to run regex replacement across chat history, including empty/null replacements for deletion.
- [slashcommands/ReloadConfigCommand.md](slashcommands/ReloadConfigCommand.md) — `/reload_config` command to reload config files and definition caches, with preflight validation for mod defs overlays, warning-only need-bar sentence validation, and restart-required reporting for mod enable/disable changes detected on disk.
- [slashcommands/ReloadLorebooksCommand.md](slashcommands/ReloadLorebooksCommand.md) — `/reload_lorebooks` command to reload lorebooks from disk.
- [slashcommands/RunPlotExpanderCommand.md](slashcommands/RunPlotExpanderCommand.md) — `/runplotexpander` command to run the plot-expander prompt immediately for a specified plot thread, storing it as hidden history and optionally echoing output with `true`.
- [slashcommands/RunPlotSummaryCommand.md](slashcommands/RunPlotSummaryCommand.md) — `/runplotsummary` command to run the plot-summary prompt immediately, storing it as hidden history and optionally echoing output with `true`.
- [slashcommands/RespecAbilitiesCommand.md](slashcommands/RespecAbilitiesCommand.md) — `/respec_abilities` command for ranged ability respec (`start_level`/`end_level`) with robust positional target parsing, alias-aware character resolution, current-location ambiguity tie-breaking, player modal-based reselection, NPC regeneration, and rollback when NPC replacements cannot be applied.
- [slashcommands/RespecSkillsCommand.md](slashcommands/RespecSkillsCommand.md) — `/respec_skills` command to rebuild an NPC's skill allocation for its current level with exact skill-point prompting, alias-aware NPC lookup, current-location ambiguity tie-breaking, rollback on failure, and support for NPCs that currently have no location.
- [slashcommands/RpCommand.md](slashcommands/RpCommand.md) — `/rp` command to toggle roleplay mode and related config checks.
- [slashcommands/OrphanedLocationsCommand.md](slashcommands/OrphanedLocationsCommand.md) — `/orphaned_locations` command to list locations missing valid region links and/or usable exits.
- [slashcommands/SceneSummariesCommand.md](slashcommands/SceneSummariesCommand.md) — `/scene_summaries` (alias `/summary_ranges`) command to list stored scene summaries by number and covered entry range, with coverage-gap diagnostics.
- [slashcommands/ScrubLegacyDebugCommand.md](slashcommands/ScrubLegacyDebugCommand.md) — `/scrub_legacy_debug` (aliases `/scrub_debug_history`, `/scrub_debug`) command to remove old check/tool-call debug pollution from chat history and stored scene summaries, with `dry_run=true` reporting.
- [slashcommands/SceneSummaryCommand.md](slashcommands/SceneSummaryCommand.md) — `/summarize` (alias `/scene_summary`) command to export scene summaries, including shared scene-index `check` behavior and `all + redo=true` full rebuild behavior.
- [slashcommands/ScheduledCommand.md](slashcommands/ScheduledCommand.md) — `/scheduled` command to list pending scheduled events in readable list-style Markdown with due time, relative time, region, location, and full event text.
- [slashcommands/ShortDescriptionCheckCommand.md](slashcommands/ShortDescriptionCheckCommand.md) — `/short_description_check` command to list missing short descriptions for regions, locations, things, and abilities.
- [slashcommands/SetConfigCommand.md](slashcommands/SetConfigCommand.md) — `/set` command to update a nested config value at runtime.
- [slashcommands/SetLastSeenCommand.md](slashcommands/SetLastSeenCommand.md) — `/set_last_seen` command to set persisted NPC last-seen time/location for every NPC currently at a specified location, supporting `all` for every non-current location, exact `h AM/PM` or `h:MM AM/PM` times, and relative `<duration> ago` parsing.
- [slashcommands/SetLevelCommand.md](slashcommands/SetLevelCommand.md) — `/setlevel` command to set the invoking player or a named character to an exact level with alias-aware target resolution while leaving XP unchanged.
- [slashcommands/SlashCommandBase.md](slashcommands/SlashCommandBase.md) — Base class for slash commands: metadata, optional execution-overlay suppression, arg validation, and listing.
- [slashcommands/SlopwordsCommand.md](slashcommands/SlopwordsCommand.md) — `/slopwords` command to report slop words over ppm thresholds.
- [slashcommands/TeleportCommand.md](slashcommands/TeleportCommand.md) — `/teleport` command to move the player to a location by id or name.
- [slashcommands/TimeCommand.md](slashcommands/TimeCommand.md) — `/time` command to advance or rewind the world clock by a signed duration, using the shared minute parser with normal forward arrival processing for positive adjustments.
- [slashcommands/VehicleStatusCommand.md](slashcommands/VehicleStatusCommand.md) — `/vehicle_status` command to display detailed markdown status for the current vehicle, including resolved and pending destination data, route list, trip-state flags, departure time, ETA, and vehicle exit metadata.
- [slashcommands/WeatherCommand.md](slashcommands/WeatherCommand.md) — `/weather` command to display current-region seasonal weather details in markdown.
- [slashcommands/WorldOutlineCommand.md](slashcommands/WorldOutlineCommand.md) — `/world_outline` command to list regions, locations, and pending stubs.
