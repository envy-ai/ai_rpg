# Follow-Up API Playtest Harness

This is the implemented fast reproduce/fix/reverify loop for the scenarios in [ten_turn_api_playtest_followup_plan.md](ten_turn_api_playtest_followup_plan.md). It combines immutable save fixtures, minimal config profiles, strict logical-completion cassettes, declarative API scenarios, and focused triage artifacts.

## Goal alignment for human review

This workflow is intended to produce naturally flowing prose, keep event detection/bookkeeping synchronized with prose and player actions, and find genuine prompt/game-code bugs. It is not an optimization loop for technically perfect narration, one exact selection of details, or 100% sentence-level accuracy. Harmless additional story material is allowed; do not treat “information the reviewer did not specifically request” as a defect unless it creates a material contradiction or unauthorized state/action.

It is also explicitly not a benchmaxxing exercise. Scenarios, cassettes, assertions, and completion counts are tools for finding and reproducing real defects; passing them is not an end in itself. Do not tune production prompts to fixture wording, alter setup so it sidesteps the behavior under test, weaken a valid contract to obtain green output, or add test-specific production behavior. Prefer a fix that generalizes to ordinary unscripted play, and treat a passing case as evidence for that fix rather than proof by itself.

Continue reviewing live-model prose, but record each issue in `tmp/prose_issues_log.md` with the case/attempt, lead-up, verbatim failing prose, and a precise diagnosis. Prose-only quality issues are queued there for joint review rather than immediately becoming new prompt restrictions. Material disagreement with player action, accepted events, tool outcomes, or authoritative state remains an in-scope synchronization failure: investigate its general prompt, context, parser, bookkeeping, or code cause, and also retain the prose example in the log.

Every proposed prose instruction has a naturalness cost because it redirects model attention from storytelling to rule compliance. Prefer mechanically authoritative context and structured validation for facts the game can know. Do not add a prompt rule merely to force one reviewed response to use preferred wording, omit harmless connective prose, or reproduce one exact human interpretation.

The operational shorthand is: let the model tell the story, let code own facts and state, validate what can break the game, and tolerate what merely makes a response imperfect. In particular, skip model checkpoints whose exact result is already derivable, keep broadly applicable prompt contracts singular and concise, enforce semantic invariants in structured parsers/domain code, ignore provably harmless duplicate/no-op events at the structured boundary, and preserve NPC agency and reasonable narrative variation.

Do not independently move prompts to or from the slower-model routing list during playtesting or human review. If a prompt appears to need the more capable model, log the prompt family, concrete evidence, expected quality improvement, approximate runtime frequency, expected latency cost, and why a general prompt/parser/code correction is not the better fix. Discuss that recommendation with the user before changing routing. Rare, difficult prose may justify the tradeoff; common prompts require particular caution because a marginal quality gain can impose a large aggregate speed loss.

This workflow was added after the follow-up API playtest was already underway. The original 71 backend cases with retained passing API assertion evidence remain complete; they do not need to be retrofitted into this harness. VEH-8 through VEH-10, REG-1 through REG-6, FAST-1 through FAST-7, NEED-1 through NEED-8, HIDE-1 through HIDE-8, TIME-1 through TIME-7, EVENT-1 through EVENT-5 and EVENT-7 through EVENT-8, REL-1 through REL-3, REL-5 through REL-8, PROG-1 through PROG-9 plus PROG-11, and ALT-1 through ALT-12 subsequently passed through the harness or its source-controlled deterministic test layer. All 148 backend cases have been exercised: 146 are verified, EVENT-6 and REL-4 are retained audited model failures, and 0 remain pending. Continue using the harness wherever an expensive or model-backed defect benefits from deterministic replay.

## Hard boundary: do not mechanically police generated prose

The harness must not use regular expressions, keyword searches, dialogue detection, prose classifiers, or ad hoc conditionals to decide whether narration obeyed an instruction. Automated assertions are limited to structured HTTP responses, authoritative game state, event/history metadata, prompt/retry metadata, cassette state, and persistence. Narrative fidelity remains an explicit human-review item in the scenario and triage report, with issues preserved in `tmp/prose_issues_log.md` rather than converted into one-response prompt patches.

## Commands

Run focused harness/cassette tests:

```bash
npm run test:followup-harness
```

Prepare a temporary server override by merging an existing base override with a source-controlled test profile and cassette mode:

```bash
node scripts/followup_api_playtest.mjs prepare-config \
  --profile vehicle-mechanics \
  --base-override config.yaml.qwen-combo-router \
  --mode live-record \
  --cassette tmp/vehicle-live-record.json \
  --output tmp/vehicle-live-record.override.yaml
```

Start the server separately with that one generated override. The harness intentionally does not start, stop, or restart the game:

```bash
npm run start -- --config-override ./tmp/vehicle-live-record.override.yaml --port 7777
```

Run a scenario:

```bash
node scripts/followup_api_playtest.mjs scenario \
  --file tests/followup_api_playtest/scenarios/vehicle/VEH-7-off-route.json \
  --mode live-record \
  --cassette tmp/vehicle-live-record.json
```

`npm run followup:scenario -- ...` is the package-script equivalent. `AI_RPG_TEST_BASE_URL` and `AI_RPG_TEST_WS_URL` override the default `http://127.0.0.1:7777` and `ws://127.0.0.1:7777/ws` endpoints.

The legacy `chat`, `interactive-request`, `request`, and `snapshot` commands remain available. `promote-fixture` promotes a validated save, while `prepare-config` only writes YAML under `tmp/`.

## Explicit modes

- `live-record` requires one active recording destination and prohibits replay. It records every accepted logical completion, then marks the cassette complete only after the scenario request settles.
- `replay` requires a complete version 2 forced-output cassette and prohibits recording. Every entry must be consumed in exact global order.
- `live-verify` prohibits recording and forced output; use it for the final intended-model verification.
- `state-only` also prohibits cassette configuration and is for setup/persistence scenarios that invoke no LLM.

There is no implicit mode and replay never falls through to a provider. Use `vehicle-mechanics-replay` when starting a replay server: in addition to the vehicle isolation settings, it blanks the managed startup script/router preload and disables model-switch/image handoff so llama.cpp is neither started nor contacted.

## Scenario declarations

Scenario JSON lives under `tests/followup_api_playtest/scenarios/` and uses schema version 1. Required top-level fields are `version`, `scenario`, `case`, and a nonempty `steps` array. Optional fields are `description`, `fixture`, `configProfile`, `trackedPaths`, final `assertions`, and `humanReview`.

`fixture` may be a canonical fixture name or an existing-save object with `saveName`, optional `saveType`, and optional `entities`. Strings anywhere in the definition can reference manifest entities with `$fixture.<path>`; unresolved fixture variables fail before the realtime connection or fixture mutation.

`snapshot`, `request`, `waitForRequest`, `chat`, `startChat`, `save`, `readSavedJson`, and `reload` steps may have a unique alphanumeric/underscore/hyphen `name`. Named request-like and saved-JSON steps retain their complete response envelope under `responses.<name>`, while snapshots are retained under `snapshots.<name>`. Later routes, bodies, and assertions can use `$response.<name>.<path>` or `$snapshot.<name>.<path>`; an exact variable may resolve to an object or array, while interpolation inside a larger string requires a scalar. Runtime variables fail explicitly at the consuming step if the named value or path does not exist.

Supported step types are:

- `loadFixture`: optional explicit load information; canonical fixtures are already loaded at scenario setup and a matching step records that intent without a second mutation.
- `snapshot`: capture a named state snapshot.
- `request`: arbitrary method/route/body, optionally interactive.
- `startChat`: begin `/api/chat` without blocking the scenario so realtime activity, pending input, and cancellation can be tested while the request is live. It accepts the same travel and optional `forcedNpcTurns` queue as `chat`. Every named start must have exactly one later `awaitChat`; definitions with missing, duplicate, or unknown pairs fail validation. Failure cleanup cancels/drains outstanding work and settles every started request.
- `waitForRealtime`: poll mechanical assertions against the realtime event collection until they pass or its explicit timeout expires. This is the synchronization boundary for active prompts and deferred player-input events.
- `awaitChat`: settle a previously named `startChat`, store its complete response envelope under `responses.<name>`, and end its realtime request scope.
- `waitForRequest`: poll one read-only or otherwise idempotent method/route until its declared mechanical assertions pass; use this to synchronize on an observable authoritative outcome instead of a guessed sleep.
- `chat`: `/api/chat` text plus optional travel metadata, interactive policy, and a nonempty `forcedNpcTurns` array of NPC names or `{ id, name }` objects when a scenario must exercise a deterministic post-player NPC queue. Forced selection does not bypass ordinary dead/incapacitated/missing/duplicate-actor rejection.
- `waitForPromptIdle`: wait until a matching prompt-progress label is absent for a quiet period. Live modes can require an observed active frame; strict replay skips that requirement because millisecond cassette completions can start and finish between websocket snapshots.
- `save` and `reload`: exercise normal persistence endpoints.
- `readSavedJson`: capture one path-safe JSON file from a named save, or from the immediately preceding `save`, for assertions on private persisted fields that intentionally do not appear in gameplay API projections.
- `assert`: run one or more mechanical assertions immediately.
- `humanReviewNote`: add a narrative/visual review item without pretending to automate it.
- `nodeTest`: run one or more explicit source-controlled `tests/*.test.js` files through Node's test runner and retain the result under the step name. Paths cannot escape `tests/`; this is reserved for deterministic parser/lifecycle contracts that do not meaningfully evaluate a live model.

Unknown top-level fields, step fields, step types, assertion fields/types, fixture variables, and malformed interactive policies fail schema validation. An interactive request must state how to handle its roll, confirmation, quest acceptance, and optional free-text answer; `deferPlayerInput: true` records a `player_input_request` without automatically answering it so cancellation can be exercised. One persistent realtime client serves the full scenario without a client-side response deadline.

## Mechanical assertions

The version 1 vocabulary is deliberately small:

- `httpStatus`, `responseOk`;
- `exists`, `absent`, `nullish`, `equals`, `notEquals`, `count`, `includes`, `notIncludes` at explicit object paths; `nullish` accepts either an omitted projection field or an explicit null;
- `stateUnchanged` and `exactDelta` across the default before/after state or explicit `beforeSource`/`afterSource` named states;
- `greaterThan`, `greaterThanOrEqual`, `lessThan`, and `lessThanOrEqual`, including comparisons against another source/path with `valueFrom`;
- `entityField` by canonical entity ID, with exact equality or conjunctive changed-value/text-inclusion checks (optionally case-insensitive); `entityArrayObjectCount` for nested structured rows on that entity, `entityArrayUnique` for duplicate-free scalar/object arrays on an entity, and `uniqueBy` for structured collections;
- `arrayObjectCount` for an exact count, `arrayObjectCountAtLeast` for a minimum, and `arrayObjectCountBetween` for inclusive bounds on array objects containing a mechanically specified field/value subset; `arrayObjectField` selects exactly one such object and checks one nested field, while `arrayObjectFieldCount` checks the size of its nested collection without relying on the object's outer-array index;
- `arrayNumericFieldBounds` to require every row in a structured array to carry a finite numeric field within declared inclusive or exclusive bounds;
- `attackResultCount` to flatten `resolveAttack` and every per-target `resolveAreaAttack` result into one shared mechanical shape before matching actor, canonical target, hit, roll, damage, or tool kind;
- `attackResultApplied` to require exactly one matching normalized attack result and prove that its recorded starting/ending health equals the canonical target entity's before/after state, with optional positive-health-loss enforcement;
- `historyAddedTypeCount` for new structured chat-history entry types, `historyAddedSequence` for exact ordered subsets of newly appended structured rows (optionally filtered by entry type), and `historyAddedNestedObjectCount` for exact subset-matching structured objects at a nested path within filtered newly added history rows;
- `dispositionChangesMatchState` to require at least one authoritative NPC-to-player disposition delta, the requested direction, and exactly one matching structured summary with exact previous/new/delta values and a nonempty reason for every changed type; it accepts both persisted/detail and client-list disposition projections;
- `factionReputationChangesMatchState` to require one authoritative sparse faction-standing delta in the requested direction plus exactly one structured summary with matching faction id, before/after/amount values, and a nonempty reason; `onlyFaction` additionally rejects unrelated faction deltas or summaries;
- `realtimeEventCount` for exact structured WebSocket-event cardinality and `promptRunCount` for unique prompt-progress runs matching a label fragment;
- `noRealtimeErrors` and the compatibility-named `noUnexpectedErrorLogs`. Realtime harness errors remain mechanical failures. `noUnexpectedErrorLogs` is diagnostic-only: it always passes, while preserving every changed `ERROR_*` file in `logs.json`, triage, and benchmark reports so non-fatal parser, tool, and transport problems remain conspicuous. Its historical exact-name/prefix and `allowRecoveredProviderRetries` options remain schema-compatible and classify the diagnostic details, but no error-log class affects scenario status merely by existing. Response status, state, parser exhaustion, realtime errors, and infrastructure failures remain independently graded;
- `cassetteConsumed` for a completed strict replay.

`equalsFrom` can compare one explicit source/path to another. Assertion sources are paths into the runner context, including `before`, `after`, `response`, `responses.<name>`, `snapshots.<name>`, `realtime`, `changedLogs`, `cassetteStatus`, `steps`, and `fixture`. History-delta assertions also accept explicit before/after sources. Assertions stop the scenario at the first failed checkpoint, but safe after-state/log/realtime capture still runs.

## Artifacts and triage

Every run allocates a new directory under `tmp/followup_api_playtest/<scenario>/<case>/attempt-N/`; attempts are never overwritten. Depending on the path taken it contains:

- the validated scenario and run metadata;
- effective config-profile values;
- before/after and named snapshots;
- request/response and per-step timings;
- realtime events and cassette status;
- changed-log manifest without deleting or truncating logs;
- `triage.json` and a short `triage.md`.

Triage reports the failed invariant, response error/stack, selected state changes, structured history additions, prompt/checkpoint timeline and retry counts, changed error logs, cassette consumption, and human-review notes. It summarizes prompt metadata but does not copy prompt text or infer narrative compliance.

State snapshots include players, locations, live region summaries, things, all hydrated scheduled-event records, history, calendar data, and detailed current-player/current-location payloads. Scheduled events come from the read-only Story Tools/admin projection so hidden descriptions and resolved/skipped lifecycle state never enter ordinary Adventure payloads. Region-expansion scenarios use the live-region collection for canonical identity checks and the region-map endpoint for exact membership/uniqueness checks.

The selected-model aggregate runner is documented in [model_benchmark.md](model_benchmark.md). It consumes this same scenario vocabulary, adds the converted pre-harness cases, pins every prompt route to one model, and produces interruption-safe JSON plus a human-review HTML dashboard.

## Canonical fixtures

Promoted fixtures live under `tests/followup_api_playtest/fixtures/<name>/` with an immutable `save/` copy and `manifest.json`. The manifest records provenance `@@` setup, canonical IDs, required mods/profile, starting world time, invariants, validation evidence, and a recursive SHA-256 integrity hash.

Reusing a starting point always means copying it first. Do not alter an existing fixture, scenario, cassette, or case-specific profile to prepare a different case. Create a uniquely named, case-owned derivative and change only that copy; the harness then makes a further disposable runtime save for execution. Shared production, harness, test-helper, profile, or documentation files may change only when the behavior is genuinely general rather than an accommodation for one test.

Promotion refuses missing/failed validation evidence, empty invariants, hash drift, and replacement without an explicit flag:

```bash
node scripts/followup_api_playtest.mjs promote-fixture \
  --name vehicle-underway-eastbound \
  --source-save <save-directory> \
  --manifest-file tmp/vehicle-underway.manifest-input.json
```

At runtime the harness verifies integrity, copies the fixture to a uniquely named disposable autosave, adjusts only loader metadata in that copy, loads through `/api/load`, checks manifest invariants immediately, and removes only that recognized temporary fixture on exit. The source fixture is never mutated. `--keep-runtime-fixture true` retains the copy for diagnosis.

## Config profiles

Profiles live under `tests/followup_api_playtest/config_profiles/`, use version 1, store dotted config paths, and support single-parent `extends` inheritance with cycle detection.

- `isolated-base` disables image generation and unrelated foreground/background prompt families.
- `alternative-prompts-rejection` derives from the shared alternative-prompt isolation and enables only the plausibility checks required by ALT-8's explicit rejection contract; its `-replay` derivative additionally prevents local model/router startup.
- `vehicle-mechanics` re-enables only event and plausibility checks needed for the vehicle case.
- `vehicle-mechanics-replay` additionally prevents local llama/router startup and lifecycle calls.
- `combat-friendly-only` enables the combat NPC scheduler with one friendly slot, no hostile slot, ordinary NPC turns off, legacy prompt checks off, and image generation inherited off.
- `combat-friendly-only-replay` retains those mechanics while preventing local llama/router startup and lifecycle calls.
- `need-bars-mechanics` enables only the event, plausibility, and TinyBrain need-bar checks required by need/status cases.
- `need-bars-mechanics-replay` retains those mechanics while preventing local llama/router startup and lifecycle calls.
- `long-time-mechanics` layers scheduled-event resolution/interruption and while-away TinyBrain families onto isolated need/status processing.
- `long-time-mechanics-replay` retains those mechanics while preventing local llama/router startup and lifecycle calls.
- `relationship-disposition-mechanics` copies relationship/faction mechanics while deferring unrelated housekeeping maintenance; `relationship-disposition-mechanics-replay` retains that isolation while preventing local llama/router startup and lifecycle calls.
- `faction-reputation-mechanics` is a separate copy for witnessed/unwitnessed reputation cases with unrelated housekeeping deferred; `faction-reputation-mechanics-replay` retains the same isolation without local llama/router startup or lifecycle calls.
- `hostility-transition-mechanics` is a separate state-only profile for exact disposition-threshold transitions; every unrelated prompt family and image generation remains disabled.
- `progression-mechanics` isolates player/NPC progression and ability-card behavior with image generation and unrelated turn families disabled; `progression-mechanics-replay` retains those mechanics while preventing local llama/router startup and lifecycle calls.
- `full-live-verification` retains normal Qwen gameplay checks needed by broader final live testing while leaving image generation and unrelated turns off.

Before fixture load, the runner reads every required profile value from the running server through `/get`. Any mismatch fails before world-state mutation. `prepare-config --base-override` is the supported way to retain a router/model configuration while layering one test profile and its cassette paths into the single CLI override accepted by the server.

## Version 2 completion cassettes

`LLMCompletionCassette.js` records/replays at the accepted logical `LLMClient.chatCompletion(...)` boundary, not at the HTTP/SSE-chunk boundary. This means parser retries, repeated TinyBrain labels, and tool-loop rounds are separate ordered entries, while low-level failed transport attempts are not.

Each entry stores a normalized metadata label, semantic SHA-256 request fingerprint, compact non-secret summary, and normalized response including tool calls. Requests are canonicalized with sorted object keys and order-preserving arrays; data URLs are hashed. API keys, auth/custom headers, request/client IDs, timestamps, generated cachebusters, and generated seeds are excluded.

Recording writes valid JSON atomically after each accepted completion and remains `complete: false` until the runner settles the scenario and calls the completion endpoint. Cassette-backed `LLMClient` calls are globally serialized: an ordinary completion owns the cassette permit for one call, while a reserved TinyBrain program retains it across every checkpoint and yields it only alongside its model permits for a nested prompt-launching tool. This gives concurrently launched event and need-bar programs a deterministic whole-program order instead of allowing their stages to interleave. Finalization waits for stable idle and rejects active or queued cassette work.

If a live-record scenario fails after accepting one or more completions, the runner preserves the partial document as `incomplete-completion-cassette.json` in that attempt's artifact directory and calls the explicit incomplete-recording reset endpoint. Reset is allowed only while idle and only for an incomplete version 2 recording; a completed cassette is refused. Live-record preflight likewise refuses a nonempty or completed destination. This prevents a later attempt in the same server process from appending to the failed attempt's process-local recorder while retaining the failed trace for diagnosis.

Strict replay requires `version: 2`, `strict: true`, `complete: true`, an exact next label/fingerprint, no unused entries, no active/queued cassette work, and no latched replay failure at the end. Tool calls still execute normally after their replayed assistant response, so downstream state and orchestration remain under test. Structured JSON tool results omit reconstruction-time metadata such as `createdAt` and `lastUpdated` from the fingerprint; semantic tool fields remain strict. Token-stream callbacks are intentionally inert during replay because the cassette records accepted logical responses rather than an SSE/token trace.

Cassette replay is not evidence for provider transport, streaming/SSE, live token deslop, XML stream-repetition interruption, router lifecycle, or narrative instruction following. Those require focused transport tests and/or the final live-model pass.

## VEH-7 vertical slice

The first converted regression is `scenarios/vehicle/VEH-7-off-route.json`, backed by `fixtures/vehicle-underway-eastbound`. It restores Baato and Mizore aboard an underway fixed-route QA Clockwork Tram, deterministically adds an explicit QA-only route-authority precondition to the copied player's description through the normal character update API, requests an off-route redirect, and asserts an explicit failed HTTP response, unchanged player/vehicle/time state, no committed `player-action` history entry, and no realtime harness error. The runtime-only description change removes the unrelated player-agency rejection branch without mutating the immutable source fixture or triggering level-up ability selection.

The application fix has two layers:

1. `Player.currentVehicle.allowedDestinations` resolves the route into canonical structured candidates, and `player_action_vehicle_destination` rejects/canonicalizes destination answers during the retryable TinyBrain checkpoint. Retry-local state anchors the first syntactically complete destination, so parser feedback cannot turn an off-route request into a different on-route request merely to satisfy validation.
2. The vehicle mutation boundary tags any remaining invalid-route domain error, and `/api/chat` rethrows that tag instead of swallowing it in the general event-check warning catch.

This is structured parser/domain validation. It does not inspect generated prose for phrases implying success.

The source-controlled replay trace is `cassettes/vehicle/VEH-7-off-route.json`. The final 2026-08-10 verification recorded a 35-completion live trace, consumed all 35 entries in a no-llama strict replay, and then passed a separate unrecorded live-Qwen run. Each pass returned the expected checkpoint-26 HTTP 500 after seven attempts and left player location, fixed-route vehicle state, world time, and committed player-action history unchanged.

## VEH-8 state-only arrival slice

`scenarios/vehicle/VEH-8-timed-arrival.json` uses the same immutable underway fixture without any LLM call. It advances the clock to minute 668, proves the ETA-669 tram is still underway with its external exit hidden, advances one more minute, and proves the arrival is finalized exactly once. A zero-minute follow-up proves the finalized vehicle and exit state are idempotent. Attempt 1 passed all 37 assertions with no realtime errors or changed error logs; Rika remained aboard and retained ownership of the QA Brass Travel Case throughout.

## VEH-9 live disembark slice

`scenarios/vehicle/VEH-9-disembark.json` restores the immutable underway fixture, advances exactly to arrival, then exercises the ordinary event-driven vehicle exit under the live Qwen router. `/api/chat` exposes the committed TinyBrain movement classification as optional structured `authoritativeMovementType`, allowing the harness to prove `disembark` without inspecting narration. The scenario separately fetches the detailed tram endpoint because the bulk locations projection intentionally omits full `vehicleInfo`. Live-verify attempt 2 passed all 31 assertions: the player moved to QA East Platform without additional time, one player action and one event summary were committed, the tram retained its canonical fixed route with cleared trip timing, and no realtime or changed error-log failures occurred.

## VEH-10 state-only persistence slice

`scenarios/vehicle/VEH-10-save-reload.json` saves the canonical fixture while the tram is underway, mutates only the disposable runtime clock, and reloads through the normal save API. It then advances across the restored ETA and performs a zero-minute duplicate check. State-only attempt 1 passed all 46 assertions: the exact active route, ETA, departure time, tracked exit, player vehicle context, companion location, and carried-item ownership survived hydration; the trip arrived once and remained stable afterward.

## REG-1 live pending-region entry slice

`scenarios/cross-region/REG-1-direct-region-entry.json` restores the immutable `cross-region-pending-frost-march` fixture, traverses its zero-minute pending-region doorway through `/api/player/move`, and then inspects the live region map, origin exit, and preserved child stub. Live-verify attempt 4 passed all 40 assertions: expansion created one canonical entrance under the pending region id, moved the player there, backfilled and rewired the origin exit to five minutes, retained QA Border Lantern as a non-region-entry child stub in QA Frost March, and produced unique region-map membership with one event summary and no unexpected errors.

## REG-2 live child-stub slice

`scenarios/cross-region/REG-2-child-stub.json` restores the derivative immutable `cross-region-expanded-frost-march` fixture. That fixture is the fully validated REG-1 post-state, so later child-expansion cases do not need to regenerate the target region while the original pending-region fixture remains available for REG-1. Live-verify attempt 1 passed all 44 assertions: moving through the existing two-minute exit expanded QA Border Lantern in place under its original `loc_38` id and QA Frost March region id, retained one canonical map member and the reciprocal entrance exits, moved the player and advanced time exactly once, added one event summary, and generated no realtime or changed error-log failures. Human review accepted the generated frozen-frontier lantern checkpoint as consistent with its saved blueprint.

`scenarios/cross-region/REG-3-dialogue-movement.json` restores `cross-region-alias-companion`, which adds living local `QA Quartermaster` with alias `Kuroha` to the validated expanded-region fixture. Live-verify attempt 3 passed all 51 assertions. The direct player-action travel prompt resolved `Kuroha` to the canonical companion, expanded the destination stub for destination prose while leaving both actors and the clock at the origin, and returned the canonical name in `accompanyingCharacters`. Passing that response unchanged to `/api/player/move` then moved both actors once, advanced exactly two minutes, retained the canonical exit and unique region membership, and emitted one event summary without another player-action entry. Human review accepted the single coherent departure/walk/arrival and found no duplicate character. The failed first attempt also identified an oversized sequential TinyBrain event transcript: each phase had retained another full base context until the request reached about 498 KB and llama.cpp returned HTTP 400. Section continuation now refreshes the one existing base-context snapshot in place while preserving earlier event prompts and accepted assistant replies; focused three-section regression coverage proves stale snapshots do not accumulate.

`scenarios/cross-region/REG-4-model-selected-destination.json` restores `cross-region-expanded-frost-march` and supplies no authoritative travel metadata beyond the ordinary travel flag. Live-verify attempt 3 passed all 41 assertions. Player-action selected QA Aurora Cairn in QA Frost March with a three-minute route; the server then generated and expanded exactly one destination, made canonical reciprocal three-minute exits, moved the player once, advanced the clock once, and retained unique region-map membership. One player-action and one event-summary row were added, and no realtime or changed error-log failure occurred. Human review accepted the single coherent trip and the generated cairn's regional/route consistency. Failed attempts exposed that the tool loop would execute any registered provider-emitted tool even when the active request omitted it. Explicit request schemas now serve as execution allowlists: undeclared calls return a retry-visible `tool_not_declared` result without reaching their executor, while the TinyBrain destination-lookup checkpoint preserves its `moreInfo`-only schema through base-context policy.

`scenarios/cross-region/REG-5-failure-isolation.json` restores `cross-region-ambiguous-destination` and sends authoritative fast-travel metadata whose canonical destination id does not exist. Live-verify attempt 4 passed all 23 assertions. The API rejected the request with HTTP 400 before any prompt or prose, retained the exact player location, time, locations, regions, and exits, added no player-action or event-summary history, preserved the two intentionally same-named stubs in their distinct regions, and produced no realtime harness error. A model-selected ambiguity input was deliberately replaced because the model could invent a region and make that action unambiguous; the invalid authoritative id makes the negative invariant deterministic.

`scenarios/cross-region/REG-6-persistence.json` restores the already expanded Frost March fixture, captures its map, cross-region origin, and child-stub projections, saves, deliberately advances time, then reloads. State-only attempt 3 passed all 57 assertions. The save restored the exact player, clock, global location, and live-region projections; retained one five-member QA Frost March map with unique ids and consistent region ownership; preserved the entrance, ordinary child stub, and all relevant one-, two-, and five-minute route topology; matched the complete pre/post map and stub payloads; and emitted no realtime or changed-log errors. Exit objects receive fresh audit timestamps during hydration, so the scenario asserts their authoritative route fields rather than misclassifying timestamp reconstruction as topology loss.

The immutable `fast-travel-visited-favorites` fixture supplies visited/favorited QA Fast Travel Grove and QA Fast Travel Tower on canonical four- and nine-minute two-way routes from March Start Marker, plus non-party QA Courier with alias Quickstep. Entity creation used the documented `@@` tool path for the courier and deterministic exit/visit/favorite APIs for route precision. `scenarios/fast-travel/FAST-1-preview.json` state-only attempt 1 passed all 43 assertions: both preview responses returned exact canonical endpoints, region names, and directed shortest-path minutes without changing player, NPC, location, clock, or history state and without emitting errors.

`scenarios/fast-travel/FAST-2-blank-confirmation.json` reproduces the blank map/Favorites confirmation sequence rather than treating it as a prose turn. Live-verify attempt 2 passed all 47 assertions: one travel-marked comment entry was stored without any model prompt or player-action row, the subsequent gameplay teleport moved once to the Grove and advanced exactly four graph minutes, one event-summary row captured travel, the aliased non-party courier remained at the origin, normal arrival processing completed without error, and the four-minute revisit correctly stayed below the WYWA threshold.

`scenarios/fast-travel/FAST-3-prompted-confirmation.json` covers the nonblank map/Favorites branch. Live-verify attempt 1 passed all 46 mechanical assertions: authoritative fast-travel metadata fixed the existing Tower as the destination, player-action generated prose and one history row without moving or advancing time, and the separate gameplay teleport performed the only movement, exact nine-minute graph advance, and one travel summary. No WYWA or runtime error occurred at the below-threshold revisit. Human review accepted the single Tower-bound trip and found no competing Grove destination or duplicate movement.

`scenarios/fast-travel/FAST-4-companion-alias.json` covers a non-party companion selected by alias. Live-verify attempt 1 passed all 48 mechanical assertions: `Quickstep` canonicalized to the one local `QA Courier`; neither actor moved during the prose step; the teleport moved both to the Grove exactly once with the exact four-minute graph advance; and alias, NPC identity, and non-party membership remained unchanged. Human review accepted the single generic courier as that character and found no party-join or second inter-location movement claim.

`scenarios/fast-travel/FAST-5-wywa.json` compares both above-threshold arrival contracts from the same saved baseline. Live-verify attempt 1 passed all 58 mechanical assertions. After 31 minutes away, a same-request event-driven TinyBrain move advanced the exact four-minute route, stored one hidden WYWA bookkeeping row, and added no visible WYWA row because its player-action already contained destination prose. Reloading the baseline and using the browser-equivalent blank comment plus normal teleport advanced the same four minutes and stored one hidden plus one visible WYWA row. Neither branch emitted realtime, arrival-processing, or changed-log errors. Human review accepted the first destination scene and the blank path's one reunion scene without duplicate travel or a conflicting destination.

`scenarios/fast-travel/FAST-6-story-tool-contrast.json` proves the story-tool shortcut is isolated from gameplay arrival processing. The scenario ages the destination past the WYWA threshold, marks existing Grove NPC Mina hidden, and deliberately supplies the courier alias to the teleport. State-only attempt 1 passed all 52 assertions: only Baato moved; time, complete history, courier location, Mina's hidden flag, and Mina's last-seen fields stayed unchanged; the response omitted arrival-processing data; no WYWA, player-action, or travel-summary row appeared; and no prompt, realtime error, or changed error log occurred.

`scenarios/fast-travel/FAST-7-invalid-target.json` sends one nonexistent canonical destination through both preview and gameplay teleport. State-only attempt 1 passed all 37 assertions. Both endpoints returned their documented HTTP 404 payloads before mutation; the exact player, character, location, clock, and history projections stayed unchanged; no player-action, WYWA, or event-summary row appeared; and no prompt, realtime error, or changed log occurred. Fast-travel coverage is now complete at 7/7 cases.

The immutable `need-bars-status-lifecycle` fixture supplies Baato, party member QA Tired Companion, non-party QA Courier, and one carried QA Restorative Meal whose ten-minute QA Well Fed status has a need modifier and attribute modifier. Entity creation used two `@@` tool calls, followed by deterministic API normalization of status mechanics, ownership, party membership, and raw need values. The manifest contains executable fixture invariants and the save tree has SHA-256 `4fd7386a4255970712445d30c58c6f7087eaa3bd9cb096c220ef2caf24878a13`.

`scenarios/need-bars/NEED-1-passive-decrease.json` uses the fixture under `need-bars-mechanics`. Its original live verification established that one agreed strenuous minute gave Baato and QA Tired Companion one structured stamina decrease apiece, ordinary elapsed-time processing applied the expected passive tick, the non-party courier remained unchanged, and the event summary matched the applied stamina changes plus elapsed time. The model benchmark deliberately does not prescribe the model-judged stamina magnitude or reject additional need effects that are valid under the configured trigger lists; it verifies the actor, bar, direction, applied summary metadata, passive processing, and audience boundary instead. Human review remains responsible for judging whether any additional need effect fits the narrated activity. The rejected first source attempt was an invalid test instruction that directly controlled the NPC; plausibility correctly prevented mutation, and the corrected setup instead gave the companion a voluntary precommitment before the player supplied the signal.

`scenarios/need-bars/NEED-2-recovery.json` dismisses the fixture companion for the case and teleports both fixture NPCs to an existing empty location through normal APIs, leaving Baato alone at the baseline marker. This isolates recovery mechanically without trying to constrain or classify NPC prose. Live-verify attempt 4 passed all 59 assertions: one explicit one-minute short rest produced exactly Baato Rest +100 and a capped stamina fill after passive time rates; player stamina stopped at its configured 1000 maximum; remote actors received only ordinary elapsed-time drift; the restorative meal remained in inventory; QA Well Fed was not applied; Baato did not move; and one action plus one matching structured event summary were added without realtime or changed-log errors. Human review accepted one quiet stationary minute with no food, drink, meditation, magic, sleep, strenuous activity, or second player action. Earlier attempts exposed test isolation and trigger-wording variability, not production defects: nearby NPCs validly performed their own narrated activities, and generic quiet sitting did not always classify as the configured `short rest` trigger.

`scenarios/need-bars/NEED-3-threshold.json` exercises the direct need endpoint without an LLM. State-only attempt 2 passed all 50 assertions. Rest is first set exactly at threshold 150, yielding `Tired` and its configured effect metadata, then set to `149.99999999999997`, JavaScript's next representable number below 150. Exactly one unique Rest bar switches to threshold 10 `Very Tired` with `-5% to health and attributes`; the old threshold is absent; other player and NPC needs, statuses, inventory, location, time, and history stay unchanged. Reapplying the same crossed value leaves the complete tracked state unchanged and creates no duplicate bar. Threshold `effect` strings are authoritative descriptive metadata used in prompt/UI/summary context; they are not executable expressions and the test does not pretend that the runtime parses arbitrary prose into stat modifiers.

`scenarios/need-bars/NEED-4-consume-effect.json` mechanically isolates Baato and sets Food to 100 so the configured normal-meal magnitude is distinguishable from a fill. Live-verify attempt 2 passed all 64 assertions and human review. One minute of eating exactly one QA Restorative Meal produced one `item_ingest`, one quantity-one `consume_item`, one gained item-backed status, and an immediate Food `large` +700 after the baseline passive tick, ending at 799.3. The meal disappeared from both inventory and the global thing map. Exactly one QA Well Fed instance started at minute 664 with duration 10, Constitution +2, and the separate Food +5-per-minute modifier; that timed modifier correctly did not apply retroactively in its creation minute. The Constitution modifier raised current and maximum health by four through existing derived-stat behavior. History added one player action, one general event summary, and the dedicated one status-summary row, with no errors. The first source attempt's only failure was a scenario assumption that status changes used a second `event-summary`; authoritative history correctly uses `status-summary`.

`scenarios/need-bars/NEED-5-expiry.json` installs the same status directly at fixture minute 663 and uses the ordinary forward-time slash-command path. State-only attempt 2 passed all 57 assertions. At minute 672 the effect remained present with duration 1 and `appliedAt` advanced to 672; Constitution +2 still raised max/current health from 39 to 43, and nine +5 modifier ticks combined with Food's nine -0.7 baseline ticks to reach 638.7. At minute 673 the last modifier tick produced Food 643, duration reached zero, the status was removed, and max/current health returned to 39. Minute 674 applied only the -0.7 baseline drift, producing Food 642.3 with the effect still absent. No chat-history row, duplicate removal, or status-summary entry was created. The first attempt compared the whole history response wrapper across a clock change; the corrected assertion compares the immutable history-entry array because the wrapper intentionally includes current world-time metadata.

`scenarios/need-bars/NEED-6-audience.json` first compares all three need endpoints before changing party/location state. Live-verify attempt 1 passed all 75 assertions. Baato reports exact audience `{player:true, party:false, nonParty:false}` and four unique Food/Rest/Stamina/Mana bars. QA Tired Companion reports party audience and exactly Food/Rest/Stamina, with its per-actor Mana applicability disabled. QA Courier reports non-party audience and exactly Stamina/Mana, with player/party-only Food and Rest absent. The scenario then dismisses/relocates both NPCs and caps player stamina before one explicit minor spell. The need prompt log shows empty `Characters at location` and `Characters in party` lists; Baato remains the implicit player actor through `playerAction`/`textToCheck` and the base player context. Planning and final XML contain exactly Baato's applicable small Mana decrease. Authoritative Mana moves from 600 through passive +10 to 510 after -100; no NPC need entry appears, and their only changes are passive elapsed-time rates. Human review accepted one spell, no NPC action, and no second player action.

`tests/fixtures/need_bar_tinybrain_invalid_outputs.json` and `tests/events_need_bar_tinybrain.test.js` cover NEED-7 without a model or server. The strict forced-output sequence drives the real `LLMClient` fixture path through one planning completion, six invalid character-phase completions, and one valid completion. The parser rejects an unknown bar, case-insensitive duplicate character, case-insensitive duplicate bar, invalid direction, invalid magnitude, and unexpected XML child with specific diagnostics. The staged runner retains the accepted planning response exactly once, removes each rejected assistant response before retrying, consumes all eight fixture outputs, and retries only the characters checkpoint. `tests/tiny_brain_prompt_parsers.test.js` independently verifies the same semantic rejection boundary.

`scenarios/need-bars/NEED-8-save-reload.json` covers full persistence after partial status elapse. State-only attempt 3 passed all 87 assertions. It sets distinctive raw Food/Rest/Stamina/Mana values, installs QA Well Fed, and advances three minutes to exact values 312.9/198.2/400/380 with duration 7, `appliedAt: 666`, Constitution +2, Food +5/minute, and derived health 43. After a normal save it deliberately overwrites every need, clears the status, and advances the clock before reloading. Reload restores the exact saved current values, their independent `initialValue` baselines, status/timestamps/modifiers, clock, inventory, derived health, and the persisted save notice. One subsequent minute produces 317.2/197.6/450/390, duration 6, and `appliedAt: 667` with no error. Attempt 1 corrected an overbroad assumption that save adds no history—the documented save notice is expected. Attempt 2 exposed a production hydration defect where `initialValue` was replaced by the current value; `Player.#initializeNeedBars` now preserves a serialized initial baseline separately from the current reading, with focused round-trip coverage.

## Long-time fixtures and TIME-1 through TIME-7

The immutable `long-time-rest-lodge` fixture supplies a visited warm lodge with a canonical twelve-minute reciprocal route, an exact normalized lodge keeper, two recorded incidental residents, the existing tired companion/courier actors, and one player-owned QA Short Tonic. Its ten-minute QA Brief Vigor status adds Luck +1 and starts from world minute 663 with exact Food/Rest/Stamina/Mana values 600/401/600/600. The executable manifest verifies the saved world and its save tree has SHA-256 `4b5a1bde3cccefc301da47fcccef21b19f3b9de7fe738f16c3fed9b0f25a93cc`.

The derivative `long-time-scheduled-events` fixture retains that baseline and adds two pending canonical records at minutes 693 and 708. Its setup passed 41/41 assertions without advancing time or narrating either event, and its save tree has SHA-256 `eb0f38ee04beb2936199ad6d5ceb61655b55a26f3fbb450413db5fda05c5a21e`. TIME-4 and later ordering cases can restore it without rebuilding schedules through the model.

`scenarios/long-time/TIME-1-multi-hour-rest.json` isolates all fixture NPCs, puts Baato inside QA Long Rest Lodge through a zero-time story-tool setup teleport, and caps stamina before the measured actions. Live-verify attempt 7 passed all 78 assertions and human review. Drinking one exact minor-stimulant tonic advanced one minute, consumed its only world instance, applied QA Brief Vigor once, and produced the configured small Rest recovery. Sleeping continuously from 11:04 AM to 7:04 PM then advanced exactly 480 minutes, filled Rest/Mana/Stamina, expired the status, and applied passive Food drain exactly once to the authoritative JavaScript result `263.29999999999995`. The player stayed in the lodge, the calendar remained Sunday, lighting changed to night, and exactly two player-action plus two time-summary entries were added without realtime or changed-log errors. Human review accepted one concise tonic action followed by one uninterrupted sleep with the correct 7:04 PM endpoint.

The rejected attempts were fixture/test corrections, not production defects. A tonic initially lacked an explicit configured need-trigger classification, so valid need analysis varied. The first sleep location was an exposed frozen marker, where a short failed doze plus Cold/Fatigued was plausible. Moving the isolated setup into the already-prepared safe lodge and stating the mechanically known endpoint removed those ambiguities. No prose matcher, domain-code workaround, or completion cassette was added.

`scenarios/long-time/TIME-2-midnight.json` covers the ordinary calendar rollover without a model. It first sets all active player needs to full, advances from fixture minute 663 to Sunday 11:58 PM, reads the persisted actor clock through `GET /api/npcs/:id`, then advances five minutes across midnight. State-only attempt 2 passed all 94 assertions. The canonical clock became day index 1/minute 3, the actor's serialized `elapsedTime` became 1443, and the client calendar changed to Monday January 2 at 12:03 AM. Winter remained active; lighting selected the deep-night seasonal description; New Year's Day and its description became null; Food/Rest reached exact passive values 454/532; Stamina/Mana remained capped at 1000; and location, statuses, schedules, calendar definition, and all history entries stayed unchanged. The fixture's persisted Clear weather state still had `nextChangeMinutes:2081`, so its complete state and client label remained unchanged across midnight. No prompts, realtime errors, changed logs, or cassette were involved.

The immutable `long-time-season-boundary` fixture derives from the lodge baseline entirely through normal region, time, and save APIs. It installs one deterministic 100000-minute Winter weather type and one distinct Spring type, then advances to Tuesday February 28 at 11:58 PM. Setup passed 66/66 assertions with the original Winter state intact at total minute 84958; its save tree has SHA-256 `74ab6abd916310b0f85bfe10d4a8ebbc357e94268abbab181e1fedebeebfe11e`.

`scenarios/long-time/TIME-3-season-boundary.json` restores that fixture and crosses into Spring by five minutes. State-only attempt 1 passed all 68 assertions: the actor clock advanced exactly to 84963; canonical client state became Wednesday March 1 at 12:03 AM; season description and lighting switched to their Spring definitions; and region weather switched from `QA Winter Still` to `QA Spring Rain`. The new weather state has exact duration 100000 and `nextChangeMinutes:184963`. A second calendar resolution preserved the whole state byte-for-byte, proving one recomputation rather than repeated rerolls. Location, statuses, schedules, calendar definition, and history remained unchanged, and there were no prompts or runtime errors.

`scenarios/long-time/TIME-4-scheduled-ordering.json` restores `long-time-scheduled-events`, advances sixty minutes across both due records, and advances one more minute to prove idempotency. Live-record attempt 6 and independent live-Qwen attempt 8 passed all 64 assertions and human review; strict no-llama replay attempt 7 consumed all 12 source-cassette completions in exact order with zero failures or leftovers. The current-location bell resolved first and added one hidden scheduled-event row followed by its sole visible prose row; the offscreen lodge ledger resolved second and added only its hidden row. Both records became resolved once at minute 723, the actor clock and passive needs advanced exactly once, and locations/things stayed unchanged. Human review confirmed that visible prose described only the bell and that both summaries introduced no character or object movement.

`scenarios/long-time/TIME-5-vehicle-arrival.json` restores `vehicle-underway-eastbound`, advances from minute 659 to one minute before ETA without model prose, then crosses ETA through an ordinary `/api/chat` action. Post-fix live-record attempt 7 and independent live-Qwen attempt 9 passed all 62 assertions and human review; strict no-llama replay attempt 8 consumed all 40 source-cassette completions in exact order with zero failures or leftovers. The turn advanced exactly to minute 669, returned `timeProgress.source: player_action` and a final location refresh, finalized the tram at QA East Platform once, preserved exit ID `exit_65` while retargeting its destination, kept the player, onboard actors, and carried case in place, and appended the Player Turn event summary before the Vehicle Movement summary. Advancing one further minute changed no arrival state or history.

Live attempt 6 demonstrated a model-path defect: the TinyBrain draft and movement checkpoint converted the vehicle's scheduled arrival into a structurally valid player disembarkation, which moved the player and let later event checks move NPCs. The player-action TinyBrain prompt now states both before drafting and at structured movement selection that vehicle travel/arrival does not itself disembark occupants, arrival or open doors still mean `NONE` when the player remains aboard, and explicit occupancy constraints must survive drafting. No prose scanner, regex, or hardcoded narrative classifier was added. The focused vehicle-arrival render regression and all follow-up harness/cassette tests pass. Independent verification also exercised the existing tool allowlist: undeclared teleport calls were rejected, logged, and retried without mutation. The strict v2 source cassette is `cassettes/long-time/TIME-5-vehicle-arrival.json` with SHA-256 `bfb9ddc3d404ffcf1c645ba88b6a4fc6ac2f397568d7ab365b99da156dea2e7b`.

`scenarios/long-time/TIME-6-wywa-age.json` restores the visited lodge at minute 663, removes the nearby companion from the party, relocates both otherwise eligible travelers, advances nineteen minutes, and then uses the canonical twelve-minute exit to return alone. Live-record attempt 4 and independent live-Qwen attempt 6 passed all 58 assertions and human review; strict no-llama replay attempt 5 consumed all 58 source-cassette completions in exact order with zero failures or leftovers. The player-action prompt calculated 31 minutes away at expected arrival, listed exactly `Mio, QA Lodge Keeper, Ruri`, supplied the existing lodge description, and explicitly fixed travel at twelve minutes without asking for an estimate. Its accepted bullets were incorporated into one destination passage. The authoritative move ended at lodge minute 694, added exactly one hidden `while-you-were-away` row with the same three names and 31-minute age, and added no `while-you-were-away-player` prose. This proves WYWA used the affirmative pre-arrival lodge snapshot even though arrival processing subsequently visited the destination. Earlier attempts corrected an omitted collection field by reading the direct location endpoint and removed mechanically eligible companion ambiguity through normal setup APIs; no production behavior or prose heuristic was added. Undeclared tool attempts remained contained by the existing allowlist and were retried without mutation. The strict v2 source cassette is `cassettes/long-time/TIME-6-wywa-age.json` with SHA-256 `d446c705e65586106efe570389a75c4835efaa80407c2582852bb732de6d759c`.

`scenarios/long-time/TIME-7-save-reload.json` saves the scheduled-event fixture on both sides of its first due minute. It installs QA Boundary Vigor for thirty-five minutes, advances to minute 692, story-tool visits the lodge and marker without time or arrival prompts so both exact `lastVisitedTime` values become 692, then writes the pre-boundary save. One ordinary minute reaches 693, reduces the status from six to five minutes, and resolves only the current-location bell before writing the post-boundary save. After deliberate status removal, lodge movement, and another minute, the scenario hydrates both saved names. Live-record attempt 2 and independent live-Qwen attempt 4 passed all 83 assertions and human review; strict no-llama replay attempt 3 consumed all 9 source-cassette completions with zero failures or leftovers. The first load restored world and actor minute 692, duration/appliedAt 6/692, both events pending, both visit timestamps 692, baseline placement, and exact history. The second restored minute 693, duration/appliedAt 5/693, the bell's resolved-at fields and nonempty summary/prose, the later lodge event still pending, unchanged visit timestamps, and exact post-boundary history. A transient router-switch socket reset is allowed only by the exact scheduled-resolution error-log prefix because the normal transport retry recovered without mutation. The strict XML parser also rejected one malformed applicability reply and retried only that checkpoint. No production defect or prose classifier was added. Long-time coverage is complete at 7/7. The strict v2 source cassette is `cassettes/long-time/TIME-7-save-reload.json` with SHA-256 `d286eae5ce038a878a46dfabdc087423d39f2b8fad37eebb3d6c42b1ecdc15e2`.

The run exposed three structured defects. The exact `scheduled_event_resolution` progress target did not cover TinyBrain resolution/interruption labels, so the default config now uses `scheduled_event_*`. The generic first-token `yes_no` parser could accept a rambling response whose final decision contradicted its first word, so scheduled applicability now requires one `<applicability>` root with exactly one `yes`/`no` decision and one nonempty reason. The dummy summary checkpoint could approve `N/A`, so a dedicated text-only `<summary>` parser rejects empty and exact null-sentinel values before local result assembly. A temporary anti-embellishment summary instruction was later removed during the prompt audit; summary fidelity remains human-reviewed, with no prose regex or classifier. `historyAddedSequence` provides the corresponding exact ordered structured-history assertion.

## Random/scheduled fixture and EVENT-1

The immutable `random-scheduled-events` fixture was created through generic `@@` tool calls and deterministic API normalization. It contains QA Bell Runner (`char_47`, alias `Bell`) and QA Brass Bell (`thing_347`) at March Start Marker, an expanded connected QA Offscreen Depot with neutral QA Depot Marker, exact pending scheduled records at minutes 665 and 666, and one exact location and region random seed. A state-only continuation passed 22/22 validation assertions; the persisted region seed was checked directly because compact region API projections intentionally omit `randomEvents`. The fixture manifest supplies executable invariants and its save tree has SHA-256 `914d915312e1d79348b7560541d527f4a613e8af119b4c9b4a0a82c0a1e7200c`.

`scenarios/random-scheduled-events/EVENT-1-random-current-location.json` restores that fixture under a profile that enables random dispatch, gives location-specific events 100% probability, and disables every other random category. One ordinary one-minute no-move turn consumes the sole location seed. Corrected live-record attempt 2, strict no-provider replay attempt 4, and independent live-Qwen attempt 5 each passed all 40 mechanical assertions; replay consumed all 51 cassette completions without provider traffic. Both accepted live narratives preserve QA Bell Runner as actor, QA Brass Bell as object, exactly one ring, and the explicit no-movement constraint. Authoritative state contains exactly one `random-event` response/history row with rarity `location`, world and actor time 664, an empty location pool, both scheduled events still pending, and no changes to locations, regions, things, player placement, or status effects.

The first live attempt passed every mechanical assertion but failed the required human review because another NPC effectively replaced QA Bell Runner as the seeded actor. A temporary rule that made named actors, objects, assignments, counts, and no-movement details rigidly authoritative throughout the prompt was later removed during the prompt audit. TinyBrain now asks the model to keep the seed recognizable while allowing narrative judgment. The failed prose remains recorded for later joint review; no regular expression, prose classifier, or application-code branch guesses whether narration complied. The strict v2 source cassette is `cassettes/random-scheduled-events/EVENT-1-random-current-location.json` with SHA-256 `422a49d0d22509a6c88d2c37bb6116023bb2b4ca03e57dbd0cad037b48e1a2af`.

EVENT-2 is split into `EVENT-2-party.json`, `EVENT-2-location.json`, and `EVENT-2-region.json` so every category starts from a genuinely isolated immutable restore. All three live-Qwen attempt-1 runs passed 33/33 assertions and human review. The file-backed party category selected a party-oriented seed and made QA Tired Companion the central participant without changing world time, party membership, spatial state, location/region/things, scheduled records, or the seeded location pool. Its precise `party.txt` seed is intentionally random and is not hardcoded into the scenario.

The location and region scenarios use their fixture's one-entry pools. The location result retained rarity `location`, followed the exact Bell Runner/Bell premise, emptied the visible location pool, and produced no second history row when forced again. The region result retained rarity `regional`, followed the exact distant blue signal-lamp premise, left the location pool untouched, and produced no second history row on a second region request. That second behavior is the API-level proof of region-pool consumption because compact region projections deliberately omit `randomEvents`. Both branches preserved time, entities, scheduled state, and player placement. No defect or source cassette was needed.

`scenarios/random-scheduled-events/EVENT-3-visible-scheduled.json` advances the same fixture from minute 663 to the local bell's exact due minute while Baato remains present. Live-Qwen attempt 1 passed 40/40 assertions plus human review. `sevent_1` resolved once at minute 665 with nonempty summary and player prose; `sevent_2` remained completely pending. Filtered history contains exactly a hidden `scheduled-event` row with `playerPresent:true` and `visibleProseSuppressed:false`, followed by one visible `scheduled-event-prose` row. The model retained QA Bell Runner, QA Brass Bell, and exactly one ring in both outputs and did not narrate the depot event. World/actor time advanced exactly two minutes while player placement, status, locations, regions, things, actor/object location, and the random-event pool remained unchanged. No production defect or cassette was needed.

`scenarios/random-scheduled-events/EVENT-4-offscreen-scheduled.json` moves Baato away from both scheduled locations without advancing time, snapshots the baseline, and advances three minutes across both due records. Live-record attempt 13 and independent live-Qwen attempt 15 passed all 55 assertions; strict replay attempt 14 also passed 55/55, consumed 11/11 source-cassette completions, and left no provider traffic or replay remainder. Both records resolved once at minute 666 with hidden bookkeeping and empty player prose. The bell made no mutation. QA Depot Marker retained its location, short description, cause effects, and every unrelated field while its `description` changed from the fixture baseline and contained `stripe` without requiring the model to reproduce benchmark-authored prose exactly. No separate visible scheduled-prose row appeared.

The failed EVENT-4 iterations found parser/domain boundaries rather than a reason to constrain narration. Offscreen applicability was first tied too closely to player presence. Direct replacement then went through `alterThing`, regenerating unrelated fields and inventing an effect. Later attempts exposed a neighboring `shortDescription` substitution, an abbreviated replacement value, and llama.cpp returning JSON-looking text instead of a native tool call even under required/forced selection. A later prompt audit removed the requirement to copy the authoritative event and its exact field/value contract. The current staged plan may run read-only lookups, then structurally validates its chosen direct targets, supported fields, JSON values, and other mutation calls. Calls remain pre-mutation validated against that accepted plan. A subsequent redundancy audit removed the model-owned change flag, duplicate id/name target fields, unused purpose prose, and the separate model tool-call round. The server now executes every accepted direct or richer call exactly and places its results into the retained conversation. Textual pseudo-tool output is never parsed as an executable fallback. The accepted cassette is `cassettes/random-scheduled-events/EVENT-4-offscreen-scheduled.json`, SHA-256 `22d66594e5b02df75059cea6ddd254ca8a26036175206c53bfa149d4efb465ca`.

`scenarios/random-scheduled-events/EVENT-5-long-action-interruption.json` restores the fixture at minute 663 and asks Baato to perform one continuous, stationary five-minute recitation. The action crosses the local bell at minute 665 and the offscreen depot update at minute 666. Fresh live-record attempt 14 and independent live-Qwen attempt 16 passed all 52 mechanical assertions; strict no-llama replay attempt 15 consumed all 40 v2 completions in order with zero failures, leftovers, or model-server availability. The local event is stored as one hidden scheduled-event child of the initiating request and folded into the sole player-action prose; there is no separate visible scheduled-event-prose row. The offscreen event is stored only as hidden bookkeeping and changes QA Depot Marker's `description` to the exact fixture value while retaining its short description and every tracked placement. World and actor time advance once from 663 to 668, all actors and objects retain their locations, unrelated state remains unchanged, and neither random-event nor duplicate schedule history appears.

Earlier EVENT-5 attempts exposed a structured checkpoint bug: a full `<turnResult>` response could be accepted where the interruption program requested prose only, then be inserted into CDATA and make the final XML malformed. Builder-bound prose drafts and revisions now pass through the semantic `player_action_required_prose` parser, which rejects XML markup and retries only that tiny-brain checkpoint; the result builder accepts the parsed prose value. This is parser enforcement of the requested checkpoint type, not generated-prose classification. Human review accepted both final live runs for event/action synchronization. Repeated Bell Runner stillness and an independent-run live-deslop substitution that produced `锈ed` are retained in `tmp/prose_issues_log.md` for joint review rather than converted into prompt rules. The strict source cassette is `cassettes/random-scheduled-events/EVENT-5-long-action-interruption.json`, SHA-256 `7e847cff5da49533c642564bbc59e7fa6cca340812fdcda383ffff6fc3bfb04c`.

`scenarios/random-scheduled-events/EVENT-6-skipped-event.json` preserves an audited model failure. It deletes both named entities required by the minute-665 Bell Runner/Bell event before taking its pre-due snapshot, then advances exactly two minutes. Attempts 1–3 returned successful API responses and retained correct time, deletion, unrelated-event, object, location, and log behavior, but all three resolved the impossible event instead of skipping it. Attempt 3's applicability prompt placed current scheduled-location NPC and thing lists directly beside the question; those lists contained neither required entity, yet the response still claimed the bell existed. That context experiment was reverted after it failed, rather than becoming permanent prompt weight.

The failure is left with the configured model rather than hidden by a generated-text entity matcher, an exact-name rule, repeated sampling until one answer passes, or an unapproved routing change. No wrong-output cassette is source-controlled. The scenario and attempts remain reproducible evidence for a future discussion about a stronger model or explicitly structured scheduled-event preconditions. The underlying lifecycle order is intact: staged final parsing precedes `markSkipped`, and the focused parser regression rejects any nonempty skipped `<scheduledEventResult>`. EVENT-6 is therefore audited but failed under the goal's alignment rule; work continues with EVENT-7.

`scenarios/random-scheduled-events/EVENT-7-tool-failure.json` isolates the backend invalid-target contract. It first resolves the earlier minute-665 bell, deletes QA Depot Marker, snapshots the surviving state, and crosses the marker event at minute 666. Source-cassette replay attempt 3 passed all 43 mechanical assertions and consumed all 8 strict completions without llama.cpp. The deterministic executor returned a structured `object_not_found` `<toolError>` on every attempt, TinyBrain rejected each failed invocation, and the slash command returned HTTP 500 after the configured seven checkpoint attempts. The process stayed alive and answered the following player request. Because `/time` advances before due-event resolution, world and actor time remained one minute later; the scheduled record itself remained `pending`, with empty summary/prose/resolution fields, no state mutation after the snapshot, and no scheduled-event history row.

The focused `scheduled_event_runtime` regression covers the mixed-plan idempotency boundary that a single missing target cannot show through final state. One successful direct update and one structured failure are executed twice through the same per-resolution cache: the successful mutation executes exactly once and is returned as cached on retry, while the failed call executes again and is never cached. The source cassette is `cassettes/random-scheduled-events/EVENT-7-tool-failure.json`, SHA-256 `9dab29f8f9c7d0f6ce3fd269f0fdc7d472d7d6b0f3166873e4a545d7bf34b995`.

Live attempt 1 remains separate diagnostic evidence rather than acceptance evidence. With the marker absent, the configured model asserted it still existed, planned an update to the `QA Offscreen Depot` location instead, replaced that location's full description, and marked the event resolved. This is the kind of future issue P-027 asked to retain after removing exact scheduled-event field/target rules. It is logged for joint review; no event-text/name matcher, exact-target prompt restriction, repeated sampling, or unapproved model-routing change was added merely to make the scenario pass.

`scenarios/random-scheduled-events/EVENT-8-save-reload-idempotency.json` resolves both fixture schedules offscreen at minute 666, saves that state, advances ten minutes, reloads the resolved save, and advances one additional minute. The initial live recording supplied six accepted scheduled-resolution completions and correct synchronized outcomes; its only failure was an over-broad reload assertion that compared hydration-refreshed NPC timestamps and derived history metadata byte-for-byte. The corrected scenario instead asserts the persisted schedule, marker, location, clock, history count, and absence of new schedule rows directly. Source-cassette replay attempt 6 passed all 64 assertions, consumed 6/6 completions with zero failures or leftovers, created no unexpected error logs, and ran without llama.cpp. Independent live-Qwen attempt 5 passed all 63 then-current assertions with no parser retries.

Both live summaries describe only the one-ring bell event and the depot-marker description change; both `playerProse` fields remain empty because Baato is away. The named save restores world and actor minute 666, both records resolved exactly once at minute 666, the exact altered marker description and placement, player placement, and unchanged schedule-history count. Neither the pre-reload ten-minute advance nor the post-reload one-minute advance launches another completion, adds scheduled-event or scheduled-event-prose history, or changes the resolved records or marker. Random/scheduled-event coverage is therefore fully audited at seven verified cases and one retained model failure. The strict v2 cassette is `cassettes/random-scheduled-events/EVENT-8-save-reload-idempotency.json`, SHA-256 `3c4a06bb51bcca4b685d2157e0c9cade37a8e6d0935afdb81558c051055e6e3a`.

## HIDE-1 automatic concealment failure

The immutable `stealth-hidden-npcs` fixture was produced by the required elevated generic setup path: one `@@` call created QA Veiled Scout and another created QA Loud Decoy, after which authoritative NPC updates fixed Scout's alias `Whisper`, hidden flag, and Dexterity/Stealth 20/20 and Decoy's visible flag and 1/0 hiding stats. The setup's only failed assertion used the wrong capture-projection field name (`currentLocation` instead of `locationId`); a state-only continuation retained the successful generated entities, passed 24/24 validation assertions, saved the state, and supplied the promotion evidence. The manifest verifies 17 runtime invariants and a recursive save-tree integrity hash.

`scenarios/stealth-hidden/HIDE-1-automatic-failure.json` uses the `stealth-hidden-mechanics` profile with `hide_hide_checks: true`. It lowers Baato to Wisdom/Perception 1/0, separates him with a story-tool teleport that intentionally bypasses arrival processing, then performs an ordinary zero-time return to the concealed Scout. State-only attempt 1 passed all 39 assertions: the first-shared-location machinery recorded exactly one automatic opposed major failure, retained `hiddenFromPlayer: true`, left last-seen fields unset, and added only one `check-results` entry carrying the canonical scout id/name plus `hiddenFromClient: true`. No player action, event summary, prompt, time change, realtime error, or changed error log occurred. This route emits no assistant prose, so narrative identity leakage is not mechanically guessed or scanned.

## HIDE-2 explicit search success

`scenarios/stealth-hidden/HIDE-2-explicit-search-success.json` restores the same fixture, isolates the visible decoy and unrelated courier, makes the player search mathematically favorable, and uses `<f>` with an injected 20 while targeting Scout by alias. The current scenario grades one authoritative `hiddenNpcChecks` resolution with the canonical actor/target and injected die, one check-results row, reveal state, location refresh, one player-action row, one event-summary row, unchanged player location, and clean realtime/error logs. It deliberately does not require `payload.toolInvocations` to contain exactly one entry: harmless lookups are transport details, while the server-owned contest and resulting state are the contract. Search duration is deliberately not fixed because the model may validly resolve the brief search as one or several minutes and HIDE-2 has no duration contract.

The first live attempts exposed that `api.js` collected the structured player-action check but the XML path in `Events.runEventChecks()` dropped it while delegating to `_runXmlEventChecks()`. After that handoff was fixed, further live variants showed valid tool results with omitted attributes and with Intelligence paired to Perception instead of the setting's default Wisdom. Hidden-event reuse now requires an exact actor, canonical opponent id/name or alias, supported opposed-check tool, and configured skill on each skill-configured side; the chosen attribute may be omitted or differ because generic checked actions permit valid skill/attribute pairing. Attribute-only setting sides still require the configured attribute. The result is consumed once across the XML before/move/after sections and never added to `hiddenNpcChecks`, preventing a duplicate roll and history row without inspecting prose.

Live-record attempt 7 passed 41/41 assertions and finalized the 38-entry strict v2 cassette at `cassettes/stealth-hidden/HIDE-2-explicit-search-success.json` (SHA-256 `0492b356b177f4acdec52498fc3d0f70f7ebce6a47d7432ff9d05fe169a7b0bc`). No-llama replay attempt 8 passed 41/41, consumed 38/38 entries with zero failures, and emitted no provider traffic. Independent live-Qwen attempt 9 also passed 41/41, recorded one forced critical-success Perception-vs-Stealth check, revealed the Scout once, refreshed the location, and passed human review for one search/reveal without movement, combat, item use, or a second action.

## HIDE-3 explicit search failure

`scenarios/stealth-hidden/HIDE-3-explicit-search-failure.json` restores the Scout's high Dexterity/Stealth values, lowers Baato's Wisdom/Perception, isolates unrelated NPCs, and submits one alias-targeted `<f>` search with die 1. It now grades the server-owned hidden contest rather than requiring a native tool call. The authoritative low roll must fail, add one check-results row, leave Scout hidden in place with null last-seen fields, omit a reveal refresh, and leave both player inventory and the world thing collection unchanged. Those last assertions specifically prevent the prior failure mode in which ungrounded prose created a parchment and moved/interacted with Scout after no check had resolved.

## HIDE-4 visible NPC hides

`scenarios/stealth-hidden/HIDE-4-visible-npc-hides.json` restores the stealth fixture, isolates Scout, and asks QA Loud Decoy to perform one promised hiding demonstration while Baato watches. Its `<f>` marker supplies Decoy's exact die 20. The current assertions read the authoritative `hiddenNpcChecks` result and require the canonical Decoy-vs-Baato Stealth/Perception identities, the injected actor die, one check-results row, one location refresh, one action/summary pair, and a living Decoy hidden at the unchanged location. They do not require a native check-tool invocation.

The current TinyBrain path no longer asks the model to plan a hidden contest and then repeat that known plan as a native tool call. Before prose, a dedicated `<hiddenContests>` checkpoint identifies only supported reveal/hide attempts. Its parser accepts exact local canonical names or aliases and semantically enforces player-versus-hidden-NPC reveal roles, visible-NPC-versus-player hide roles, current visibility, and uniqueness. Code resolves the configured opposed mechanic immediately using either an inline die override or the normal interactive forced-roll callback, then returns the result to subsequent prose stages. Event checking receives the same pre-resolved check; a matching planned contest also overrides a mistaken `useOpposedCheck:false` reveal instead of bypassing a failed roll. Read-only lookups remain available at the planning checkpoint, while skill-check tools are absent because the server already owns that resolution. If later prose generation nevertheless calls the semantically identical opposed-check tool, the tool loop returns that same authoritative result without rolling again or adding a second check-results row. Matching uses actor (when supplied), opponent, skill, and attribute identities and accepts canonical names and aliases; an omitted actor is accepted only when those mechanics identify exactly one planned contest. Non-TinyBrain event checking and unrelated player/NPC checks are unchanged, and no free-form prose classifier was added.

The Qwen 35B benchmark continuation first exposed two integration gaps: server-side pre-resolution used the inline override but not `<f>`'s interactive forced-roll callback, and the generic information-gathering checkpoint preceded the structured contest plan, allowing an early native check before an authoritative result existed. The resolver now uses the shared forced-roll callback, and general information gathering runs after hidden-contest resolution. Final attempt 6 passed HIDE-2 40/40, HIDE-3 43/43, and HIDE-4 42/42 with the requested actor dice 20/1/20, one check-results row per case, no native check-tool invocation, and no new error log. The continuation updated `airpgbench-Qwen3.6-35B-A3B-uncensored-heretic-Native-MTP-Preserved-GGUF.html` to 133 passed, 19 failed, and zero infrastructure errors across 152 scenario variants.

## HIDE-5 attack reveal

`scenarios/stealth-hidden/HIDE-5-attack-reveal.json` restores Scout as a living hidden actor, isolates the other fixture NPCs, and gives Scout a precommitted one-attack demonstration. Its `<1>` control and low attack stats guarantee a miss. Live-record attempt 5 and independent live-Qwen attempt 7 each passed 42/42 assertions and human review. Strict no-llama replay attempt 6 passed the same assertions and consumed all 39 cassette entries with zero failures. The one `resolveAttack` result carries canonical Scout/Baato identity, die 1, and `hit:false`; Baato remains at 39 health, Scout becomes visible without moving, and one location refresh plus one check/action/summary set is emitted without realtime or changed-log errors. Both accepted live narratives show one no-contact miss and leave Scout plainly visible at the marker.

Attempt 1 exposed a post-resolution defect when the model selected an attack that hit for zero damage. The domain resolver returned the authoritative target health, but the tool-result formatter looked only for a positive-damage application or incomplete summary percentages. It threw after reveal, so the model retried an otherwise resolved action. Hit formatting now also reads the authoritative `attackOutcome.target`, allowing `Damage: 0%` and the actual remaining-health percentage without another resolution. Single-target attack summaries also retain `defender.id` and `target.maxHealth`; miss normalization uses that canonical id instead of weakening assertions to name-only. Attempt 2 then proved the desired gameplay behavior while exposing the missing id. Attempt 4 mechanically passed after both fixes but failed human review because prose described light contact despite `hit:false`; its cassette is preserved under that attempt, and only prompt guidance was tightened before recording again. No prose matcher, classifier, or wording assertion was added. The accepted source cassette is `cassettes/stealth-hidden/HIDE-5-attack-reveal.json` with SHA-256 `827b157ccb7fbc30278527312a40ca3c5a815b3b2c350414a36687dfcb361e78`.

## HIDE-6 corpse visibility

`scenarios/stealth-hidden/HIDE-6-corpse-visibility.json` restores the living hidden Scout and tests the death/visibility invariant without involving prose generation. In one authoritative NPC update it requests both `isDead=true` and `hiddenFromPlayer=true`; state-only attempt 1 passed all 59 fixture and scenario assertions. The domain model made the Scout dead at zero health, cleared concealment, started the ordinary five-turn corpse countdown, retained location membership, and exposed exactly one `hiddenFromPlayer:false` corpse through the location client payload. A second update requesting concealment could not re-hide the corpse, and a full NPC-status read confirmed the same state. World time, last-seen fields, history, realtime errors, and changed error logs remained untouched. This state-only case found no production defect and requires no cassette.

## HIDE-7 hidden NPC movement

`scenarios/stealth-hidden/HIDE-7-hidden-movement.json` moves the same hidden Scout from March Start Marker to the existing Stairwell through `npcDeparture`, then back through `npcArrival`. Live-record attempt 10 and independent live-Qwen attempt 12 passed all 76 assertions. Strict replay attempt 11 consumed the source cassette's 19/19 completions in global order with no prompt-progress or provider traffic. Both legs preserve the actor id, hidden flag, location membership, and null last-seen fields while reporting the canonical actor name. The scenario then uses a story-tool separation plus normal player return so ordinary arrival processing records exactly one failed automatic reveal check with `hiddenFromClient:true`; Scout remains hidden.

The first live attempts exposed two production gaps. `SanitizedStringSet` stores normalized lowercase keys, and both event result builders returned those keys directly in `npcUpdates.added`/`departed`. They now resolve tracked values back to authoritative actor names, with a focused parser/result-builder regression. Separately, the character-presence checkpoint could return `<done/>` even when prose physically placed an actor absent from current membership into the scene. Its structured instruction now explicitly compares current membership with physical presence, requires `npcArrival` for visible or hidden arrivals, and excludes plans, memories, dialogue mentions, and offscreen activity. No application code scans narration. The strict v2 cassette is `cassettes/stealth-hidden/HIDE-7-hidden-movement.json` with SHA-256 `b8bc8ad5270ea9f65b73c70a64bbb1c0f34dc6d4d868a684c2a01a69130205b3`.

## HIDE-8 hidden-state persistence

`scenarios/stealth-hidden/HIDE-8-save-reload.json` establishes meaningful pre-save sighting asymmetry without direct private-field mutation. A story-tool separation followed by ordinary player arrival leaves the living hidden Scout unseen with null `last_seen_time`/`last_seen_location`, while visible Decoy records minute 663 at March Start Marker. After saving, the scenario reveals Scout, moves both NPCs to Stairwell, advances five minutes, and performs a normal player arrival so both disposable actors acquire contradictory minute-668 Stairwell sightings. The reload must therefore reverse real mutations rather than merely reproduce fixture defaults.

State-only attempt 3 passed all 90 assertions with no model prompt, cassette, realtime error, or changed error log. Hydration restored Scout's hidden flag, alias, null sighting fields, and March Start Marker membership; restored Decoy's visible flag and exact minute-663/location sighting; returned the player to the baseline and clock to minute 663; removed both NPC ids/profiles from Stairwell; and preserved the saved history including its one save notice. The full actor reads and current-location client payload each expose exactly one Scout with `hiddenFromPlayer:true` and one Decoy with `hiddenFromPlayer:false`. That is the backend contract consumed by the Adventure UI's living-hidden-NPC filter: trusted location payloads deliberately include hidden records and the browser suppresses them by default. UI rendering remains outside this backend-only goal. No production defect was found, and stealth/hidden coverage is complete at 8/8.

## REL-1 directed relationship

The immutable `relationships-factions` fixture is an independent copy derived from `fast-travel-visited-favorites`; the source fixture remains byte-for-byte unchanged. Its own setup scenario creates deterministic QA Lantern Guild and QA Cinder Guild records, neutral reciprocal faction relations, zero player standings, QA Lantern Witness and QA Cinder Witness together at March Start Marker, and QA Private Contact offscreen. The copied save's stale `lastHousekeepingTurnId` was removed because its referenced turn had already been pruned before the source fixture was promoted; only this derivative copy and its manifest/integrity hash were changed. The resulting fixture passed 21/21 setup assertions and has save-tree SHA-256 `a1ca40e1ed52ed88401b403fa38075bbd273dd13c5bc3341a70ddf5bc5de69c2`.

`scenarios/relationships-factions/REL-1-directed-relationship.json` restores that fixture, adds an independent trust commitment to Lantern's setup notes without prescribing the relationship wording, and takes one normal one-minute action. Live-record attempt 5 and independent live-Qwen attempt 7 each passed all 40 assertions and human review. Strict no-llama replay attempt 6 consumed all 37 globally ordered completions with zero failures or leftovers. The recording produced Lantern → Cinder `places trust in`; independent Qwen produced Lantern → Cinder `trusts`. In both cases Cinder's reverse map remained empty, both dispositions stayed zero, and locations, factions, schedules, and actor placement were unchanged. History gained exactly one player-action and one relationship-update entry with clean realtime and changed-log checks. The test therefore enforces the directed data contract while preserving model choice of a natural short relationship label. The strict source cassette is `cassettes/relationships-factions/REL-1-directed-relationship.json`, SHA-256 `71a21bd1df94a8c12bbb7a24b93cf9d5696a69157167f73d147397f4a159d91e`.

## REL-2 reciprocal relationship

`scenarios/relationships-factions/REL-2-reciprocal-relationship.json` restores the same immutable source fixture afresh; it does not alter REL-1's scenario, cassette, or setup. One ordinary turn lets Lantern and Cinder independently choose different relationship labels, and a second turn asks Lantern to revise only her own view while Cinder explicitly retains hers. Assertions bind the second turn to the first turn's actual model-selected values rather than prescribing exact labels. Live-record attempt 3 and independent live-Qwen attempt 6 each passed all 45 assertions and human review. Strict no-llama replay attempt 4 consumed all 75 globally ordered completions with zero failures or leftovers. The recording changed Lantern → Cinder from `Reliable partner` to `Trusted partner, deeper bond` while retaining Cinder → Lantern `Steady hand, trusted`; independent Qwen changed `Partner (real)` to `Partner (trusted)` while retaining `Partner (not friend)`. In both paths dispositions, factions, placement, schedules, and unrelated tracked state remained unchanged; time advanced exactly two minutes and history gained exactly two player actions plus two relationship-update entries. The strict source cassette is `cassettes/relationships-factions/REL-2-reciprocal-relationship.json`, SHA-256 `e890cfc5fc47f3d079b6b2caa09ec26dd18bf13265116e2e50689583cc50ec83`.

The first live attempt exposed a general housekeeping boundary defect: malformed relationship XML failed strict parsing inside the event layer, but the deferred event-check wrapper logged the failure and still completed the player turn without applying bookkeeping. Housekeeping finalization now uses semantic parse retries around the already-deferred response. Each rejected response is prompt-logged with the exact parser error, cannot mutate state or advance the housekeeping history boundary, and is retried locally until one complete `<housekeeping>` document passes. Relationship parsing also rejects unknown or duplicate child tags, missing character/label fields, labels over six words, and labels on removals. The independent run exercised two consecutive malformed replies before accepting the third, confirming that invalid intermediate output does not leak into state or the final turn response.

An interrupted live recording exposed a separate restart lifecycle edge: the reset endpoint correctly replaced the incomplete v2 cassette with an empty document, but a newly started process still refused to claim that existing path. Recording startup may now claim only a validated strict v2 cassette that is incomplete and has zero entries. Completed, nonempty, malformed, or legacy destinations remain protected from overwrite. This keeps failed attempts recoverable across server restarts without weakening accepted cassette evidence.

## REL-3 player disposition

`scenarios/relationships-factions/REL-3-player-disposition.json` restores the same immutable source fixture into a new disposable runtime copy; it does not edit the REL-1/REL-2 scenarios, cassettes, or fixture. Case-local API setup moves Cinder away, supplies Lantern's favor context, and replaces Lantern's goals through the dedicated goals endpoint. Baato then gives one modest minute of practical help holding Lantern's existing satchel steady, leaves her at March Start Marker, and walks alone over the already-existing nine-minute route to the already-visited QA Fast Travel Tower.

Live-record attempt 6 and independent live-Qwen attempt 9 each passed all 49 assertions and human review. Strict no-llama replay attempt 8 passed the same assertions and consumed all 79 globally ordered completions with zero failures or leftovers. The recording selected one `platonic` first-impression change; independent Qwen selected distinct `platonic`, `respect`, and `trust` changes. The scenario does not force an exact number or type. It requires at least one mechanically positive change and exactly one structured summary per changed type whose previous value, new value, delta, and nonempty reason match authoritative NPC state. Cinder and the private contact remain unchanged, Lantern remains at the origin, the player reaches the tower without a companion, total elapsed time is exactly ten minutes, and relationships, factions, schedules, and unrelated tracked state remain stable. The strict source cassette is `cassettes/relationships-factions/REL-3-player-disposition.json`, SHA-256 `8a938ba97faab3a79f77382bae042e2880340dcaf8d9f676f37742ce50de68a9`.

This case added two general harness synchronization contracts. `waitForRequest` polls a declared authoritative API projection until mechanical assertions pass, avoiding guessed post-prompt sleeps. `dispositionChangesMatchState` accepts both save/detail-shaped `dispositions[playerId]` and the all-player client projection `dispositionsTowardPlayer`. Strict replay prompt-idle waits do not require observing a fleeting active websocket frame, because cassette completions can finish between progress snapshots; full ordered cassette consumption and the authoritative outcome remain mandatory. The recording's harmless bare `[Storyteller]` suffix is preserved in `tmp/prose_issues_log.md` instead of being converted into a prose heuristic or prompt micro-rule.

Earlier attempts exposed a production parser defect rather than a scenario defect. Malformed generated region XML had been normalized far enough to produce partial location definitions, so expansion reached later instantiation checks or returned a generic failure. Pending-region generation now strictly validates the complete `<region>` document and every consumed component before mutation, using the configured semantic retry budget. A first retry implementation retained the roughly 10 KB rejected document; live attempt 3 demonstrated that the local model copied the same malformed XML byte-for-byte seven times. `runPromptWithParseRetries()` now supports omitting rejected output from retry context. Region retries use that mode and regenerate from the original prompt plus the exact parser error, while smaller structured-generation callers retain their established correction context.

## REL-5 no faction witness

`scenarios/relationships-factions/REL-5-no-faction-witness.json` is a separate case-owned copy of the closest relationship/faction scenario structure. It shares the immutable `relationships-factions` source fixture and the purpose-built `faction-reputation-mechanics` profile, but never edits the source fixture or any REL-1 through REL-4 scenario, cassette, or profile. The harness verifies the source hash, copies it into a fresh disposable runtime save, then applies only REL-5's setup: both Lantern members are placed at the offscreen tower, the Cinder member remains with Baato, and Baato privately writes the same complete route-safety report without delivering it to or informing any Lantern representative.

Live-record attempt 1 and independent live-Qwen attempt 3 passed all 43 assertions plus human review. Strict no-llama replay attempt 2 passed the same assertions and consumed all 36 globally ordered cassette entries with no provider traffic, failure, or leftover. Both live narrations kept the report private; the independent prose allowed the colocated unrelated Cinder witness to observe Baato writing, which is coherent and does not qualify as a Lantern witness. The authoritative sparse standing map remained exactly `{ faction_1: 5 }`, no structured faction-reputation summary appeared, world and actor time advanced exactly five minutes, every actor remained at the intended location, and relationships, dispositions, faction definitions, locations, things, and schedules stayed unchanged. History gained one player action and no relationship-update entry, and realtime/error-log assertions stayed clean.

`tests/events.faction_reputation.test.js` covers the underlying witness boundary without model judgment: the handler applies a `+1` Lantern delta when a living Lantern member is colocated, and applies no standing or emitted change when only the unrelated Cinder member is present. The strict source cassette is `cassettes/relationships-factions/REL-5-no-faction-witness.json`, SHA-256 `fd3245767a49f8a38fcc84421ca56f7ee156d087035224d457728bbb532ec09a`.

## REL-6 hostility transition

`scenarios/relationships-factions/REL-6-hostility-transition.json` is a separate REL-6-owned scenario and uses its own `hostility-transition-mechanics` profile. It reuses the immutable relationship fixture only through the normal integrity-checked disposable runtime copy; no REL-1 through REL-5 scenario, profile, cassette, or source save is edited. Because the exact threshold and expected state are already code-owned, this case is state-only and does not ask an LLM to repeat them.

Attempt 5 passed all 48 assertions with zero completions. QA Lantern Witness began with raw `isHostile:false`, `platonic:0`, and derived `isHostileToPlayer:false`. Setting only `platonic` to the exact configured `-31` boundary left the raw flag false but changed the `/api/players` client projection to hostile; the real `/api/npcs/:id/trade/session` gate then rejected the otherwise present, living, willing merchant with HTTP 409 before any pricing prompt. Returning the value to `0` immediately restored the derived projection. All asserted disposition, relationship, placement, faction, object, schedule, calendar, history, and player-location semantics returned to their exact baseline. `lastUpdated` is deliberately not a byte-for-byte invariant because both legitimate edits stamp it.

The defect was combat-slot classification: it still read `npc.isHostile` while serialization and barter independently recomputed disposition hostility. `DispositionHostility.isActorDispositionHostile(...)` is now the single shared threshold classifier used by all three paths. The friendly-transition event handler uses it as well, so a disposition-hostile/raw-friendly actor can be reconciled rather than silently skipped. `tests/disposition_hostility.test.js`, `tests/events.hostile_to_friendly.test.js`, and the focused API wiring tests cover exact/above thresholds, stale flags in both directions, invalid contracts, serialization, barter, combat selection, and recovery. Direct API collection responses are also now valid `entityField` assertion sources, which keeps scenario assertions ID-based rather than relying on array position.

## REL-7 housekeeping relationship lifecycle

REL-7 reuses evidence rather than reconstructing entities or spending another three live runs. REL-1 and REL-2 already ran automatic housekeeping through live record, strict replay, and independent Qwen and retained authoritative evidence for adding and updating real directed relationship maps plus visible `relationship-updates` history. Neither those cases nor the immutable fixture was changed.

`tests/rel7.housekeeping_relationship_lifecycle.test.js` is the new case-owned missing boundary. It uses a copied, isolated fake-character runtime and passes one `<housekeeping>` document containing add, update, and remove entries through `Events._parseHousekeepingXmlResponse()`, `_applyHousekeepingXmlResponse()`, the real batch `setRelationship` tool with parser-only removal authority, `buildHousekeepingUpdateLogEntries()`, and `shouldIncludeEntryInBaseContextHistory()`. The edge changes from absent to `cautious colleague`, then `trusted colleague`, then absent; action metadata and visible log rows are exactly `added`, `updated`, and `deleted`; the reverse edge remains absent; and the log entry is excluded from normal and all-entry prompt history. The existing parser, tool, tracker/relationship-log, and history-filter tests provide adjacent syntax, error, reciprocal, hidden-tracker, and batch coverage. Since these results are deterministic and the live add/update boundaries were already proven, REL-7 requires no new model cassette.

## REL-8 relationship/faction save and reload

`scenarios/relationships-factions/REL-8-save-reload.json` and `relationship-persistence-mechanics.json` are new REL-8-owned assets. The scenario reuses the canonical relationship fixture only by asking the harness to validate its hash and create a disposable runtime copy; it does not edit the fixture source, its manifest, or any earlier case's scenario, profile, or cassette. Because every expected value is code-owned, the case runs state-only and asks no model to repeat known facts.

The case uses deterministic APIs to establish Lantern → Cinder `trusted rival`, Cinder → Lantern `guarded ally`, opposite faction memberships, Lantern standing `7`, and Lantern dispositions `platonic:-31`/`trust:12`. Raw hostility remains false while the shared disposition classifier reports derived hostility. It saves this exact state, then proves the reload is meaningful by deliberately replacing both directed maps, swapping memberships, zeroing dispositions, and changing the standing to `-9` before loading the save. Attempt 3 restored all original semantic values, actor placement/count, factions, locations, things, schedules, clock, and history with no completion calls or runtime errors. The comparison intentionally ignores `lastUpdated`, which hydration restamps as operational metadata, and instead asserts the complete meaningful relationship and disposition maps explicitly.

The general `PUT /api/npcs/:id` route now accepts an optional complete `relationships` object. Before applying the map it validates that the value is a plain object and that every target is a real character; `Player.setRelationships()` continues to enforce self-target and label semantics. This supports deterministic editing and ordinary API consumers without adding a test-only mutation path.

## PROG-1 player level-up and pending selection

`scenarios/progression/PROG-1-level-up.json` reuses the immutable `fast-travel-visited-favorites` source fixture through the harness's verified disposable-copy path. No player or trainer was recreated: the existing level-3 Baato already has three abilities assigned at each level 1–3, and the same fixture includes NPCs suitable for later progression separation coverage. The new `progression-mechanics` profile is family-scoped so the remaining progression cases can share it without changing another case's profile.

State-only attempt 2 passed 56 assertions with no prompt-progress event or changed error log. The real level-up endpoint moved Baato from level 3 to 4, reset health from `39` to the recalculated maximum `45`, retained all nine existing ability objects exactly, and appended one `level-up` history row whose metadata records `char_2`, previous level 3, new level 4, and base-context exclusion. Pending selection identifies only level 4, requires three choices, and reports all six option cards still ungenerated with empty options and preselection. Reading the selection endpoint again with `generateOptions=false` returns the identical draft without calling the model. Attempt 1 failed only because the new scenario expected the private persisted draft map in the public player projection; removing that redundant assertion changed no production or prior test asset.

## Reusable level-4 pending progression fixture and PROG-2 gates

`PROG-fixture-level4-pending.json` copied `fast-travel-visited-favorites` into a disposable runtime, called the real player level-up endpoint once, verified 39 setup invariants with no completion, saved the derivative, and promoted that save as `progression-level4-pending` with SHA-256 `0ff8d3ce00655e3e2c501c581bad8108cbd3c0a212dc22e151c6bfb7ed4878c4`. The source fixture was not changed. The derivative retains existing QA Courier and Mina, avoiding another NPC-creation pass for PROG-9, and supplies the common level-4/no-cards-yet starting state needed by PROG-2 through PROG-5 and PROG-11. Its first runtime validation established that the hydrated public experience value is `15` although the raw historical save value is fractional, and that private declined-option storage is intentionally absent from client serialization; only the new manifest was corrected.

`scenarios/progression/PROG-2-gameplay-gate.json` uses a fresh disposable copy of that derivative. State-only attempt 3 passed both rejection paths: chat returned `409` before a turn began, movement returned `409` before acquiring/mutating movement state, and both included the identical pending level-4 selection. The complete tracked state was unchanged after chat and again after movement, with no player-action row, prompt-progress activity, realtime error, or changed error log. Attempts 1–2 stopped at the new fixture's invalid public-projection invariants and never executed either behavioral request.

## PROG-3 generated ability cards and reusable options fixture

`scenarios/progression/PROG-3-generate-cards.json` uses a fresh disposable `progression-level4-pending` runtime and exercises the complete generation/cache/exclusion sequence. Corrected live-record attempt 2, strict no-llama replay attempt 3, and independent live-Qwen attempt 4 each passed 80/80 assertions. Replay consumed the complete four-entry source cassette exactly with no provider traffic, failure, queued work, or unused response. The first request produced six unique level-4 cards at the correct level with no exact current-name reuse; the second request returned the byte-identical cached selection without another prompt. After the first three were selected and the other three became declined, level 5 produced six further unique cards with no exact current, selected, or declined name reuse. Only the explicit selection/level-up state changed; unrelated world state, time, player-action history, and actors stayed unchanged.

Human review found repeated semantic near-duplicates across independent generations despite the correct exact-name mechanics. Those complete examples are logged as `PROSE-005` in `tmp/prose_issues_log.md`; no prompt restriction, semantic keyword check, prose classifier, or unapproved routing change was added. The strict v2 cassette is `cassettes/progression/PROG-3-generate-cards.json` with SHA-256 `86eb983f0630e7883cc05339ac3c05d469a803abb1271318bab0dfd7741bb480`.

The pre-submission save from corrected attempt 2 was promoted by copy as the new immutable `progression-level4-options` fixture with SHA-256 `1242976bccdc2e1f83ee8b0509989698167bc3228ee83a7de869cddca3e61875`. Its manifest records the exact six cached option names. PROG-4 and PROG-5 can each use their own disposable copy without making another model request or editing `progression-level4-pending`; the older pending fixture still hashes to `0ff8d3ce00655e3e2c501c581bad8108cbd3c0a212dc22e151c6bfb7ed4878c4`.

## PROG-4 valid submission and PROG-5 atomic rejection

`scenarios/progression/PROG-4-submit.json` restores a disposable `progression-level4-options` copy and calls the ordinary selection route with exactly three known cached cards. State-only attempt 2 passed 56/56 assertions: the selected cards were assigned with their complete card records at level 4, the draft cleared, no level remained missing, and no deferred intro or prompt ran. The request also submitted all six names as declined to exercise selected-name precedence. A newly generated save proved only the three unselected cards were persisted in `declinedAbilities`, the selected cards were absent there, and the pending-options map was empty. Attempt 1 corrected only the new case's invalid expectation that the aggregate player array would stay unchanged while the current player's abilities intentionally changed.

`scenarios/progression/PROG-5-invalid-submissions.json` begins from a separate disposable copy. State-only attempt 1 passed 47/47 assertions. Wrong level, too few choices, too many choices, a duplicate selected name, an unknown selected name, and a duplicate declined name each produced the documented exact `400` contract and an immediately unchanged complete state snapshot. A final read and generated save prove the original six-card draft remained exact and no decline was persisted. Neither case contacted a model or changed an existing case asset.

These cases use the general additive `readSavedJson` scenario step to inspect one path-safe `.json` file from a save produced by the same run. This keeps private persistence validation out of production gameplay APIs. The step rejects path traversal and unknown save types, and focused harness coverage exercises both successful capture and rejection.

## PROG-6 partial preselection

`scenarios/progression/PROG-6-partial-preselection.json` restores the separately named immutable `progression-level4-preselected` fixture. That fixture was created by copying the closest pending-level starting state, adding one level-4 `Prepared Catalyst` card only to the derivative setup copy, validating the partial draft, and promoting that copy. It was not made by altering `progression-level4-pending`, `progression-level4-options`, or another case's scenario/cassette. Their hashes remain valid; the new fixture's save-tree SHA-256 is `92777e154667a88ea9ec25e7f83f2b18bd23b12ee643ad316b965a693b388ce9`.

Live-record attempt 1, strict no-llama replay attempt 2, and independent live-Qwen attempt 3 each passed 50/50 assertions. Both live requests generated exactly the five missing cards, retained `Prepared Catalyst` at index zero as the exact sole preselection, returned six unique level-4 options, and served the identical cached selection on the second read. The submit replaced the preselected card with options one through three. Public state then contained exactly those three level-4 cards and no `Prepared Catalyst`; a newly written raw save contained exactly three declines including `Prepared Catalyst` and an empty private pending map. Time, location, prior abilities, experience, inventory, skills, status, factions, schedules, world objects, unrelated actors, and history remained unchanged except for the explicit save notice. Replay consumed the complete one-entry strict v2 cassette with zero failures or leftovers; its SHA-256 is `ec809cc00482b810d69977d0843ae04eb115552c276ef323bce694eeb55f5dfc`.

Human review found semantic overlap with existing cards in both live generations. The complete examples and context are appended to `PROSE-005` in `tmp/prose_issues_log.md`. This did not become a prompt prohibition, semantic keyword assertion, prose classifier, heuristic, or routing change.

## PROG-7 sequential missing levels and atomic next-level generation

`scenarios/progression/PROG-fixture-level6-multiple-gaps.json` creates a PROG-7-owned starting point without changing any prior case asset. It loads a disposable copy of `progression-level4-options`, reuses that fixture's cached level-4 draft, applies two ordinary state-only level-ups, validates 49/49 setup assertions, and saves/promotes only the derivative as `progression-level6-multiple-gaps`. Levels 1–3 already have the required three cards, levels 4–6 are missing, and the derivative save-tree SHA-256 is `09e38bdb3db1faebe4bdcd1de03098620d749bca5c812aee230ade812fd2c1f6`. The source cached-options fixture still validates at `1242976bccdc2e1f83ee8b0509989698167bc3228ee83a7de869cddca3e61875`.

`scenarios/progression/PROG-7-multiple-missing-levels.json` fills those gaps in sequence. Corrected live-record attempt 2, strict no-llama replay attempt 3, and independent live-Qwen attempt 4 each passed 99/99 assertions. The existing level-4 cards required no completion. Submitting level 4 generated level 5 while retaining a movement gate; submitting level 5 generated level 6 while retaining a chat gate; submitting level 6 returned no missing levels, and the same ordinary move then succeeded. All sufficient levels remained untouched, each completed level contains exactly three cards, no player-action history was introduced by the blocked requests, and unrelated state stayed exact. Replay consumed its complete three-entry strict v2 cassette with zero failures or leftovers; cassette SHA-256 is `194b6fa3206644b1b853c88bce49f753af4f44d91b8f4f2d849a67e5376a0591`.

Live-record attempt 1 exposed a real general defect. Level-6 generation yielded only five nonexcluded cards because later responses repeated excluded names, and the hard-coded three-attempt loop returned HTTP 400 after the level-5 submission had already mutated abilities and declines. Player card generation now uses `resolveConfiguredPromptMaxAttempts(config.ai)` rather than a magic `3`. Ability submission also snapshots current abilities, declined cards, and every pending draft; if automatic generation of the next missing level fails, it restores that snapshot before propagating the error. A focused executable regression invokes the real selection function, forces the next-level resolution to throw, and verifies complete semantic rollback. The independent live run needed all seven configured attempts to accumulate level-5 options and then passed, directly exercising the expanded budget.

Both live runs continued to produce semantically overlapping card ideas. Their full structured output and context are appended to `PROSE-005`; no semantic keyword rule, prose classifier, routing change, or test-specific prompt restriction was added.

## PROG-8 formula-derived skill spending

`scenarios/progression/PROG-8-skill-spend.json` is a new case-owned state-only scenario. It references the existing immutable `progression-level4-pending` fixture, which the harness integrity-checks and copies to a disposable runtime directory before load. It does not modify the source fixture or any earlier progression scenario, cassette, or profile. Because the restored level-4 player already exposes eight formula-derived unspent skill points, the case reuses that known starting point instead of creating another trainer, applying another level-up, or promoting a redundant derivative fixture. The source fixture still validates at `sha256:0ff8d3ce00655e3e2c501c581bad8108cbd3c0a212dc22e151c6bfb7ed4878c4`.

State-only attempt 2 passed 46/46 assertions. The real skill endpoint raised Alchemy from `5` to `6` and the live serialized point pool from `8` to `7`; exact before/after deltas prove the result came from current formula evaluation rather than trusting a mutable stored counter. Level, XP, attributes, abilities, declines, pending drafts, inventory/world state, factions, schedules, placement, time, and history remained unchanged. A nonexistent `QA Unknown Skill` request returned `400 Unknown skill: QA Unknown Skill`; a subsequent request for eight points returned `400 Not enough unspent skill points` because only seven remained. The complete tracked state equaled the post-success snapshot after both failures. There was no completion request, prompt-progress entry, cassette, changed log, or realtime error.

## PROG-9 NPC progression remains separate from player selection

`scenarios/progression/PROG-9-npc-separation.json` is a new PROG-9-owned scenario. It reuses QA Courier and Mina from the immutable `fast-travel-visited-favorites` fixture only through the harness's integrity-checked disposable-copy path; it does not edit that fixture or any earlier progression scenario, profile, or cassette. This avoids generating redundant NPCs while keeping every mutation local to the new case's runtime copy.

Live-record attempt 3, strict no-llama replay attempt 4, and independent live-Qwen attempt 5 each passed 61/61 assertions. Raising QA Courier from level `2 → 3` preserved XP and the exact four prior abilities, added two case-sensitive unique level-3 abilities, changed skills and attributes, and spent both formula-derived point pools to zero. Baato's exact no-pending ability-selection payload remained unchanged before and after the NPC work; Mina and all tracked location, time, faction, schedule, object, and player history state also remained exact. One base-context-excluded level-up row named QA Courier, and no player-action row appeared.

Replay consumed the complete one-entry strict v2 cassette with no provider traffic, failure, or remainder. The accepted live runs each needed one completion with zero retries and no unexpected error log. Earlier attempts remain preserved: one read authoritative NPC state too early after relying on transient prompt-idle, and one encountered an avoidable router-switch socket reset before its normal retry. The final case polls the NPC API until the code-owned progression invariants hold, while its temporary run profiles preload the model already assigned to `levelup_abilities`; no production model-routing assignment changed. Human review found conceptual overlap between some generated abilities and the courier's existing set. The verbatim examples are logged under `PROSE-005` rather than converted into a prompt restriction or prose heuristic. The source cassette SHA-256 is `ad125c601b3ec29b4ef018e3696c9686abc3eb7c618507c1cb69ca7528664758`.

## PROG-11 save/reload of completed, declined, and pending abilities

`scenarios/progression/PROG-11-save-reload.json` is a separate PROG-11-owned scenario built from copies of the proven PROG-3/PROG-4 and save/reload patterns. It restores `progression-level4-options` through the normal integrity-checked disposable runtime path; the fixture, prior progression scenarios, their cassettes, and shared profiles remain unchanged. Starting with that cached draft avoids regenerating level-4 cards. The case selects its first three cards, records the other three as declines, raises Baato once, generates the level-5 draft, and saves the resulting combination of completed selections, declines, and pending options.

The test then deliberately consumes the level-5 draft in runtime, proving the live state differs, and reloads the earlier named save. Live-record attempt 2, strict no-llama replay attempt 3, and independent live-Qwen attempt 4 each passed 72/72 assertions. Public non-generating and default reads returned the exact pre-save pending payload and 12-card completed list. A second raw save after hydration matched the original raw save exactly for player level, complete abilities, the three private level-4 declined cards, and the complete six-card `pendingAbilityOptionsByLevel.5` array. Location, inventory, attributes, skills, status, things, factions, schedules, time, and saved history also returned to the saved snapshot.

Replay consumed the complete one-entry strict v2 cassette without provider traffic, failure, or remainder. The recording needed one accepted completion. Independent Qwen needed three configured accumulation attempts to collect six parser-valid cards, all within the initial generation step; its post-reload cached reads took 79 ms and 63 ms and launched no completion. Semantic card overlap—including exact reuse of several QA Courier ability concepts in the independent set—is preserved verbatim under `PROSE-005`; it did not become a persistence-test exception, a prompt micro-rule, a prose heuristic, or a routing change. The source fixture remains at `sha256:1242976bccdc2e1f83ee8b0509989698167bc3228ee83a7de869cddca3e61875`; the strict cassette SHA-256 is `36bcbd938874a730b15db41b54041fb58982d0b94da665c82ae349c42c30c5cb`.

## ALT-1 question dispatch and isolation

`scenarios/alternative-prompts/ALT-1-question.json` and the new `alternative-prompts-mechanics`/replay profiles begin the final prompt-mode family. Rather than creating another witness and token, ALT-1 reuses QA Lantern Witness and her existing `Whistle of the Ridge` from the immutable `relationships-factions` fixture. Every run receives the harness's integrity-checked disposable copy, so no relationship scenario, cassette, profile, or source save changes. The fixture still validates at `sha256:a1ca40e1ed52ed88401b403fa38075bbd273dd13c5bc3341a70ddf5bc5de69c2`.

Live-record attempt 3, strict no-provider replay attempt 4, and independent live-Qwen attempt 5 each passed 43/43 assertions. Both live responses naturally identified `Whistle of the Ridge` and whiteout signaling. The server stripped the leading `?` from the saved question, appended exactly `user-question` then `storyteller-answer`, and added no player action, event summary, random event, NPC turn, or slop-remover entry. Player mechanics, the three fixture NPCs, witness inventory ownership, locations, regions, things, factions, schedules, and world time remained semantically unchanged. Replay consumed its one strict v2 entry without provider traffic, failure, or remainder.

Attempt 1 preserved a correct answer but exposed two test-only assumptions: all character `lastUpdated` values refresh at request receipt, and preloading the default model caused an avoidable switch to the question route's configured prose model. Attempt 2 then proved every semantic invariant but expected raw inventory ids where the client projection deliberately expands item objects. Only the new ALT-1 assertion/config was corrected; no production behavior or existing artifact was changed. The accepted cassette SHA-256 is `12e1d13e166518d6c91551dd874c50407daf306904f571c244496cb0fa0ae849`.

## ALT-2 creative dispatch and authoritative bookkeeping

`scenarios/alternative-prompts/ALT-2-creative.json` is a separate ALT-2-owned scenario. It copies the reusable mechanics from ALT-1 but does not edit ALT-1, any relationship case, or the immutable `relationships-factions` fixture. Each run restores the fixture through the integrity-checked disposable runtime path and asks QA Lantern Witness for a creative demonstration of her existing `Whistle of the Ridge`, avoiding redundant Prompt Witness/Coin setup.

Live-record attempt 4, strict no-provider replay attempt 5, and independent live-Qwen attempt 6 each passed 51/51 mechanical assertions. Both live requests selected `creative_mode_action`, used its TinyBrain program, appended exactly one assistant `player-action` rather than question/generic history, advanced no time, emitted no event, and retained the player, all three fixture NPC placements, whistle ownership, relationships, factions, quests, needs, statuses, schedules, locations, regions, and things. The response contains no fabricated plausibility or action-resolution payload. Replay consumed all three staged completions with no provider traffic, strict failure, queued work, or remainder.

The first executable attempt failed before transport because the derived TinyBrain progress label had no default target. `creative_mode_action*` now covers both direct and staged labels, with a focused default-config regression. The next live completion exposed a separate dispatch defect: when plausibility checks were disabled, the fallback manufactured automatic-success plausibility and skill-check rows even though creative actions are authoritative. That fallback now applies only to ordinary nonempty player actions. Neither fix changes generated prose or model routing. Independent prose did invent a weather-clearing ability absent from the whistle's state; the complete sample and authoritative context are logged as `PROSE-006` for joint review instead of becoming a prompt micro-rule, prose heuristic, or pass-seeking retry. The source fixture remains `sha256:a1ca40e1ed52ed88401b403fa38075bbd273dd13c5bc3341a70ddf5bc5de69c2`; the three-entry cassette hashes to `ed20844ddf3f9fa5885d24ac3fa032eafe6ea31b08996688dc65c422acfec8a7`.

## ALT-3 forced-event dispatch and exact application

`scenarios/alternative-prompts/ALT-3-forced-event.json` and the `alternative-prompts-forced-event` live/replay profiles are separate ALT-3-owned assets. The scenario restores the same immutable `relationships-factions` fixture through the integrity-checked disposable runtime path, but it does not modify ALT-1, ALT-2, a relationship scenario, or the fixture. It reuses QA Lantern Witness and her existing `thing_283` rather than generating a new Prompt Witness/Coin solely for this test.

Live-record attempt 2, strict no-provider replay attempt 3, and independent live-Qwen attempt 5 each passed 54/54 assertions. The `!!` route returned its exact code-owned acknowledgment, set only forced-event dispatch flags, produced one parser-approved `transfer_item`, moved the same whistle from QA Lantern Witness to Baato, retained a zero thing-count delta, and stored one matching `event-summary`. World time, actor placement, relationships, factions, schedules, locations, regions, and unrelated player mechanics remained unchanged. No generated player-facing prose or plausibility/action-resolution attachment was introduced.

Replay consumed the complete ten-entry strict v2 cassette with no provider inference, failure, or remainder. It includes a wrong-category answer that the stage parser rejects and the corrected retry, proving the retry path deterministically. Attempt 1 exposed only a new-scenario mistake—the API exposes the forced structure at `debug.forcedEventStructured`, not `events`—and the case-owned assertion was fixed. Independent attempt 4 hit a transient router-switch `ECONNRESET`; normal transport retry completed all mechanics, and the unchanged clean attempt 5 passed without a new error log. No production prompt, parser, model route, or prose policy changed. The fixture remains `sha256:a1ca40e1ed52ed88401b403fa38075bbd273dd13c5bc3341a70ddf5bc5de69c2`; the cassette SHA-256 is `cacdc6dbbcfe2f2d8516a66f6b8bb159c06224e3ff6277651a11bc547b9587c0`.

## ALT-4 normal generic prompt and read-only tool loop

`scenarios/alternative-prompts/ALT-4-generic.json` was created by copying the proven ALT-1 isolation structure into a new ALT-4-owned file and changing that copy. It uses the shared alternative-mode profile unchanged and restores an integrity-checked disposable copy of `relationships-factions`. The prompt calls `moreInfo` for the existing offscreen QA Private Contact, so this mode gets a real read-only generic tool-loop exercise without creating Prompt Coin/Witness replacements or mutating an existing fixture entity.

Live-record attempt 1, strict no-provider replay attempt 2, and independent live-Qwen attempt 3 each passed 54/54 assertions. Both live answers correctly placed QA Private Contact at QA Fast Travel Tower. Each request selected base-context `generic_prompt` with normal storage, stripped the leading `@`, invoked only one `moreInfo({ name: "QA Private Contact", type: "character" })`, stored `user-generic-prompt` → excluded `tool-call-debug` → `generic-prompt-response`, bypassed slop/event/random/NPC processing, and changed no tracked mechanics, world entity, schedule, location, faction, ownership, or time. Replay consumed both entries without inference, strict failure, queued work, or remainder. No prose issue, production fix, prompt change, profile change, or routing change was needed. The source fixture remains `sha256:a1ca40e1ed52ed88401b403fa38075bbd273dd13c5bc3341a70ddf5bc5de69c2`; cassette SHA-256 is `a170f03b52dc40d4064218e7c4e0b2cce7178d98d975e6e9e5bdae04d13cfdc5`.

## ALT-5 elevated generic mutation and restoration

`scenarios/alternative-prompts/ALT-5-elevated-generic.json` was created as a separate copy of the proven ALT-4 isolation scenario, then changed only in the new case-owned file. It restores the same immutable `relationships-factions` fixture through the harness's integrity-checked disposable runtime copy and reuses the existing `thing_283` Whistle of the Ridge. It never creates a redundant Prompt Coin, changes the source fixture, or repurposes an ALT-1 through ALT-4 asset.

Live-record attempt 3, strict no-provider replay attempt 4, and independent live-Qwen attempt 5 each passed 74/74 assertions. The first `@@` prompt called `updateObjectFields` exactly once to change only `shortDescription` to `ALT-5 temporary whistle marker.`; the second called it exactly once to restore `Bone whistle for signaling location in whiteout conditions.` Both full tool loops stored stripped `user-generic-prompt` → excluded `tool-call-debug` → excluded `generic-prompt-response`, with exact completed tool arguments and structured result metadata. The final Thing projection matched every original semantic field, while its normal `lastUpdated` mutation timestamp advanced. Thing count, player mechanics, locations, regions, factions, schedules, world time, placement, ownership, and unrelated history types stayed unchanged.

Both recording acknowledgments and both independent acknowledgments briefly and correctly described their matching tool results. Replay consumed all four ordered completions with no provider inference, strict failure, queued work, or remainder. The source fixture still validates at `sha256:a1ca40e1ed52ed88401b403fa38075bbd273dd13c5bc3341a70ddf5bc5de69c2`; cassette SHA-256 is `6524e4bb40c20668b2ad977bc89c80efdf061e1f776b5a0045528aba84adb642`. No production code, prompt, parser, shared profile, model route, or prose policy changed.

## ALT-6 highest-context/no-log and no-context modes

`scenarios/alternative-prompts/ALT-6-highest-no-context.json` was created by copying ALT-5 into a new ALT-6-owned file and changing only that copy. It restores the immutable `relationships-factions` fixture through an integrity-checked disposable runtime. The scenario seeds a unique untyped context canary, exercises `@@@` with a reversible two-request mutation, and then exercises `\` with an exact context-free answer. No fixture, ALT-1 through ALT-5 source asset, or shared profile is repurposed or modified.

Live-record attempt 4, strict no-provider replay attempt 5, and independent live-Qwen attempt 6 each passed 69/69 assertions. The `@@@` prompt saw the canary and full tool schema, changed the existing whistle's `shortDescription`, restored its exact original semantic value, and persisted no user, assistant, `tool-call-debug`, or `check-results` rows. The `\` prompt exposed no tools, omitted the canary and fixture entity names from its rendered raw template, returned `NO_CONTEXT_READY`, and persisted only an excluded `user-generic-prompt` followed by an excluded `generic-prompt-response`. Both bypassed slop/event/turn processing, advanced no time, and left all semantic game state unchanged.

Attempt 2 found a production contract defect rather than a prose issue: `@@@` suppressed its ordinary user/assistant history but its tool loop could still persist diagnostic rows. `api.js` now applies one request-local no-log flag to user, assistant, tool-debug, and check-results persistence while preserving immediate structured `toolInvocations` and normal prompt logging. Attempt 3 corrected only ALT-6's case-owned assertion for the zero-tool mode's intentionally absent `debug.toolInvocations`. Focused base-context/history, chat tool-loop, harness, and completion-cassette tests all pass. Replay consumed 4/4 ordered entries without inference or remainder. No production prompt, parser, model route, or prose policy changed. Cassette SHA-256 is `ecbbeafe70872e494f75d3c644a5468afff9442f32ab3e58e669cb95eeae189a`.

## ALT-7 interactive forced skill roll

`ALT-7-interactive-roll-low.json` and `ALT-7-interactive-roll-high.json` are separate case-owned copies of the proven HIDE forced-roll scenario structures. Only the new copies were changed. Each restores an independent integrity-checked disposable copy of `relationships-factions`, and the low/high branches submit identical ordinary player-action text. The ALT-7-owned live/replay profile copies enable the required plausibility/check path without altering the shared alternative-mode profiles. Source HIDE scenarios and shared profiles retain their recorded hashes.

Both branches passed 40/40 assertions at all three gates: low live-record/replay/independent attempts 5/6/7 and high attempts 1/2/3. Each emitted exactly one integer `player_input_request`, received exactly one successful `/api/chat/user-input-response`, called `resolveSkillCheck` once, wrote one matching check-results row plus one tool-debug row, and stored the exact action with `<f>` removed. The injected die 1 failed; die 20 succeeded. Ordinary action time and resulting need decay were accepted, while all tracked entities, placements, inventory, factions, schedules, and other game state remained unchanged.

Human review found both recording and independent prose synchronized with the mechanics: the low branch did not find the mark and the high branch found it. Early attempts exposed only ALT-7 assertion overreach, not production defects: `Perception` may validly omit an attribute or use a suitable model-selected attribute; a low raw die does not guarantee one exact failure degree after bonuses; and the actor may be represented as `player` or the canonical player name. Those representation assumptions were removed only from the ALT-7 copies while exact roll injection, one-check cardinality, and outcome agreement stayed strict. Both 30-entry strict v2 cassettes replayed completely with no provider inference, failure, or remainder. The low cassette SHA-256 is `be3c71507afa1dbe1cfdc3bd1c78cfb79621ec312cf1c72b0d13335c903cdf74`; the high cassette SHA-256 is `975e1492b30763ff537d7d3d0a77ade7b77bf69206d59fe3f446b59a7c48478e`. No production code, prompt, parser, model route, or prose policy changed.

## ALT-8 explicit player-agency rejection

`ALT-8-explicit-rejection.json` was copied from ALT-2's proven isolation/state-audit structure into a new ALT-8-owned file and changed only there. It restores a disposable copy of `relationships-factions` and attempts to dictate movement, item surrender, and permanent obedience for the offscreen unrelated QA Private Contact without any player action or control ability.

Live-record attempt 2, strict replay attempt 3, and independent live-Qwen attempt 4 each passed 49/49 assertions. The first TinyBrain acceptance checkpoint returned a clear one-sentence structured rejection. The server made the visible response equal to the structured reason, marked both user and response rows `excludeFromBaseContextHistory`, added exactly one plausibility attachment, and stopped before tool calls, check results, slop removal, time, event/random/NPC processing, or semantic mutation. Contact placement, inventory, relationships/dispositions, and all tracked world/player state stayed exact. Replay consumed its complete two-entry strict cassette without provider inference or remainder. Attempt 1 changed only ALT-8's copied assertion to ignore normal player hydration timestamps while retaining explicit semantic comparisons. ALT-2 retains its original hash. No production code, prompt, parser, model route, or prose rule changed. Cassette SHA-256 is `6fc7b286723683c9ec583483a80a00a6d9bcb35499acce3bddfba8f744c4bc6d`.

## ALT-9 impossible checked action

`ALT-9-impossible-checked-action.json` was copied from ALT-7's proven low-roll structure and changed only in its new case-owned file. Its live and replay profiles are separate ALT-9-owned copies as well. ALT-7 scenarios, profiles, cassettes, and the immutable relationship fixture were not altered.

Live-record attempt 4, strict no-provider replay attempt 5, and independent live-Qwen attempt 6 passed the complete scenario. Both live runs accepted lifting the anchored March Start Marker as an attempted action, requested exactly one integer input, and injected die 1 into exactly one unopposed Legendary Strength check. The authoritative result was critical failure at total 13 against DC 30; every tracked entity, placement, inventory, faction, schedule, and marker field remained unchanged. Both narrations agreed that the marker did not move and introduced no second player action. Replay consumed 30/30 ordered completions with no inference, failure, or remainder.

Attempt 3 failed only because the copied assertion required the check to retain a named `Construction` skill. The mechanics correctly modeled the raw feat as Strength with no skill. That overconstraint was removed solely from ALT-9 while exact tool cardinality, difficulty, roll injection, failed outcome, stored marker-free input, check/debug history, and no-mutation requirements remained strict. No production code, prompt, parser, model route, or prose policy changed. The source cassette SHA-256 is `1cd27c256ae7efb2817d4a4c020679907b84b4906e49200832f4b3c27d6524a9`.

## ALT-10 structured tool errors and recovery

`ALT-10-tool-errors.json` was copied into a separate ALT-10-owned scenario, with separate live/replay profiles and cassette. The ALT-4 source scenario and shared alternative mechanics profile retain their exact hashes. The test uses the existing relationship fixture through the disposable restore path rather than generating replacement entities.

Live-record attempt 3, strict no-provider replay attempt 4, and independent live-Qwen attempt 5 passed. One deliberately broad `moreInfo({name:"a"})` query matched 303 records and exceeded the 50-result limit; one `getFullScene({sceneNumber:999999})` lookup requested a nonexistent scene. Both failures were returned as structured tool errors in one two-call diagnostic lifecycle, the model recovered with a clean response, and the server remained healthy. Time and every tracked semantic world field stayed unchanged. Replay consumed its complete cassette without inference or remainder. No prompt, parser, routing rule, or prose policy changed. Cassette SHA-256 is `a2d4df6ad1fcb366c09f180a8b0070c0a77971d4bb5a1d910670cbdd49427651`.

## ALT-11 active and pending-input cancellation

The long-generation and pending-roll branches are distinct ALT-11-owned scenario/profile copies. They restore independent disposable copies of the immutable relationship fixture and never share a mutated runtime or alter another case's scenario, profile, fixture, or cassette.

The live-only long branch passed on attempt 2. It started an 8,000-word no-context request asynchronously, observed active prompt progress, cancelled and drained it, and awaited the original request. The request returned the documented empty-response failure, persisted only the excluded user-generic row, left no assistant response, and changed no world state or time.

The pending-roll branch's live-record attempt 4, strict replay attempt 5, and independent live-Qwen attempt 7 passed. It deferred automatic handling of `<f>`, observed the `player_input_request`, cancelled the prompt, and then awaited the original chat. The cancellation endpoint returned 200 with one cancellation and an empty drain; the chat returned `Prompt canceled by user`. Exactly one check-results lifecycle and one tool-debug lifecycle ended in `error`, with tool error code `user_input_cancelled`; no diagnostic remained `running`, and no player action, event, time, or world mutation survived.

This exposed a general production lifecycle bug: fatal tool execution previously rethrew before finalizing its live diagnostics. `chat_tool_calls.js` now emits the terminal error phase with the original message/code and then propagates the failure. The harness also gained paired `startChat`/`awaitChat`, mechanical `waitForRealtime`, deferred player-input handling, minimum matching-array counts, and failure cleanup for outstanding requests. Focused unit coverage protects both the lifecycle and scenario validation/execution. Replay consumed all 13 ordered entries; cassette SHA-256 is `ee9aa4149360dc3b0c68587533c174816f7cc626a5b1b38b4d0e87c83155e856`. No prose or routing change was made.

## ALT-12 prompt-history contract matrix

`ALT-12-history-audit.json` was copied from ALT-6 into a separate ALT-12-owned scenario and changed only there. Its profiles and cassette are also case-owned copies. ALT-6, the shared mechanics profile, its cassette, and the immutable relationship fixture retain their recorded hashes.

Live-record attempt 2, strict no-provider replay attempt 3, and clean independent live-Qwen attempt 5 passed. The scenario seeds one ordinary comment and then exercises `@@`, `\`, `@@@`, normal `@`, and `?` in sequence. It proves that ordinary prompts see the ordinary comment; `@@` is excluded from ordinary history but included in later generic all-entry context; `\` receives neither history context nor tools; `@@@` receives all-entry context but persists no user, assistant, tool-debug, or check-results row; normal `@` sees prior excluded/no-context rows but not the ephemeral `@@@` response; and the following ordinary question sees the ordinary comment and normal generic response while excluding the hidden/no-context/no-log material. The exact nine-row stored sequence and all exclusion flags passed, as did the 338-row full history, the 279-row turn-pruned ordinary history, no-log absence, and unchanged state/time.

Attempt 1 corrected only new-case assumptions: stored comment text retains its leading `#`, and the ordinary history limit selects turns rather than imposing a raw row count. Live attempt 4 recovered from a transient router `ECONNRESET`; that run was neither allowlisted nor accepted, and the unchanged attempt 5 passed cleanly. A focused base-context regression independently covers ordinary, all-entry, excluded, diagnostic, and no-log contracts. Replay consumed all five entries. Cassette SHA-256 is `d9c8651f52de149ed061be0792a9641446b7998943add3783253a2e685bdab56`. No production behavior, prompt, parser, prose rule, or model routing changed.

## COMBAT-1 vertical slice

`scenarios/combat/COMBAT-1-hostile-npc-attack.json` uses the immutable `combat-elemental-skirmish` fixture. It restores Baato with QA Ash Beetle, QA Frost Beetle, and QA Shieldhand at the isolated combat location, forces die 12 through the player action, and asserts the exact acting attacker, one `resolveAttack` invocation/summary, positive applied damage, surviving-player health loss, unchanged attacker health, one check-results entry, one player-action entry, no duplicate NPC turn, and no realtime harness error. Narrative attribution and single-attack fidelity remain explicit human-review items.

Live verification originally led to a temporary one-attack prose micro-policy. The later prompt audit removed that policy: multiple attacks by one or more actors are deliberate supported behavior, and each attack that survives into prose must receive its own mechanical resolution. The tool result no longer appends a vocabulary-based prose scope note, and no application or harness code scans prose to classify attack wording. Human-review disagreements about narration belong in the prose-issues log rather than becoming case-specific prompt restrictions.

The source-controlled replay trace is `cassettes/combat/COMBAT-1-hostile-npc-attack.json` (SHA-256 `35301680ec858750eb6fbc0005973827a89180064e48178e09bbde741b96d683`). Final 2026-08-10 evidence is:

- live record `attempt-20`: 51/51 mechanical assertions and human-reviewed one-attack prose; 40 completed cassette entries;
- strict replay `attempt-21`: 51/51 assertions, 40/40 entries consumed, zero latched failures, empty cassette queue, and no llama.cpp process;
- independent live-Qwen `attempt-22`: 51/51 assertions plus one lunge/bite followed only by withdrawal/posture, with no absent character attacking.

## COMBAT-2 vertical slice

`scenarios/combat/COMBAT-2-friendly-npc-attack.json` reuses the immutable `combat-elemental-skirmish` fixture under `combat-friendly-only`. It isolates Frost Beetle, gives QA Shieldhand deterministic combat stats and intent through the normal NPC update API, then has Baato begin combat without taking an attack action. The turn must produce exactly two independently attributed mechanical attack results—QA Ash Beetle against Baato and QA Shieldhand against QA Ash Beetle—while legacy prompt checks are disabled. Both result applications must match canonical before/after health, Shieldhand must remain undamaged, and already-acted combatants must not receive a duplicate scheduled NPC turn. Prose attribution, one-impact fidelity, and survival wording remain human review only.

Independent verification exposed a harness assumption rather than a production defect: Qwen validly represented Shield Bash once as a one-target `resolveAreaAttack` while the scenario required two `resolveAttack` calls at fixed indexes. `attackResultCount` and `attackResultApplied` now normalize both supported attack tools by actor and per-target application. They compare only structured mechanics and authoritative state; they do not inspect prose. The focused `api.combat_npc_turns_config.test.js` regression separately proves that TinyBrain independent NPC turns pre-resolve their own attack mechanics even when legacy prompt checks are disabled.

The source-controlled replay trace is `cassettes/combat/COMBAT-2-friendly-npc-attack.json` (SHA-256 `d9cddf0e72f9c37f302b17d98607f1f1fac9c71873eef64c01e40eaf2cca68f0`). Final 2026-08-10 evidence is:

- live record `attempt-6`: 51/51 original mechanical assertions, human-reviewed separate bite/shield-strike prose, and 40 completed cassette entries;
- strict replay `attempt-10`: the current 43/43 normalized assertions, 40/40 entries consumed, zero latched failures, empty cassette queue, and no llama.cpp process;
- independent live-Qwen `attempt-9`: 43/43 normalized assertions plus one bite and one shield impact, no duplicate scheduled turn, and no character declared dead contrary to state.

## Fix loop

1. Restore/promote one pre-case fixture and run one isolated `live-record` reproduction.
2. Preserve the completed cassette and triage artifacts.
3. Add the smallest parser/domain/API regression for the identified boundary.
4. Start the server with the replay profile and iterate using strict whole-turn replay without llama.cpp or ComfyUI.
5. Confirm an intentional prompt/contract change produces a fingerprint mismatch rather than stale-output reuse.
6. Restart with the intended Qwen/router profile, run `live-verify`, and perform the listed human prose review.
7. Use focused Playwright only when the contract is browser-visible; API/state correctness remains in this harness.
