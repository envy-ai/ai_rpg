# Game Lifecycle API

Common payloads: see `docs/api/common.md`.

This page covers game lifecycle, save/load, mod state, active calendar, per-game config override, summary-style, and short-description backfill routes. Most routes are registered in `api.js`; `PUT /api/game-config-override` is registered in `server.js`. There is no standalone `/api/game` route in the inspected implementation.

## POST /api/new-game

Start a game from the active world setting.

Request:
- Body supports: `playerName`, `playerDescription`, `playerClass`, `playerRace`, `playerLevel`, `startTime`, `startingLocation`, `startingCurrency`, `attributes`, `skills`, `clientId`, `requestId`
  - `startTime` must be an integer hour from `0` through `23`. Omitted values fall back to the active setting/default flow, then to `9`.
  - `attributes` and `skills` are optional starting player value maps. Available skill definitions come from the active setting's configured skill list, not from the request body.
- `numSkills` and `existingSkills` are rejected with 400; configure available skills in Game Settings.
- `unspentSkillPoints` and `unspentAttributePoints` are rejected with 400 because point pools are formula-derived at read time.

Response:
- 200: `{ success: true, message, player, startingLocation, region, factions, skills, pendingAbilitySelection, introDeferred, gameState, worldTime }`
  - `player`: `Player.toJSON()`
  - `startingLocation`: location JSON plus `pendingImageJobId` and `npcs`
  - `region`: region JSON
  - `factions`: faction JSON entries generated by the prompt-backed remainder of setup. Preconfigured setting factions are loaded into runtime state but are not an authoritative full-registry payload in this field.
  - `skills`: skill definition JSON entries loaded from the active setting list
  - `pendingAbilitySelection`: ability-selection state when startup choices are required; otherwise `null`
  - `introDeferred`: `true` when the opening scene waits for startup ability choices
  - `gameState`: `{ totalPlayers, totalLocations, currentLocation, regionEntranceId }`
  - `worldTime`: active world-time payload, including local weather fields when available
- 400: `{ success: false, error }` for missing active setting or invalid/disallowed request fields.
- 500: `{ success: false, error, details }`

Behavior:
- The route clears any loaded game's `gameConfigOverride.yaml` layer by reloading merged config and defs with an empty per-game override before reading defaults or generating world state.
- The in-memory new-game reset clears prior save-scoped world maps for players, things, locations, exits, regions, factions, skills, pending region stubs, generated image metadata, image job queues, chat history, chat summaries, and scene summaries.
- The reset also clears model static indexes for players, things, locations, quests, regions, factions, mystery boxes/threads, scheduled events, and trackers before generating the new starting region, so absolute world-minute records and stale indexed objects from an old game cannot leak into new-game prompt context.
- Pending player-input requests and quest confirmations are rejected when a new game starts. Active image jobs receive a runtime-generation id; jobs from an older generation are ignored if they start or finish after the reset instead of mutating the new game.
- The newly created player is installed as the active `Globals.currentPlayer` before starting-region generation, so prompt context and world-time helpers read the new player's timestamps instead of any previous save's player.
- Need-bar prompt sentences are validated in strict mode before existing game state is cleared. Missing active need-bar `sentence` values fail the request.
- When `clientId` is supplied, realtime status events are emitted during generation. `requestId` is passed through the stream emitter when present.
- Player defaults come from the active setting, then hardcoded fallbacks.
- Faction setup uses active-setting drafts and counts before config fallback:
  - `defaultFactions` are loaded first, up to the target count.
  - `defaultFactionCount` sets the target count when it is a non-negative integer.
  - If no target count is set and drafts exist, the draft count is used.
  - `config.factions.count` is the fallback target; missing/invalid config defaults to `5`.
  - A target count of `0` disables faction setup.
  - When the target exceeds available drafts, the remainder is generated through faction prompts.
  - Each faction receives relation entries for every other active faction; missing or invalid relation entries normalize to neutral.
- Calendar setup uses the active setting's `calendarDefinition` when present. Otherwise the server runs the `calendar_generation` prompt and falls back to the built-in Gregorian-style calendar if generation fails.
- The generated calendar prompt requires Gregorian output for reasonably Earth-like settings and asks for seasons, seasonal time-of-day lighting descriptions, and holiday descriptions.
- The selected start hour is applied through the minute-based world-time path.
- Startup ability-selection state is resolved before the opening scene.
- If startup ability choices are pending, the game-loaded gate is set and the intro is deferred until the final ability-selection submission.
- If no startup ability choices are pending, `game_intro` runs immediately, stores visible assistant prose as a `type: game-intro` chat entry, applies slop cleanup when configured, logs the prompt, and attempts the standard autosave path.
- Intro generation failures are logged and do not fail game creation.

## POST /api/new-game/settings/save

Save a New Game form configuration to disk.

Request:
- Body: `{ saveName?: string, settings: NewGameFormSettings }`
  - `settings` supports: `playerName`, `playerDescription`, `playerClass`, `playerRace`, `playerLevel`, `startTime`, `startingLocation`, `startingCurrency`, `attributes`, `skills`

Response:
- 200: `{ success: true, saveName, saveDir, metadata, message }`
- 400/500 with `{ success: false, error }`

## POST /api/new-game/settings/load

Load a saved New Game form configuration.

Request:
- Body: `{ saveName: string }`

Response:
- 200: `{ success: true, saveName, metadata, settings, message }`
- 400/404/500 with `{ success: false, error }`

## GET /api/new-game/settings/saves

List saved New Game form configurations.

Response:
- 200: `{ success: true, saves, count, directory }`
  - `saves` entries include `saveName`, `timestamp`, `source`, `saveFileSaveVersion`, `playerName`, `playerLevel`, `currentSettingName`, `totalAttributes`, and `totalSkills` when present in metadata.
- 500 with `{ success: false, error }`

## POST /api/save

Save the current game.

Response:
- 200: `{ success: true, saveName, saveDir, metadata, message }`
- 400 with `{ success: false, error }` when no current player exists.
- 500 with `{ success: false, error }` for save failures.

Behavior:
- Save names are generated from timestamp, setting, player, location, and a uniqueness fragment.
- The serialized save directory contains world state, chat history, images, things, players, factions, mystery boxes, mystery threads, scheduled events, trackers, skills, metadata, pending region stubs, world time, calendar definition, per-game config override YAML, setting data, chat summaries, and scene summaries.
- Metadata includes totals for players, things, locations, exits, regions, factions, mystery boxes, mystery threads, generated images, and skills.
- Metadata includes `enabledMods`, the startup-frozen active enabled mod directory names.
- Metadata includes `npcAliasesGenerated`, normalized to `true` only when runtime metadata explicitly has `true`.
- Metadata includes current setting identifiers, current location identifiers, summary style, prompt counters, plot analysis, offscreen NPC activity state, save-file version, and ID counters.
- `gameConfigOverride.yaml` persists the active per-game YAML override exactly as normalized by `Globals.setGameConfigOverrideYaml`.
- Manual saves use `saves/`. Autosaves use `autosaves/` through the shared save helper and may omit a duplicate final chat entry when it matches the last autosave.

## POST /api/load

Load a saved game.

Request:
- Body: `{ saveName: string, saveType?: 'autosaves'|'saves', clientId?: string, modMismatchChoice?: 'keep-current' }`
  - `saveType` defaults to `saves`; only the string `autosaves` selects the autosave root.
  - `modMismatchChoice: 'keep-current'` bypasses a detected mod-enable mismatch for this load.

Response:
- 200: `{ success: true, saveName, source, metadata, loadedData, message }`
  - `loadedData`: `{ currentPlayer, totalPlayers, totalThings, totalLocations, totalLocationExits, chatHistoryLength, totalGeneratedImages, currentSetting, worldTime }`
  - `currentPlayer` uses the serialized client NPC/player profile shape.
- 409: `{ success: false, code: 'MOD_ENABLEMENT_MISMATCH', error, modMismatch }`
  - `modMismatch`: `{ hasMismatch, activeEnabledMods, savedEnabledMods, missingFromActive, extraActive }`
- 400/404/500 with `{ success: false, error }`

Behavior:
- `saveName` is rejected with `400` (`INVALID_SAVE_NAME`) when it contains `/`, `\`, or `..`; save names never escape the save root directory.
- Saves with `metadata.enabledMods` are compared against the startup-frozen active enabled mod list before hydration. Saves without that metadata skip the mismatch check.
- `missingFromActive` lists saved mods that are not active in the running server. `extraActive` lists running active mods not recorded in the save.
- The save's `gameConfigOverride.yaml` is applied through the same merged-config reload path used by `/reload_config` before hydration. Need-bar prompt-sentence validation runs in strict mode for load.
- Load clears transient job queues and image-generation queues before hydrating serialized state.
- Hydration includes compatibility migrations for hour-based world-time/status-duration data, pre-`1.1` need-bar scale values, and pre-`1.2` compact domain-object IDs such as `char_n`, `thing_n`, and `loc_n`.
- The in-memory save metadata version is normalized to the current save version after hydration so the next save persists upgraded data.
- Missing saved `calendarDefinition` data is filled from the loaded setting's `calendarDefinition` when available, otherwise through `calendar_generation` with Gregorian fallback.
- If the loaded setting lacks required hide/perception selections, the `setting_hide_perception` prompt fills them from defined attributes and optional setting skills. Invalid required prompt results fail load; successful backfills update the setting and rewrite the loaded save.
- Faction references are reconciled before restoring the current player. Invalid faction IDs are cleared from player faction fields, player standings, location/region/pending-stub controlling faction IDs, and faction relation edges.
- Party members are removed from explicit location NPC lists and have direct location state cleared while remaining in the player's party.
- Missing image files clear affected `imageId` fields for things, players, current player inventory, and NPCs/items in the current location.
- Metadata totals are refreshed from hydrated registries.
- Chat backlog summaries run when `summaryConfig.summarize_on_load` is not `false`.
- Short-description backfill planning runs after hydration. When `clientId` is present and missing short descriptions exist, `/api/short-descriptions/pending` can report the plan for that client.
- Pending player level-up ability draft state is resolved without generating option text; option generation runs from `/api/player/ability-selection` when requested.

## GET /api/mods/manager

Return discovered mods and configured/runtime enablement state.

Response:
- 200: `{ success: true, modState }`
  - `modState.mods[]`: `{ name, dir, hasModJs, hasDefsDir, configPath, configuredEnabled, activeEnabled, restartRequired, fileConfigEnabled }`
  - `modState.activeEnabledMods`: startup-frozen active enabled mod names
  - `modState.configuredEnabledMods`: enabled mod names from the current merged config view
  - `modState.restartRequired`: `true` when configured enablement differs from the startup-frozen active mod set
- 500 with `{ success: false, error }`

## PUT /api/mods/enabled

Write the selected enabled mod set to `config.yaml` and reload merged config/defs.

Request:
- Body: `{ enabledMods: string[] }`

Response:
- 200: `{ success: true, enabledMods, modState, reloadResult, restartRequired }`
- 400 with `{ success: false, error }` for invalid names, unknown mods, invalid payloads, or reload failures.

Behavior:
- The route writes explicit `mods.<name>.enabled` flags for every discovered mod.
- Runtime config and defs reload immediately with the current per-game YAML override and warning-mode need-bar sentence validation.
- The startup-frozen active mod set does not hot-toggle; `restartRequired` reports whether the server needs a restart for mod hooks, defs overlays, or assets to match configuration.

## POST /api/mods/apply-save-config

Apply a save's persisted enabled mod list to `config.yaml` and prepare that save to load after restart.

Request:
- Body: `{ saveName: string, saveType?: 'autosaves'|'saves' }`

Response:
- 200: `{ success: true, enabledMods, modState, reloadResult, pendingLoad, restartRequired, selfRestartStarted, manualRestartRequired, restartResult }`
- 400/404/422 with `{ success: false, error }`

Behavior:
- `saveName` is rejected with `400` (`INVALID_SAVE_NAME`) when it contains `/`, `\`, or `..`; save names never escape the save root directory.
- The save must contain `metadata.enabledMods`; saves without it cannot be applied automatically.
- The route writes `tmp/pending-load.json` before restart handling. The pending-load client script consumes that intent on startup and posts `/api/load`.
- `server.allowSelfRestart: true` allows the route to spawn a replacement server process. Otherwise the response sets `manualRestartRequired: true`.

## GET /api/pending-load

Return the pending post-restart load intent, if one exists.

Response:
- 200: `{ success: true, pendingLoad: null|{ saveName, saveType, reason, createdAt } }`
- 500 with `{ success: false, error }`

## DELETE /api/pending-load

Clear the pending post-restart load intent.

Response:
- 200: `{ success: true, cleared: boolean }`
- 500 with `{ success: false, error }`

## GET /api/calendar

Return the active in-game calendar definition and current world-time payload.

Response:
- 200: `{ success: true, calendarDefinition, worldTime }`
- 500: `{ success: false, error }`

Behavior:
- `calendarDefinition` is the normalized persisted calendar object used for date, season, holiday, and light-level resolution.
- `worldTime` is built through the shared world-time payload path and includes current-location weather fields when available.

## PUT /api/calendar

Replace the active in-game calendar definition.

Request:
- Body: `{ calendarDefinition: object }`

Response:
- 200: `{ success: true, message, calendarDefinition, worldTime }`
- 400: `{ success: false, error }` when `calendarDefinition` is missing, not an object, an array, or fails calendar validation.
- 500: `{ success: false, error }`

Behavior:
- Validation uses the same calendar normalization rules as game creation and load hydration.
- Invalid calendars fail without mutating the active calendar.
- The current `{ dayIndex, timeMinutes }` is preserved; only date/season/holiday/light interpretation changes.
- The returned `calendarDefinition` is the normalized version persisted on the next save.

## PUT /api/game-config-override

Update the per-game YAML config override for the loaded game and reload merged runtime config immediately.

Request:
- Body: `{ yaml?: string }`
  - Blank or omitted `yaml` clears the override.
  - Non-blank `yaml` must parse to a YAML object.

Response:
- 200: `{ success: true, message, reloadResult, gameConfigOverrideYaml }`
- 400: `{ success: false, error }`

Behavior:
- Requires `Globals.gameLoaded === true`.
- Merge precedence is `config.default.yaml`, `config.yaml`, CLI `--config-override`, then the per-game YAML override.
- Uses the same config/defs reload path as `/reload_config`.
- Need-bar prompt-sentence validation is warning-only on this live reload path, so bad definitions are reported without failing the running game.
- Mod enable/disable drift requires a restart even though the override reloads immediately.
- The normalized YAML is stored in memory and written to `gameConfigOverride.yaml` on the next save.

## GET /api/saves

List available saves.

Request:
- Query: `type` (`saves` or `autosaves`, default `saves`)

Response:
- 200: `{ success: true, type, saves, count, message }`
  - `saves` entries start with baseline metadata (`saveName`, `timestamp`, `playerName`, `playerLevel`, `source`, `isAutosave`) and merge fields from each save's `metadata.json`, including `plotAnalysis` when present.
- 400/500 with `{ success: false, error }`

Behavior:
- Results are sorted by descending metadata timestamp, with invalid/missing timestamps sorted last.
- Missing save root directories return an empty list.

## DELETE /api/save/:saveName

Delete a manual save from `saves/`.

Response:
- 200: `{ success: true, saveName, message }`
- 400/404/500 with `{ success: false, error }`

Behavior:
- This route targets only the `saves/` root, not `autosaves/`.
- `saveName` is rejected with `400` (`INVALID_SAVE_NAME`) when it contains `/`, `\`, or `..`; deletion never escapes the `saves/` directory.

## POST /api/summaries/style

Update summary style in runtime save metadata and persist it when the loaded game has a save directory.

Request:
- Body: `{ style: 'line' | 'scene' }`

Response:
- 200: `{ success: true, summaryStyle, persisted }`
- 400/500 with `{ success: false, error }`

Behavior:
- Fails with 400 when `style` is not `line` or `scene`.
- Fails with 400 when no save metadata is available.
- `persisted` is `true` only when metadata was written to the loaded save directory.

## GET /api/short-descriptions/pending

Check for pending short-description backfill work for a client.

Request:
- Query: `clientId` (required)

Response:
- 200: `{ success: true, pending, plan }`
  - `plan.counts`: pending `items`, `locations`, `regions`, and `abilities`
  - `plan.prompts`: estimated prompt count per entity type
  - `plan.totalPending`, `plan.batchSize`, `plan.createdAt`
- 400/500 with `{ success: false, error }`

Behavior:
- Plans are populated during `/api/load` when the load request includes `clientId` and missing short descriptions exist.

## POST /api/short-descriptions/process

Run or dismiss pending short-description backfill.

Request:
- Body: `{ clientId: string, action: 'run'|'process'|'skip'|'dismiss' }`

Response:
- 200: `{ success: true, processed: true }` or `{ success: true, skipped: true }`
- 400/404/409/500 with `{ success: false, error }`

Behavior:
- `skip` and `dismiss` clear the client's pending plan without generating descriptions.
- `run` and `process` process pending item, location, region, and ability short descriptions for the loaded game.
- A single global backfill can run at a time; concurrent requests return 409.
